# 專案執行進度與 Claude Code 工作交接備忘錄

**日期**：2026-09-13  
**目標知識圖譜**：KG#4 `236903cf-055a-40a8-8923-b9d06601f3b7`  
**環境設定**：
- **Neo4j**：`bolt://localhost:17990`（Docker container: `kg2-neo4j`）
- **Ollama**：`http://localhost:11434`（WSL2，模型：`qwen2.5:7b` + `bge-m3:latest`，`OLLAMA_EMBEDDING_NUM_GPU=0`）
- **Workspace Runtime**：`$env:WORKSPACE_DIR="D:/Users/666/Desktop/kg-runtime"`
- **工作分支**：`master`

---

## 1. 目前已完成項目與驗收成果

### 1.1 全量重抽收尾（方案 A 100% 完成）
- **處置標的**：對 9 筆 failed chunk（原因 `qwen2.5:7b` 貪婪重複崩潰截斷）套用破迴圈參數（`temp=0.45, rep_pen=1.4, rep_last_n=256, no seed, num_predict=4096`）重抽。
- **成果**：9 / 9 筆全數一次成功，耗時 18.0 分鐘。
- **最終圖譜規模**：
  - Document：64 份
  - Chunk：3,303 個（completed = 3,303, failed = 0, pending = 0，完成率 100.0%）
  - Entity：12,096 個
  - Fact：16,736 筆
  - Entity-Entity 邊：13,953 條
- **存檔備份**：`D:\Users\666\Desktop\kg-backups\kg-runtime-drain-planA-100pct-all-completed-20260912.zip`。

---

### 1.2 L6 R2 實體候選集 Recall 對照（報告 40 §7）
- **執行腳本**：`compare_entity_candidate_recall.py`
- **對照成果**：抽樣 300 個真實法規 mention：
  - 新舊一致率 **100.0%**（300/300 題新候選集完全涵蓋舊候選集）。
  - 候選空間由平均 12,096.0 大幅縮減至 605.6（**候選空間縮減 95.0%**）。
- **產出檔案**：`compare_entity_candidate_recall_result.json`。

---

### 1.3 L9 V1 檢索文件範圍對照（報告 41 §6）
- **執行腳本**：`compare_doc_scope_retrieval.py`
- **對照成果**：8 題中 7 題（Q1~Q6, Q8）新舊皆精確命中 Gold 文件：
  - 實體錨定帶動三元組召回數量提升 3~25 倍。
  - 報告 41 主目標 `26-Q5`（災後保費）正解事實三元組直接位列第 0 名。
  - `26-Q7`（高溫作業特殊健檢頻率）為 Canary 誠實拒答題（原法規本無規定），新版未下推至無關法規，符合語意邊界。
- **產出檔案**：`compare_doc_scope_retrieval_result.json`。

---

### 1.4 報告 39 七路檢索比較準備（SDD-1 ~ SDD-4）
- **修復並構建 B0/B1 原文 Chunk 基準索引**：
  - 修復 `build_baseline_chunk_index.py` 讀取 frontmatter 時 source 殘留雙引號導致比對失敗的問題。
  - 針對 KG#4 完整 64 份文件產生乾淨 chunk 向量索引：
    - `baseline_rag_index_236903cf-055a-40a8-8923-b9d06601f3b7_cs500.npy`（1,181 chunk，dim=1024）
    - `baseline_rag_index_236903cf-055a-40a8-8923-b9d06601f3b7_cs500.json`
- **前導可行性檢查（`check_comparison_readiness.py`）**：
  - **7 條 Arm 全部 ✅ PASS**：
    - `D`（純 LLM）：✅ PASS
    - `B0`（Naive chunk-RAG）：✅ PASS
    - `B1`（Strong chunk-RAG + Hybrid RRF）：✅ PASS
    - `F`（Fact-RAG only）：✅ PASS
    - `G`（BFS only）：✅ PASS
    - `K`（Full KG）：✅ PASS
    - `K-2b`（Full KG 關閉限制性重生）：✅ PASS
  - **8 項系統檢查全數合格**：`original.md` 存在 (6/6)、`baseline .npy` 已建 (1,181 chunks 涵蓋全部)、`pending == 0`、抽取新鮮度合規、`fact_embedding` 100.0% (16,736/16,736)、`Entity.name_embedding` 100.0% (12,096/12,096)、比較文件皆有 Fact 節點、關係型別向量索引正常 ONLINE。

---

### 1.5 T2 驗收測試（報告 27 §6.2 / 報告 30 §2）
- **產出驗收腳本**：`run_t2_acceptance.py`
  - 支援斷點續跑（Checkpoint / Resume），每次呼叫即時寫入 `t2_acceptance_results.json`。
  - 嚴格比對逐題 Rubric（含同單位錯值過濾、正解全中要求），收集延遲、三元組數、Fact 數、重新生成與 Grounding 支持比。
- **重點標的執行現況**：
  - **Q1（N0060029 高架作業§4 列舉分段完整性）**：
    - **條件 A（新 KG）**：**3/3 乾淨通過（✅ 100% PASS）**！
      - Run 1 (215.98s)、Run 2 (179.18s)、Run 3 (115.87s)。
      - 20分、25分、35分 三段齊全完整召回，徹底解決重抽前遺漏「2–5m→20分」的歷史缺陷。
    - **條件 D（純 LLM）**：**0/3 通過（❌ 100% FAIL）**（平均 21.8s，產生幻覺轉引勞基法或答無法規）。
  - **Q2（N0060016 重體力勞動§2 精準命中）**：
    - 條件 A Run 1 通過（159.35s，四十公斤、四點五公斤全中）。

---

## 2. 當前執行中項目（Active Task）

- **T2 全量 8 題 × 3 次驗收測試**：
  - 執行指令：
    ```powershell
    $env:WORKSPACE_DIR="D:/Users/666/Desktop/kg-runtime"
    python run_t2_acceptance.py --runs 3
    ```
  - 當前狀態：後台持續執行中，輸出持續寫入 `t2_acceptance_results.json`。

---

## 3. 接手給 Claude Code 的下一步操作指引

### 步驟 1：檢視 T2 驗收總結
確認 `t2_acceptance_results.json` 中 8 題 × 條件 A/D 的執行結果：
- **Q1**：三段休息（20/25/35分）——已驗證 3/3 乾淨。
- **Q2**：四十公斤、四點五公斤——驗證中。
- **Q3**：三個月、三十日——多事實不混淆。
- **Q4**：三十二小時、新台幣五千元——單位保真。
- **Q5**：災後六個月、自災害發生當月一日起算——起算日事實採納。
- **Q6**：離職退保當日起二年內——低連通度可及性。
- **Q7**：特殊健檢無規定——誠實拒答。
- **Q8**：變量係數 2——多跳推理與範圍修飾詞守衛效果。

### 步驟 2：執行報告 39 七路檢索比較（前導 6 題評測）
由於前導可行性檢查（readiness）已 100% 通過，可直接執行標準化 7 條 Arm 的比對：
```powershell
$env:WORKSPACE_DIR="D:/Users/666/Desktop/kg-runtime"
python run_retrieval_comparison.py `
  --kg-id 236903cf-055a-40a8-8923-b9d06601f3b7 `
  --doc-ids N0030006_勞工請假規則,N0030018_育嬰留職停薪實施辦法,N0090051_受聘僱從事就業服務法第四十六條第一項第八款至第十款規定工作之外國人請假返國辦法,F0040034_員工接受召集請假期間薪資費用加成減除辦法,D0080015_警察人員特別休假辦法,N0050030_災區受災勞工保險與勞工職業災害保險及就業保險被保險人保險費支應及傷病給付辦法 `
  --questions 18-Q1,18-Q2,18-Q3,18-Q4,18-Q5,26-Q5 `
  --arms D,B0,B1,F,G,K,K-2b `
  --runs 3 `
  --chunk-size 500 `
  --out report39_comparison/pilot_6docs
```
*(注意：此命令會依序評測 6 題 × 7 arms × 3 runs，請確保單獨執行以獲得乾淨的延遲數據)*。

### 步驟 3：人工評分與產出報告 40
依據報告 39 §4 指引，對評測輸出的 context 與答案比對 Gold 標準，產出七路檢索評測報告。

---

## 4. 關鍵約束與技術注意項

1. **嚴格禁止改動**：`docs/報告/36_B2AgenticRAG強基準設計.md`（使用者未提交之關鍵設計）。
2. **Neo4j 全文索引注意事項**：`entity_name_fulltext` 索引必須使用 `cjk` analyzer，先前殘留的 `entity_name_ft` (standard) 已刪除。
3. **LLM 並行限制**：WSL2 內的 Ollama 服務同一時間請避免並行兩個大量 prompt 任務，以免造成排隊導致延遲失真。
