# 70 報告68/69批次整合進master任務書（交付Codex）

**建立日期**：2026-09-23
**文件性質**：任務書，交付 Codex 執行，完成後由 Claude Code 複驗。
**觸發脈絡**：2026-09-23 完成過一次大規模跨worktree版控整合，把本分支（`worktree-sdd-retrieval-comparison`）9筆commit分4組cherry-pick進main checkout的master，`origin/master`已fast-forward到`7e67f2b`（詳見 [HANDOVER.md](../../HANDOVER.md) 「2026-09-23：報告68/69收尾 + master整合完成」段落）。當時**明確排除**本worktree同一天新增的報告68（評測harness查詢embedding快取）／報告69（固定metric-judge與受測arm-judge解耦）內容，留待下一輪整合。本任務書就是那個下一輪。

---

## 0. 一頁摘要

**目標**：把報告68／69的production程式異動（`core/providers/factory.py`、`scripts/eval/run_rq1_comparison.py`、`scripts/eval/frozen_baseline_stage.py`及對應測試）比照09-23那次「逐commit審查＋pytest攔截」的流程，安全整合進main checkout的`master`並push。

**已查證的風險等級**：**低**。報告68/69兩份任務書都明確聲明「只動評測harness層與`core/providers/factory.py`新增函式，完全不改`routers/agent.py::chat()`或任何production行為路徑」，且預設（不傳新CLI旗標）零行為變化。這跟09-23整合時踩到的`svo_service.py`衝突（master自己獨立演進的cfg門檻接線功能）性質不同——這次的改動是**純新增函式**，不是修改既有函式簽名，衝突風險理論上較低，但仍要實際比對master當前的`core/providers/factory.py`是否有這段期間的獨立演進，不能假設一定乾淨。

**範圍**：只處理main checkout（`D:\Users\666\Desktop\world knowledge graph rag`，目前在`master`）。**不要**在本worktree（`sdd-retrieval-comparison`）做任何合併操作；本worktree的任務是被cherry-pick的來源，不是整合發生的地方。

---

## 1. 現況（已查證，供直接引用）

1. `origin/master`目前為`7e67f2b2aed17eaa801a9339812a44a045c27b3d`（`chore: ignore all pytest basetemp directories`）。
2. 本分支（`worktree-sdd-retrieval-comparison`）上，以下4筆commit**逐一驗證過`git merge-base --is-ancestor`皆回傳「不是`origin/master`的祖先」**，即尚未以任何形式（含cherry-pick產生的新SHA）進入master：
   - `92d84926f02ec159efb3133b1284b64b444e411f` `feat(報告68): 加入評測 embedding cache`
   - `1f353d802f04e18bcd942ba93c3e31e95e5952ba` `feat(eval): decouple fixed metric judge from test arm judge`
   - `b8f01783ce4cd10eaf2dd1fa29fee0c73db45d2e` `docs(HANDOVER): 報告69驗證完成——...`（純HANDOVER.md文件異動）
   - `313b3d5a6019d86ee913ac0a293f7cf3a133e2e9` `chore: ignore all pytest basetemp directories`（`.gitignore`異動）
3. **兩個核心production異動commit的diffstat**：
   - `92d8492`：`core/providers/factory.py`（+16行）、`scripts/eval/embedding_cache.py`（新檔，+86行）、`scripts/eval/frozen_baseline_stage.py`（+34/-行）、`scripts/eval/run_rq1_comparison.py`（+19行）、`HANDOVER.md`、`docs/報告/68_....md`（新檔）。
   - `1f353d8`：`core/providers/factory.py`（+13行）、`scripts/eval/frozen_baseline_stage.py`（+14行）、`scripts/eval/run_rq1_comparison.py`（+43行）、`tests/core/test_providers_factory.py`（+19行）、`tests/scripts/test_frozen_baseline_stage.py`（+22行）、`tests/scripts/test_rq1_harness_failures.py`（+15行）、`tests/scripts/test_rq1_metric_judge.py`（新檔，+232行）。
4. 兩個commit都**修改同一個檔案`core/providers/factory.py`兩次**（先加`override_embedding_provider_for_eval()`附近的`make_llm_provider_for_eval()`前置基礎，`1f353d8`再加`make_llm_provider_for_eval()`本體），cherry-pick時**必須按時間順序**（先`92d8492`再`1f353d8`），不可調換或只取其中一個。
5. `b8f0178`／`313b3d5`是純文件／`.gitignore`異動，衝突風險最低，但`b8f0178`修改的是`HANDOVER.md`——**master的`HANDOVER.md`內容跟本分支已經不同步**（09-23整合時master已經有自己那次整合的記錄段落），**不要整份cherry-pick`HANDOVER.md`的變動**，只需確認master的`HANDOVER.md`已經記載了報告68/69完成這件事即可（若沒有，用一段精簡摘要手動補上，不要整份取代）。
6. `docs/報告/68_....md`、`docs/報告/69_....md`、`docs/參考文獻/38_.../`、`docs/參考文獻/39_.../`都是**本分支獨有的新路徑**，master上不存在同名檔案，理論上cherry-pick或直接複製都不會衝突——但仍要用`git status`確認，不要假設。

---

## 2. 任務清單

### T1（S，先決）：main checkout環境確認

- 進入main checkout `D:\Users\666\Desktop\world knowledge graph rag`，執行`git status -sb`確認乾淨（若有使用者既有未commit異動，**比照09-23的做法**：先`git stash push -u -m "<具名tag>"`保護，事後`stash apply`+確認後`drop`，不要用裸`git stash`／`git stash pop`，因為stash stack是跨worktree共用的）。
- `git fetch origin`確認本地`master`與`origin/master`一致（都在`7e67f2b`）。

### T2（S）：建立中繼分支並cherry-pick

- 從`origin/master`建立中繼分支（例如`codex/integrate-report68-69`）。
- 依§1.4的順序cherry-pick：`92d8492` → `1f353d8` → `313b3d5`（`.gitignore`，若跟09-23那次整合的`.gitignore`修正重複，`git cherry-pick`會自動偵測空diff並可`--skip`，不算異常）。
- `b8f0178`（HANDOVER.md）**不要直接cherry-pick**——改成手動在master當前的`HANDOVER.md`找一個合適位置，用2-3句話精簡記錄「報告68/69的production程式碼（embedding快取、metric-judge解耦）已整合進master」，比照09-23整合時「用`git rm`只排除文件、保留code」的精神：**只取產品程式碼異動的事實，不整份搬運本worktree的敘事文件**。
- `docs/報告/68_....md`、`docs/報告/69_....md`、`docs/參考文獻/38_.../README.md`、`docs/參考文獻/39_.../README.md`：直接從本分支複製檔案內容進中繼分支（不透過cherry-pick，避免拉進不相關的commit歷史），作為獨立一筆`docs:`commit。

### T3（S）：衝突與回歸檢查

- 若`core/providers/factory.py`在cherry-pick時出現衝突，**先讀懂衝突兩側**——master自09-23整合後是否有人再改過這個檔案（`git log origin/master -- core/providers/factory.py`）。若有獨立演進，**手動合併保留雙方**，不要整份取代任一邊（比照09-23踩到`svo_service.py`衝突時的教訓）。
- Cherry-pick完成後，跑`python -m pytest tests -q -p no:cacheprovider --ignore=tests/core/test_embedding_migration.py`，**必須全綠**才能繼續。若有失敗，比照09-23的原則：不是「整份取代」去讓測試通過，而是理解失敗原因、精確合併。

### T4（S）：fast-forward進master並push

- 確認中繼分支的`git log`只包含預期的commit（無意外夾帶其他未審查異動）。
- Fast-forward `master`到中繼分支（`git merge --ff-only`），**不要**用`git reset --hard`或force push。
- **push前必須先取得使用者明確同意**（比照HANDOVER.md「工作目錄與分支」章節的既有規定：「推送須取得使用者明確同意」）。若這是在無人值守的背景工作中執行，先完成到「本地master已fast-forward、push待執行」這一步，在報告中明確標注「已本地整合，push待使用者核准」，不要自動push。

---

## 3. 明確不做的事

- **不整份搬運本worktree的`HANDOVER.md`到master**——只取精簡的完成記錄。
- **不force push、不reset --hard、不merge --no-ff製造多餘的merge commit**——維持fast-forward。
- **不觸碰`reextract-v2`／`worktree-report-gemini-review`這兩個worktree**——09-23已確認它們維持不動，本任務書不改變這個結論。
- **不在本worktree（`sdd-retrieval-comparison`）執行任何合併操作**——所有整合動作在main checkout進行。
- **push前不得省略使用者確認**——即使T1-T4的程式碼異動風險評估為低，push仍是對共用遠端的異動，需要明確同意。

---

## 4. 驗收標準

1. main checkout的`master`本地已fast-forward，包含`92d8492`／`1f353d8`／`313b3d5`的內容（新SHA，因cherry-pick會重新產生commit hash）與新增的4個docs檔案，`HANDOVER.md`有精簡記錄但未整份搬運本worktree版本。
2. `python -m pytest tests -q -p no:cacheprovider --ignore=tests/core/test_embedding_migration.py`在main checkout的master上全綠，測試數量應反映`tests/core/test_providers_factory.py`／`tests/scripts/test_frozen_baseline_stage.py`／`tests/scripts/test_rq1_harness_failures.py`／`tests/scripts/test_rq1_metric_judge.py`的新增測試（對照`1f353d8`原始commit message「1086 passed」為基準，master原有測試數量另計）。
3. `git diff`確認`routers/agent.py`與`services/`目錄完全未被本次整合觸碰。
4. 報告本次整合結果（是否push、遇到什麼衝突、怎麼解的）用一段文字附在commit message或回報中，**如實記錄**，不要省略遇到的問題。

---

## 5. 給 Codex 的指令（可直接貼上）

> 請執行 `docs/報告/70_報告68_69批次整合進master任務書.md` 的T1-T4。背景：2026-09-23已完成一次把本分支9筆commit分4組cherry-pick進main checkout master的整合（`origin/master`現為`7e67f2b`），但當時**明確排除**了本分支同一天新增的報告68（embedding快取）／報告69（metric-judge解耦）內容，這次任務就是把那批內容補進去。**核心待整合commit**（本分支上）：`92d8492`（報告68 production code）、`1f353d8`（報告69 production code，兩者都只改`core/providers/factory.py`新增函式＋`scripts/eval/`兩個harness腳本的選填CLI參數，**完全不碰`routers/agent.py`或`services/`**，預設零行為變化）、`313b3d5`（`.gitignore`）。**操作地點是main checkout**（`D:\Users\666\Desktop\world knowledge graph rag`，目前在`master`），**不是**本worktree。步驟：(1) main checkout先`git status`確認乾淨（若有使用者既有異動用具名`git stash push -u -m`保護，不要用裸`git stash`）；(2) 從`origin/master`建中繼分支，依序cherry-pick`92d8492`→`1f353d8`→`313b3d5`（順序不可換，兩者都改`core/providers/factory.py`）；(3) `docs/報告/68_....md`、`docs/報告/69_....md`、`docs/參考文獻/38_.../`、`docs/參考文獻/39_.../`直接從本分支複製檔案內容進中繼分支（不透過cherry-pick，避免拉進不相關commit歷史），另立一筆`docs:`commit；(4) `HANDOVER.md`**不要**cherry-pick`b8f0178`整份取代，改成在master現有`HANDOVER.md`找位置手動補一段2-3句話的精簡記錄；(5) 若`core/providers/factory.py`衝突，先查`git log origin/master -- core/providers/factory.py`確認master是否有獨立演進，手動合併保留雙方，不要整份取代任一邊；(6) 跑`python -m pytest tests -q -p no:cacheprovider --ignore=tests/core/test_embedding_migration.py`確認全綠；(7) 確認中繼分支沒有夾帶其他未預期commit後，`git merge --ff-only`到本地`master`。**push前必須明確詢問使用者同意，不要自動push、不要force push、不要`reset --hard`**。完成後回報：實際cherry-pick出的commit SHA對照表、是否有衝突及如何解的、pytest結果、`routers/agent.py`與`services/`是否確認未被觸碰、以及push是否已取得同意或仍待核准。
