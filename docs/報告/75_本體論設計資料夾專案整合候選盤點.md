# 75. 本體論設計資料夾專案整合候選盤點

> **日期**：2026-09-24
> **性質**：盤點清單（Living Inventory），**不是決策文件**。目的只是把 `D:\Users\666\Desktop\本體論設計` 資料夾的內容，按「跟這個 KG 專案的接入可能性」分類存放，具體要不要採用、何時導入、依據哪個文獻版本，全部留待之後逐項決定。
> **範圍限定**：使用者已明確表示目前只專注處理**單一 KG**（勞基法合規 KG）的環節，本文件的分類以此為準——凡預設多 KG 聯邦、企業級規模治理者，一律歸入「不建議近期混入」。
> **前置閱讀**：本文件整理自對話中已讀過的 `本體論設計` 資料夾文件與程式碼對照結果，未涵蓋資料夾內尚未詳讀的全部檔案（見第五節）。

---

## 一、已對照程式碼驗證，可作為近期候選的項目

以下項目**已實際核對 `services/svo_service.py`、`models/knowledge_graph.py`、`core/constants.py` 等程式碼**，確認對應缺口目前確實存在，不是文件過時的空談。

| 候選 | 來源文件 | 對應現況 | 備註 |
|---|---|---|---|
| **Relator 關係載體節點化** | 《本體抽取與系統落地工程缺口紀錄表》GAP-06；《Agent驅動本體工程設計指南》步驟 6.1 具體化黃金法則 | `services/svo_service.py::merge_triples_to_graph` 目前仍是簡單 `(:Entity)-[:RELATION]->(:Entity)`，無多方關係載體 | 缺口紀錄表明確標為「**AGGR18 根因**」——對應 [[project_report57_context_quality_decoupling_and_kg_role]] 已知的「新舊法認定機構寫反、Atomic Accuracy 仍 100%」弱點。判斷準則：凡關係具備「起訖時間、審核狀態、多方牽涉」者才具體化，不是全面重構。 |
| **法律模態關係型** | GAP-03 | `core/constants.py::SVO_REL_TYPES` 目前只有 ConceptNet 5.5 的 35 類通用關係，無 `OBLIGATES`／`PERMITS`／`PROHIBITS`／`DEEMS` | 概念源自 LKIF-Core，但落地只是擴充常數清單 + prompt，改動面小。 |
| **條件（Condition）與法效果綁定** | GAP-04；《gemini 整理報告》的「適用條件」上下文物件 | 標準 SVO 抽不出「年資滿5年未滿10年→特休15日」這種條件綁定 | **雙重獨立佐證**：兩份完全不同來源、不同風格的文件各自獨立指向同一缺口，優先度可比單一來源的建議高。 |
| **Fact 節點雙時態** | GAP-07；《佐證文獻與實作專案盤點》Graphiti(Zep) | `models/knowledge_graph.py` 目前無 `valid_start`／`valid_end`／`is_active` 欄位 | Graphiti 是現成開源實作（valid_at/invalid_at + created_at，過期事實失效而非覆寫），可當設計參考，**非採用依賴**。改動面較大，建議排在法律模態穩定之後。 |
| **Per-KG 設定分層斷點 1~4** | 《知識圖譜分層客製化架構現況與接續任務報告書》 | 查詢端／生成端已 100% 接線；抽取端 few-shot 參數化、切塊配置讀取、範圍審查規則獨立成檔仍未接線 | 內容與既有 [[project_per_kg_config_architecture]] memory 完全吻合，可直接當接續施工清單，不需重新分析。 |

---

## 二、概念可能有價值，但需先驗證文獻／評估成本，列入中長期方向

這些項目**不建議現在排進 sprint**，但值得留存，等相關 CQ（能力問題）真的出現時再評估。

| 候選 | 來源 | 為何先擱置 |
|---|---|---|
| 三問決策門（關係要不要向量化：能否用距離解釋／是否參與推理守恆／有無 CQ 需要） | 《RAG與知識圖譜結合技術報告 v2.0》§2 | 成本幾乎為零，可隨時寫進設計原則，但目前專案沒有任何關係向量化的實際需求，先記錄不急著落地。 |
| OntoClean 四問分類法（Substance／Role／Relator／Event） | 《Agent驅動本體工程設計指南》步驟 5 | 可作為未來判斷「新抽取的節點要不要具體化」的思考輔助，但直接對應到 GAP-05／GAP-06 才有意義，不必單獨立項。 |
| Schema.org 雙軌別名機制（標準名 + 內部別名對照表） | 同上步驟 4 | 若未來要做 GAP-05（實體型別對齊 schema.org）才用得上，現在專案的 `entity_type` 尚無此需求。 |
| Router 逐查詢選檔（L0–L4） | 《RAG與知識圖譜結合技術報告 v2.0》§3 | 報告完全沒評估分類器誤判率與維護成本；本專案近期報告（62/69）顯示評測 harness 本身的判準穩定性都還在處理中，疊加查詢複雜度分類器風險過高。 |
| 混合檢索向量粗篩＋符號精驗 | 同上 §11 | 前提是「圖遍歷變慢」，但目前所有已知瓶頸都在抽取品質與角色歸屬，沒有證據顯示 BFS 速度是問題。 |
| 島規模治理（CQ 閉包、聯邦分片） | 同上 §14 | 前提是「多 KG 之間要跨島查詢」，目前每個領域各自一個 KG，還沒有這個實測出來的需求。 |

---

## 三、不建議近期混入本專案範圍

以下內容**主題相近（多半也是台灣勞基法/人資領域）但屬於不同產品線或不同架構典範**，混入會讓範圍失焦，違反使用者目前「先專注單一 KG」的決定。

| 內容 | 實際定位 | 為何不建議 |
|---|---|---|
| `Round1_Core_Entity_Catalogue` ~ `Round4_Taiwan_Legal_Rule_Ontology` + `SeanOntology/` 兩個案例資料夾 | 「台灣中小企業數位人資長」——一個**確定性合規規則引擎**產品構想（`RuleVersion`／`ApplicabilityAssessment`／`ComplianceTask`／`automationMode: AUTO_EXECUTE`） | 跟本專案「診斷 KG 何時能幫助 RAG 回答問題」的機率性檢索系統，架構本質完全不同；搬入等於重做一個規則引擎。**唯一可能殘餘價值**：Round4 §二的 SRC-001~024 官方法規來源登記表，若本專案沒有集中管理法規來源版本，可單獨抽出當 metadata 參考。 |
| `Agent本體工程_v2/` 全部（含《熱水AiEMS》案例） | 跨領域通用方法論 + IoT 監測控制系統案例研究 | 案例領域（熱水系統監測）與本專案（法律文件 RAG）無關，方法論本身是給「各領域導入」用的抽象框架，套用前還需要再做一次本專案客製化，效益不明。 |
| 《三層工程方法論》的 Freeze Gate／SHACL 形狀約束／人機紅藍對抗 Agent 三角色 | 企業級 DDD + PostgreSQL 治理思維 | 跟本專案「小步快跑、跑測驗證」的既有工作模式落差大，個別概念（Relator、雙時態、防塌縮四鐵律）已拆分進第一、二節，整套流程不建議照搬。 |
| 《gemini 整理報告》的 Staging Area（PostgreSQL/Redis）、Event Node、8 週企業提案格式 | 泛用 AI 顧問提案範本 | 已於前次評估確認：無程式碼依據、無文獻佐證、多項聲稱（85% 成本減少等）無方法論支撐；「Evidence Quote」「非同步抽取」等主張本專案已有不同形式的實作。唯一價值（Condition 綁定）已收錄進第一節 GAP-04。 |

---

## 四、可能的佐證文獻／實作專案（未逐篇查證）

《佐證文獻與實作專案盤點 v1.0》列出的部分項目與第一、二節候選直接相關，抄錄於此供未來查證用，**目前皆未 live 驗證，正式引用前必須依專案慣例現查**（[[feedback_literature_and_project_backing_requirement]]）：

- **Graphiti (Zep)**——雙時態（valid_at/invalid_at + created_at）開源實作，對應 GAP-07。
- **Neo4j Document Intelligence**——chunk→實體去重 merge、實體連回被提及 chunk，對應本專案既有的 `source_doc_id`/`source_article_no` 證據掛載（已實作，僅供對照，非待辦）。
- **KGGen**（embedding 聚類 + LLM 去重兩階段）——對應本專案既有的跨文件實體別名消解（`docs/參考文獻/10_跨文件實體別名消解與增量聚類`），僅供對照。
- **LazyGraphRAG**（微軟）——索引成本主張，若未來要評估「分階段建圖成本」時可查。

---

## 五、尚待後續詳讀，本輪未深入評估

以下檔案本輪僅瀏覽標題/目錄或完全未讀，分類屬於推測，如需要可再指定詳讀：

- `Agent本體工程_v2/01_本體論的形式基礎.md`、`通用流程/00_流程總覽.md`、`通用流程/團隊導入與分享.md`、`實測_熱水AiEMS/01_文件分級.md`——預期延續第三節分類（通用方法論/異領域案例）。
- `AI_Only_Ontology_Engineering_Execution_Plan_v1.0.txt`
- `流程文獻對照工作底稿_v0.1.md`
- `本體論seannotebooklm.md`
- `專案理解一頁式報告.html`
- `知識圖譜分層客製化架構現況與接續任務報告書_v1.0 copy.md`（與已讀正本同內容的複本）
- `RAG與知識圖譜結合技術說明_對話整理_v1.0.md`（v2.0 的前版，已被取代）
- `SeanOntology/` 兩個案例的 `raw_transcript/`、`對話分析報告.md`
- `微光Core 表.pdf`、`關於人工智慧這件小事.pdf`

---

## 六、下一步（建議，非決定）

本文件只完成「盤點與分類」，尚未決定：

1. 第一節哪一項要先排進本專案的目標與範圍（建議候選順序：GAP-06 → GAP-03 → GAP-04 → per-KG 設定斷點 1~4 → GAP-07，理由見各項備註）。
2. 第二節項目要不要正式寫入設計原則文件，或繼續留在候選狀態。
3. 第四節文獻若要正式引用進論文，何時安排逐篇 live 查證。

待使用者決定要推進哪一項後，再依專案既有慣例（先討論、逐項核准、再動 `services/`／`models/` 程式碼）個別展開。
