# 32．T1 階段測試結果與列舉分段 DEDUP 診斷報告

**日期**：2026-09-07
**背景**：報告30 §9 判定 c15949bf 全量重抽的 14 份題目文件已完整重抽（主 drain 27%、
`completed=896/3303`），T1 階段測試就緒。本報告記錄 T1 結果，以及據此對「列舉分段
DEDUP 過度合併」（報告30 §9.3 的 E1）的深入診斷與收斂。**本報告不含程式碼變更**。

- T1 執行：2026-09-07 12:30:02 → 15:42:50（3h13m），72 次真實 `routers.agent.chat()`，**0 例外**。
- 方法：暫停主 drain（避免搶 Ollama 汙染延遲量測），跑 `run_t1.py`（合併 `run_report26_comparison.py`
  ＋ `rerun_report25_conditionA.py` 題組，各題 ×3）。原始輸出 `t1_output.txt`（955 行）。
- 條件：報告25 只跑 A（新 KG c15949bf）；報告26 跑 A＋D（純 LLM）。B（凍結舊 KG）沿用報告26 數據。

---

## 1. 報告25 條件A 8 題回歸（×3）

| 題 | 文件 | 結果（3 次） | 判定 vs 報告25 §7 |
|---|---|---|---|
| Q1 | N0030011 夜間安全設施 | 不得上鎖／不得堆置 **3/3** | ✅ 無回歸 |
| Q2 | N0030018 育嬰留停 | 十日前 **3/3**；「每次≥六個月」2/3 答錯成「三十日以上」；「二次為限」3/3 未乾淨答出 | **◐ 部分回歸**（發現3 的十日前守住，§2 其餘子規則退化） |
| Q3 | N0030025 未滿15歲工時 | 三段年齡只有 **1/3** 完整（run2）；2/3 漏「未滿6歲→二小時」；「未滿6個月→三十分鐘」3/3 對 | **◐**（見 §5：資料層 3 節點正確，屬生成/檢索端） |
| Q4 | N0060007 高溫作業 | 不得減少 ＋ 五十度 **3/3** | ✅ 無回歸 |
| Q5 | N0060012 精密作業 | 2/3 三部分全對；1/3 漏「15 分休息」 | ◐ 輕微（非決定性；run2 推理來源可疑） |
| Q6 | N0060065 母性健康保護 | 保護期間＋3 年保存 **3/3**；「血中鉛 10μg/dl 以上→第三級」**3/3 全未答出** | **◐ 回歸**（見 §5：第三級門檻主詞遭誤併，事實消失） |
| Q7 | N0070020 檢查費 | 八千／四千／八百／50% **3/3** | ✅ **無回歸——`每一型式` 未誤併，報告30 §9.4 下調成立** |
| Q8 | N0080016 原住民訓練補助 | 三百小時／一百元／三十日 **3/3** | ✅ 無回歸 |

**淨：4/8 乾淨守住（Q1・Q4・Q7・Q8）；4/8 退化（Q2・Q3・Q5・Q6），退化全集中在「列舉分段／多子規則」事實。**

## 2. 報告26 8 題（A＋D，×3）

| 題 | 文件 | A 結果（3 次） | vs 報告26 §2 |
|---|---|---|---|
| Q1 | N0060029 三段休息 | 20m→35 分 **3/3 ✅**；2–5m／5–20m 兩段混淆 3/3（run3 甚至把 5–20m 答成二十分鐘） | ◐；**列舉分段 DEDUP，見 §5** |
| Q2 | N0060016 重體力 | 四十／四點五公斤 **3/3 ✅** | 持平 ✅ |
| Q3 | N0080013 大量解僱 | 3/3 都答出「發給三個月為限」＋三十日報備/申請 | **↑ 改善**（報告26 為 ◐ 糊成一團） |
| Q4 | N0080013 數值密集 | 三十二小時／五千元／「台」保真 **3/3 ✅** | 持平 ✅（發現4 白名單有效） |
| Q5 | N0050030 災區保險費 | 六個月 **3/3 ✅**；起算日「當月一日」**3/3 未答出** | 持平 ◐（生成端沒用圖裡的事實，報告26 §2.2 已知） |
| Q6 | N0050011 繼續加保 | 二年內 **3/3 ✅**（BFS 三元組=22，低連通度節點可及） | 持平 ✅ |
| Q7 | N0060007 誠實拒答 | 「資料未明確記載」**3/3 ✅✅** | 持平 ✅✅ |
| Q8 | N0060004 變量係數 | run1 答錯 1.25；run2 拒答；run3 推對「係數 2」又自我推翻拒答 | **✗**——資料已修（run3 事實清單有「1 以上未滿 10→係數 2」），生成端多跳＋接地過度保守擋住 |

## 3. 純 LLM 對照（條件 D）

報告26 8 題 × 3 次 = 24 次，**0 次乾淨答對**（Q7 兩次含糊講「未明確」但答非所問）。純 LLM 一律
產生篤定錯誤數字（Q2「25 公斤」、Q8「1.25」「2–4 倍」、Q6「30 天」）或整段拒答。**價值主張
強力成立。**

---

## 4. 三個跨題觀察

1. **限制性重生成過度修正普遍**：~75% 的 A 呼叫 `regenerated=True`，且常把對草稿改壞
   （Q2 part1「六個月」→「三十日以上」；Q8 run3 推對又推翻）。報告25 §4 標「2b ROI 遞減暫不做」——
   **T1 推翻此判斷，現在是生成端最大槓桿點**。
2. **分段清單答不全**：Q3／Q6 只有部分 tier 逐字命中就整段退掉——grounding 把「一個 tier 清單」
   逐條當 claim 罰。
3. **多跳區間推理仍缺**：Q8「5 落在『1 以上未滿 10』→ 係數 2」，事實在清單、生成端不會推，
   接地核對把推論當「未明確記載」。

---

## 5. E1 診斷：列舉分段 DEDUP 過度合併

報告30 §9.3 把 Q1 歸為「`_MEASURE_PATTERN` 缺長度單位」。深入診斷（唯讀 Cypher／讀
`services/svo_service.py`）後**收斂為：部署時序 ＋ 殘留 alias ＋ 2–3 個明確樣式缺口，
不是「現行抽取碼壞掉」，也不是開放式的「區別性語素」研究**。

### 5.1 時序：題目文件抽取早於發現29

`task_queue.updated_at` 顯示 14 份題目文件的現行抽取全部落在 **2026-09-04**（B 的 14 份
targeted 重抽）。`_RANGE_COMPARATOR_PATTERN`（發現29，commit `df2bc62`）**2026-09-06 才
ff-merge 進跑 drain 的碼**。→ 題目文件是用「有發現C、無發現29」的碼抽的。主 drain 後續
掃到 896，但這些文件早已 `completed`、橫掃跳過，未再經現行碼。

### 5.2 兩層問題

**A 層——真正的節點誤併（09-04 抽取、發現29 守衛未上）**

| 題 | 情況 | 現行碼會否擋 |
|---|---|---|
| **Q1** | `高度在二公尺以上未滿五公尺者` **無獨立節點**，僅為 `高度在五公尺以上未滿二十公尺者` 的 alias；2–5m→二十分鐘 的事實孤兒接到 5–20m 節點 | **會**（`_RANGE_COMPARATOR_PATTERN` 命中「未滿五」）→ **重抽即修，不需改碼** |
| **Q6** | `血中鉛濃度在十 μg/dl 以上者` 僅為 `血中鉛濃度在五 μg/dl 以上未達十 μg/dl`（第一級）的 alias→「≥10→第三級」事實整條消失；`第一級管理` 亦帶 alias `第二級管理`／`第三級管理` | **不會**（見 §5.3 F2／F3a）→ **要改碼** |

**B 層——殘留 alias 汙染（`revoke_chunk_facts()` 不清 Entity 節點）**

全庫掃描：`aliases` 長度>1 的 Entity 共 1134 個，其中「去數字後同形、數值不同」（分段合併
特徵）**67 組**。**多數 name／alias 的守衛判定為 `True`**（元、類、種、％、日、歲…）→ 現行
`resolve_entity_name` 守衛會擋 → 這些是**發現C／發現29 之前的原始 c15949bf 抽取留下的殘留**，
重抽不會重建，但 `revoke_chunk_facts()` 也不會清。

- **Q3**：`年齡未滿六歲者`／`年齡六歲以上未滿十二歲者`／`年齡十二歲以上未滿十五歲者`
  **三個節點都獨立存在、邊都正確**（`OBSTRUCTED_BY→二／三／四小時`）；只是舊 alias 還掛在節點上。
  → T1 Q3 的 2/3 漏 tier 是**生成／檢索端**（§4 觀察 2），非 DEDUP。Q7 同理（答案 3/3 正確）。

### 5.3 現行碼仍存在的樣式缺口（要改碼）

| # | 缺口 | 例 |
|---|---|---|
| **F2** | `_RANGE_COMPARATOR_PATTERN` 的 `[數字]\s*(?:以上\|以下\|以內)` 要求數字緊貼比較詞，中間夾單位即失效 | `十 μg/dl 以上`、`二十公尺 以上` |
| **F3a** | `_MEASURE_PATTERN` 單位表缺 `級`，以及量測單位（`μg`／`dl`／`公尺`／`公分`／`℃`／`ppm`…） | `第三級管理`、`30.6℃`／`32.6℃`（WBGT 溫度，N0060007）、`高度在二公尺…` |
| **F3b** | 序數列舉完全無守衛（`_MEASURE_PATTERN`／`_RANGE_COMPARATOR_PATTERN` 皆 `False`） | `精密作業之一／之三`、`第一類外國人／第二類`、`附表一／附表二`、`二分之一／五分之一`、`1.25／1.5` |

### 5.4 影響範圍

67 組中真正造成事實遺失的是 A 層型（範圍比較詞夾單位、`級`、序數）。題目文件受影響：
**N0060029（Q1）、N0060065（Q6）、N0060004（Q8 部分）**；非題目文件另有「外國人分類」
「事業分類」「喪假日數」「WBGT 溫度」「貸款額度」「容許濃度級距」等（分佈廣）。`svo_service.py`
的 `resolve_entity_name` 守衛本身運作正常（Q7 答案正確、Q3 節點獨立可證）。

---

## 6. 生成端待辦（G，不受抽取端凍結限制，可即刻設計／改碼）

| # | 項目 | T1 證據 | 方向 |
|---|---|---|---|
| **G1** | 限制性重生成過度修正（2b） | ~75% regen；常改壞對草稿 | 重生成要看得到原草稿、或只重生成那一句未接地主張。**優先** |
| **G2** | 多跳區間推理 | Q8：事實在清單、不會推、接地當「未明確」罰 | 報告28 分解機制擴充；或 prompt 加區間比對提示 |
| **G3** | 分段清單答不全 | Q3／Q6：部分 tier 逐字命中即整段退 | grounding 把一個 tier 清單當一個 claim |
| **G4** | 事實在圖裡、生成端不採用 | Q5 起算日 3/3 未答（報告26 §2.2） | 同 G1 家族 |

## 7. 更新後的解凍批次與執行順序

### 解凍批次（DRAIN-DONE `pending=0` 之後、報告27 §6.2 前）

原報告30 §9.5「`_MEASURE_PATTERN` 補長度單位」擴大為：

1. **F2**：`_RANGE_COMPARATOR_PATTERN` 允許數字與「以上／以下／以內」間夾一個單位 token。
2. **F3a**：`_MEASURE_PATTERN` 補 `級` ＋ 量測單位。
3. **F3b**：序數列舉守衛（`第[數字]+[級類種款項條]`、`[數字]+分之[數字]+`、小數、`附表[數字]`），
   或「兩候選名僅差一個數字/序數 token」的結構性判斷。
4. F2／F3 一起加測試、跑全套 pytest。
5. **一次性 `aliases` 清理 backfill**：移除「alias 為 name 的數字變體」的跨 tier alias（§5.2 掃描邏輯精修）。
6. **targeted 重抽**：N0060029、N0060065、N0060004 ＋ alias 掃描標記的文件（有界，估 ~10–20 份）。
7. §4.1 短語素守衛（`每一型式` vs `每增加一種型式`）——維持在批次內、低優先（報告30 §9.4）。
8. L2 向量引導 prize 剪枝（報告27）。
9. **報告27 §6.2 全量 8 題驗證（×3）＋ 報告25 條件A 8 題回歸**——此時 Q1／Q6 應轉 ✅。
10. 論文正文同步（報告30 §7 步驟 8）。

### 生成端（平行、不卡凍結）

G1 即刻可設計＋改碼（純查詢端）；驗證需 Ollama，併到之後某次短暫停或 §6.2 窗口。**G1 優先**。

### E3（抽取端，歸解凍批次）

Q2 的「每次以六個月為單位／二次為限」退化：N0030018 §2 chunk 2 把多個子規則抽成一個
擠在一起的 entity 名（`三十日以上未達六個月：以二次為限,未滿三十日：每次申請…`）。列入解凍
批次的 targeted 重抽 ＋ 視情況調 §2 的 SVO 切塊。

---

## 8. 相關文件

- `docs/報告/25_擴大版新舊KG問答品質比對報告.md` §7（8 題條件A、發現3/C 收尾）
- `docs/報告/26_修正後瓶頸探測問答比對報告.md` §1（題組）、§2（逐題）、§4（瓶頸排序）
- `docs/報告/27_圖遍歷向量引導剪枝機制設計報告.md` §6.1／§6.2（驗證）
- `docs/報告/29_實體模糊合併範圍修飾詞與數字比較詞守衛設計報告.md` §2.4／§4.1／§4.3
- `docs/報告/30_c15949bf全量重抽最終驗收計畫.md` §9（T0 smoke、T1 就緒、§9.3–§9.5）
- 原始輸出：`t1_output.txt`（T1 全 72 次呼叫）、`diag_e1.py`（DEDUP 診斷腳本）
