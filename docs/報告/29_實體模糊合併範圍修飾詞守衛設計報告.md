# 29．實體模糊合併範圍修飾詞守衛設計報告

**日期**：2026-09-05
**狀態**：設計提案，**尚未實作**，待確認
**對應**：`docs/報告/25_擴大版新舊KG問答品質比對報告.md` § 4 發現3收尾（⚠️ 仍未涵蓋段落）；
`docs/論文/03_系統設計與方法論.md` § 3.4 `DEDUP4`／`GUARD4`；
`docs/論文/02_文獻探討.md` § 2.4.5（RQ4b）

---

## 1. 背景與觸發

報告25 §4 發現3（收尾）修完量詞實體模糊合併問題後，明確記錄了一個**尚未涵蓋**的
殘留案例：

> ⚠️ **仍未涵蓋**：不含任何量詞的短法律語素（`每一型式` vs `每增加一種型式`，
> `_edit_ratio`＝0.727）——本輪靠「刪孤兒節點＋乾淨重抽」繞過；治本要「差異字是否
> 為區別性語素」判斷或此語料的 DEDUP 門檻校準（Fellegi & Sunter 方法論、需驗證集），
> 另立「實體混合＋embedding 過度合併」調查。

本報告是這個「另立調查」的正式產出——確定影響位置、查證文獻、給出設計提案。

## 2. 前置查證（2026-09-04/05）

### 2.1 現行 KG 實測：這次重抽沒踩到，但守衛邏輯確實有漏洞

直接查詢 c15949bf（已完全重抽的 N0070020）：「每一型式」只有一條邊
（`→ 新臺幣八千元`），「每增加一種型式」完全不存在於圖中——「四千元」那筆事實
這次被抽成完全不同、逐字抄錄原文的長 subject（「同時申請多種型式且皆屬相同
危險性機械或設備種類者」），跟「每一型式」編輯距離差很多，這次沒有觸發問題。
**推測是 `qwen2.5:7b` 非決定性（報告20 已確立）讓這次的措辭恰好避開碰撞**，
不是問題已解決。

### 2.2 根因：`resolve_entity_name()` 的守衛是不對稱的

`_MEASURE_PATTERN` 守衛（報告25 發現3/發現C）只檢查**正在解析的新名稱 `name`
本身**，不檢查候選清單裡的既有實體：

- 「每增加一種型式」含「一種」（數字＋單位），若它是新進來的 `name`，會命中
  `_MEASURE_PATTERN` 而提早返回，不會被合併——安全。
- 「每一型式」不含任何「數字＋清單內單位」組合（「一」後面接「型式」，不在
  `_MEASURE_PATTERN` 清單內），**完全不受保護**——若它是新進來的 `name`、
  「每增加一種型式」已在候選清單裡，`_edit_ratio`＝0.727 ≥
  `ENTITY_DEDUP_EDIT_RATIO_THRESHOLD`（0.70）會直接合併。

**是否觸發純屬「誰先進圖」的運氣**，不是穩定行為。

### 2.3 這是新的一類問題，不是量詞問題

「每一」vs「每增加一」不是「同單位不同數值」（`_QUANTITY_PATTERN`／`_MEASURE_PATTERN`
處理的量詞類），是**基礎量 vs 遞增量**——「增加」這個**範圍修飾詞**改變語意，
表面極相似、語意相反。`_MEASURE_PATTERN` 的「數字＋單位」樣式規則結構上抓不到
這類不含數字的短語，需要新的偵測邏輯，不是擴充既有樣式就能涵蓋。

## 3. 文獻對照

完整內容見 `docs/參考文獻/24_短語模糊合併與否定範圍偵測/README.md`，本節摘要：

| 文獻 | 角色 |
|---|---|
| **Mrkšić et al.（2016，ACL）**《Counter-fitting Word Vectors to Linguistic Constraints》，arXiv:1603.00892 | 確立「表面相似掩蓋語意相反」是 distributional semantics 已知現象（反義詞因常見於相同句法框架而被判高相似度）。解法依賴語言學約束詞典，本專案無對應中文法規量詞反義詞典，不直接複用。官方碼 [nmrksic/counter-fitting](https://github.com/nmrksic/counter-fitting)。 |
| **Tosun, Buldur, Ezerceli & ElHussieni（2026）**《Beyond Cosine Similarity: Taming Semantic Drift and Antonym Intrusion in a 15-Million Node Turkish Synonym Graph》，arXiv:2601.13251 | 幾乎逐字對應的知識圖譜情境——大規模圖譜同義詞聚類同樣因 cosine 誤把反義詞聚類。解法（語義關係分類器 90% F1 + 拓撲剪枝）證實問題真實、現行，但規模遠超本專案需求，僅佐證問題存在。 |
| **Chapman, Bridewell, Hanbury, Cooper & Buchanan（2001）**《A Simple Algorithm for Identifying Negated Findings and Diseases in Discharge Summaries》（**NegEx**），*J Biomed Inform* 34 | 支持「規則式觸發詞清單」路線的關鍵先例——臨床否定偵測用簡單觸發詞+regex，精確率 68.4%→84.5%。⚠️ 付費牆，未下載全文，僅摘要層級。 |
| Splink／dedupe.io（本專案 `13_` 既有實體去重專案） | 查證後確認**無**針對「否定詞/範圍修飾詞短語誤判」的設計——兩者皆為真實世界紀錄（姓名/地址）的機率式比對，此問題屬 NLP 語意層級，非傳統 record linkage 範疇，查無現成專案可直接借鏡。 |

**誠實聲明**：三篇文獻是問題定位與方法論路線（規則式 vs 訓練分類器）的佐證，
本設計的守衛規則本身查無直接文獻先例，是本論文自行設計的工程決策。

## 4. 設計

### 4.1 核心機制：範圍修飾詞雙向過濾

新增 `_SCOPE_MODIFIER_PATTERN`，比照 `_QUANTITY_PATTERN`／`_MEASURE_PATTERN`
既有架構（NegEx 式受控觸發詞表 + regex），但**不做早退**（與 `_MEASURE_PATTERN`
不同）——而是在比對迴圈**前**，把候選清單依「是否含範圍修飾詞」與 `name` 分成
同類/異類，只保留同類候選進入編輯距離／cosine 比對：

```python
_SCOPE_MODIFIER_PATTERN = re.compile(r"增加|額外|追加|新增")

def resolve_entity_name(name, candidates, ...):
    if _MEASURE_PATTERN.search(name):
        return name

    name_has_modifier = bool(_SCOPE_MODIFIER_PATTERN.search(name))
    candidates = [
        c for c in candidates
        if c["name"] == name
        or bool(_SCOPE_MODIFIER_PATTERN.search(c["name"])) == name_has_modifier
    ]
    # ↓ 以下編輯距離／cosine 兩個迴圈改用過濾後的 candidates，其餘邏輯不變
    ...
```

**為什麼不是早退，而是雙向過濾**：`_MEASURE_PATTERN` 命中就整個跳過模糊比對，
適合「這個名稱本身就是精確量詞、不該跟任何東西模糊合併」的情境。範圍修飾詞
不同——「每增加一種型式」若跟另一個同樣含「增加」的候選（例如未來語料裡
「每再增加一種型式」）比對，仍然**應該**走正常的模糊合併邏輯，只是不該跟
「不含範圍修飾詞」的候選（「每一型式」）合併。過濾候選清單（而非早退）能
保留「同類可模糊合併、異類不可比對」這個更精確的語意。

### 4.2 觸發詞清單（v1，保守）

`增加|額外|追加|新增`——只有「增加」有本次真實案例（`每增加一種型式`）確認，
其餘三個是同義的「附加規定」常見措辭（法規領域常見寫法），依domain知識預防性
納入，比照 `_MEASURE_PATTERN` 當初從 `_QUANTITY_PATTERN` 擴充時「先涵蓋確認
案例、再依域知識預留相近變體」的既有慣例。**刻意不做**更寬泛的清單（如「另」
「再」「又」單字），這些字在法規文本中極常見於非範圍修飾語境（「另定之」「再者」
「又稱」），誤判風險高於效益，留待真的遇到新案例再擴充。

### 4.3 影響範圍

| 位置 | 動作 |
|---|---|
| `services/svo_service.py::resolve_entity_name()` | 新增 `_SCOPE_MODIFIER_PATTERN` 與候選過濾邏輯 |
| `tests/services/test_svo_service.py` | 新測試：「每一型式」／「每增加一種型式」不合併（雙向）；同為修飾詞類的候選仍正常比對；不含修飾詞的候選間比對不受影響（迴歸） |
| `docs/論文/03_系統設計與方法論.md` § 3.4 `GUARD4` | 補上範圍修飾詞這一支（若確認實作） |
| `docs/論文/02_文獻探討.md` § 2.4.5 | 標記機制從「設計討論中」→「已實作」 |
| `docs/報告/25_...md` §4 發現3 收尾 | 補一句指向本報告，標記「另立調查」已完成 |

## 5. 風險與相容性

| 風險 | 緩解 |
|---|---|
| 觸發詞清單覆蓋不全（新案例用「另」「再」等未涵蓋的字） | v1 保守清單，明確記錄「非窮舉」；比照既有慣例，發現新案例再擴充（如發現3→發現C 的擴充模式），不追求一次到位 |
| 過度保守：兩個真的同義、只是一個多寫「增加」的候選被誤判為不同實體 | 與 `_MEASURE_PATTERN` 同樣的既有取捨——寧可少合併（多一個節點、下游仍可個別檢索到），不可誤合併（資料寫死錯誤關聯）。報告25 已在多處採用相同保守側原則 |
| 現有測試可能有實體名稱恰好含「增加/額外/追加/新增」且預期被合併 | 實作前先跑 `grep` 掃描既有測試與真實 KG 資料，確認無現存的合法合併案例會被新守衛誤擋 |
| c15949bf 全量重抽尚未完成，抽取端程式碼變動政策 | **本設計屬 `resolve_entity_name()`（DEDUP4，抽取端相鄰程式碼）**——若主 drain 仍在跑，需依既有政策等全量完成、抽取端凍結後才能上線，不能像報告27/28（純查詢端）那樣立即實作 |

## 6. 驗證計畫

1. 單元測試（可立即寫，不受主 drain 狀態限制）：
   - 「每一型式」為 `name`、候選含「每增加一種型式」→ 不合併
   - 「每增加一種型式」為 `name`、候選含「每一型式」→ 不合併（對稱驗證）
   - 「每增加一種型式」為 `name`、候選含另一個同樣含「增加」的短語且編輯距離達標
     → 正常合併（確認不是全面阻斷，只擋跨類）
   - 不含修飾詞的既有案例（如既有的「台積電」/「台積電公司」）不受影響（迴歸）
2. 真實驗證（**須等主 drain 全量完成、抽取端凍結**）：對 N0070020 重抽後查
   `每一型式`／`每增加一種型式`（或該次抽取實際措辭）是否為兩個獨立節點、
   各自正確連到 `新臺幣八千元`／`新臺幣四千元`。

## 7. 落地順序

1. ✅ **本報告確認**
2. ⏳ 單元測試 + 程式碼實作（`resolve_entity_name()` 新增守衛）——**須等主 drain
   全量完成、抽取端凍結**（跟 L2 同一政策，見報告27 §4）
3. ⏳ 全量完成後，真實驗證（見 §6 第2項）
4. ⏳ 論文 §2.4.5／§3.4 `GUARD4`／報告25 §4 發現3 收尾 同步「已實作」狀態

## 8. 相關文件

- `docs/報告/25_擴大版新舊KG問答品質比對報告.md` § 4 發現3（觸發，含「另立調查」原文）
- `docs/論文/03_系統設計與方法論.md` § 3.4 `DEDUP4`／`GUARD4`
- `docs/論文/02_文獻探討.md` § 2.4.5（RQ4b）
- `docs/參考文獻/24_短語模糊合併與否定範圍偵測/`
