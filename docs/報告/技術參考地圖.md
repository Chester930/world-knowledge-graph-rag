# 技術參考地圖

> 目的：逐層記錄「本專案哪個功能/元件，主要參考了哪個外部專案，針對哪些重點做了補強，實際在哪個分支/PR 導入」。這是**持續累積**的追蹤表，不需要一次填滿——每次真正把某個借鏡技術落地實作時，才登記一列。
>
> 與既有文件的分工：
> - `產品競品研究/` — 競品調查，回答「市面上有哪些做法」
> - `技術導入評估.md` — 逐項技術的「要不要採用」評估，回答「值不值得做」
> - **本文件** — 只記錄**已經或正在實作**的項目，回答「實際做了什麼、參考誰、在哪一版」
>
> 用途：這份表格是 02 文獻探討（哪些設計選擇有對應文獻/前例）與 03 系統設計與方法論（哪些架構決策的參考依據）的**素材庫**，寫作時直接從這裡撈資料，不用重新回憶當初為什麼這樣設計。
>
> 專案定位提醒：本專案（v2）**不是**智慧知識庫（v1）的延伸開發，而是在 v1 已驗證核心概念可行性與價值的前提下，重新、更全面地準備一套完整的專案與論文。因此下表的「主要參考專案」欄，v1 也可以是一個條目（視為「已驗證但需要重新設計」的參考來源，不是「直接沿用」的來源），需在補強重點欄註明是重新設計還是延用邏輯。

## 追蹤表

| # | 本專案功能/元件 | 主要參考專案 | 補強重點 | 導入分支/PR | 狀態 | 對應論文章節 |
|---|---|---|---|---|---|---|
| — | （尚無已落地項目，待實作後登記） | | | | | |

## 規劃中候選項目（尚未有分支，先佔位）

以下取自 `技術導入評估.md` 的建議與 `01_本地抽取與混合架構評估.md`／`02_競品借鏡與技術導入藍圖.md` 已定案的具體規格，一旦真正開分支實作，移到上方追蹤表並補齊分支/PR 資訊：

| 候選功能/元件 | 主要參考專案 | 補強重點 | 對應論文章節（預期） |
|---|---|---|---|
| 本地抽取 + 雲端閘道混合架構 | WhyHow.ai / Zep（設計理念）＋自訂 Electron Daemon 設計 | 解決雲端 LLM 抽取的 Token 成本與批次任務中斷風險，見 `01_本地抽取與混合架構評估.md` | 03 系統設計與方法論、04 系統實作 |
| 雙層圖結構（Lexical + Entity Graph） | Neo4j LLM Knowledge Graph Builder ＋ HippoRAG (Gutiérrez et al., 2024) | 讓回答可追溯到原始 chunk，強化可解釋性 | 03、04 |
| 實體去重 | RAGFlow | 避免同義詞被當成不同節點，提升圖譜品質 | 03、04 |
| GraphRAG 選配化 + Global/Local 查詢 | RAGFlow（工業實作）＋ Edge et al. (2024) Microsoft GraphRAG（學術原始文獻） | 解決建圖冷啟動延遲、補上全局查詢能力 | 03、04 |
| 雙時態知識圖譜 | Zep/Graphiti（Rasmussen et al., 2025） | 補強現有隱性時序衰減為顯式有效期追蹤 | 03、04（進階，優先序中） |
| 命名空間隔離 + Service-first 架構 | WhyHow.ai | 輕量多知識庫隔離，並記取其棄用純 SDK 的教訓 | 03 |
| 本地輕量向量 + 段落共現分析 | Obsidian（Smart Connections / InfraNodus） | 降低冷啟動延遲、補足 SVO 抽取召回率盲點 | 03、04（進階，優先序中） |
| GraphRAG 語境生成（Prompt Grounding） | Obsidian（Simple Graph Builder） | 把 2-hop BFS 撈出的三元組轉譯為結構化 Context 並附來源標記 | 04 |
| 輕量語意推理（未來工作） | Stardog（理念參考，非直接採用其閉源平台） | `owlready2`/`rdflib`/`NetworkX` 規則傳遞，補強邏輯推理能力 | 07 結論與未來工作 |

## 登記範例（供之後參考格式）

> 以下為格式示範，非真實資料，實際登記時刪除本節或改為真實內容。

| # | 本專案功能/元件 | 主要參考專案 | 補強重點 | 導入分支/PR | 狀態 | 對應論文章節 |
|---|---|---|---|---|---|---|
| 範例 | 知識圖譜全局查詢（社群摘要） | RAGFlow（github.com/infiniflow/ragflow）＋ Edge et al. (2024) | 補上本專案原本缺乏的 Global Query 能力，解決「無法回答全局性問題」的缺口 | `feature/community-summary`（PR #XX） | ✅ 已合併 | 03.2 系統架構、04.3 全局查詢實作 |
