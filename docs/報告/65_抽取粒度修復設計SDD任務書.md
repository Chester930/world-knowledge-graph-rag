# 65 抽取粒度修復設計SDD任務書：條件限定詞遺失問題（F）

**建立日期**：2026-09-22
**文件性質**：設計提案。**本報告內容尚未實作，需使用者核准後才可開始撰寫程式**——這是流程規矩（先查文獻與參考專案、記錄查證結果，未確定內容以報告形式儲存，確認後才進論文與程式碼），不是拖延。
**分支／工作區**：`worktree-sdd-retrieval-comparison`，`D:\Users\666\Desktop\world knowledge graph rag\.claude\worktrees\sdd-retrieval-comparison`
**接續脈絡**：HANDOVER.md 2026-09-22「A/B → F → C → D/E → G/H」優先序排定；F 對應報告62 §4.21 類別B（抽到但破碎／丟失限定條件，35個gold span中佔14個、40%）。

---

## 0. 一頁摘要

**問題**：`services/svo_service.py::_svo_prompt()` 規則7要求「一句話的每一項附加規定都各自拆成獨立三元組」（報告18/19/20時期為修復「遺漏」問題而設計），副作用是遇到「共同條件 → 一個或多個結果」的複合法規句時，條件與結果被拆進互不相干的三元組，任一筆單獨看都不完整。今天（2026-09-22）人工複核 `57-AGGR19` 時又抓到一個新鮮案例：Fact 排名**第1名**（不是排序問題）仍判未命中，因為丟了「依前項第一款規定」這個適用範圍限定。

**根因已用真實資料定位**（非猜測），見 §1。**文獻查證已完成**（§2，`docs/參考文獻/37_三元組條件限定詞與超關係表示/`），誠實結論：找不到直接研究「LLM 抽取時如何在『避免遺漏』與『避免拆散丟條件』之間取捨」的文獻，但相鄰領域（proposition 檢索、超關係知識圖譜、法律規範顯式表示）都認真對待「限定條件不該被丟」這個大原則，可作為設計方向的旁證，不能宣稱「文獻證明這樣做有效」。

**建議**：先做方向A（修 prompt，成本低、與既有修法慣例一致），用定向重抽驗證（不動全域），確認有效後再考慮是否需要方向B/C。**本報告完成後，下一步是使用者核准，再開始寫程式**。

---

## 1. 問題診斷（真實資料，非假設）

### 1.1 資料來源

`data/eval/baseline_runs/20260920_frozen/analysis/retrieval_failure_diagnosis.json`（報告57 §4.21 產出，唯讀查詢 Neo4j 產生，含真實 Fact 文字與排名）。

### 1.2 具體案例（原文 vs. 實際被拆出的 Fact）

| 題目 | 原文（單一複合句） | 實際 Fact 候選（節錄） | 診斷 |
|---|---|---|---|
| `57-AGGR19` | 「職業災害勞工**依前項第一款規定**終止勞動契約時，準用勞動基準法規定預告雇主」（第85條第2項） | 排名第1、cos=0.942：「職業災害勞工 終止勞動契約時 準用勞動基準法規定預告雇主」 | **丟了「依前項第一款規定」這個適用範圍限定**；排名已是最好情況，不是檢索排序問題 |
| `57-AGGR7` | 「雇主依本法第二十四條第二項規定辦理訓練，並申請訓練費用補助者，最低開班人數應達五人，且訓練時數不得低於八十小時」（第6條） | 拆成4筆獨立 Fact：「最低開班人數應達五人」「訓練時數不得低於八十小時」「雇主依本法辦理訓練」「訓練申請補助費用」 | 數字類兩筆完全丟了「雇主辦理訓練並申請補助」這個前提，看起來像無條件成立 |
| `57-AGGR8` | 「雇主依前項規定辦理職業訓練，中央主管機關得予訓練費用補助」（第24條） | 「中央主管機關 得予 訓練費用補助」 | 丟了「雇主依前項規定辦理職業訓練」這個前提 |
| `57-CANARY5` | 「本法施行前依法應為所屬勞工辦理參加勞工保險而未辦理之雇主，其勞工發生職業災害事故…經…發給補助者，處以…罰鍰」（第101條，兩個並列條件才罰） | 拆成兩筆，各自只有一半條件 | 兩個並列條件（未辦理保險 + 依保護法發給補助）分家 |
| `18-Q5` | 「有左列情事之一者，給予三至七日之特別休假」（第4條，列舉前言） | 各列舉項目正確帶天數（如「因特殊功績受頒勳章者 給予 三至七日之特別休假」），但**前言本身**單獨被抽成一筆「左列情事之一者 給予 特別休假」——原文明明有「三至七日」，這筆卻掉了 | 與 `57-DIST1/2`（報告62 §14.10）同型：列舉前言的天數在「前言自身」這個獨立三元組裡遺失，即使各細項都正確 |

### 1.3 根因

`services/svo_service.py::_svo_prompt()` 規則7（第232行）：

> 「一句話常常包含不只一件事實…這些都要各自拆成獨立的三元組完整抽出，不可以只抽主要動作、把附加規定省略掉…」

這條規則是報告18/19/20時期為了修復「遺漏」問題（如漏抽「十四日」上限）刻意設計的，本身沒有錯，但沒有處理「拆出來的多筆三元組之間如何保留彼此的條件關係」——這是規則7的**設計範圍之外**，不是它做錯了什麼。

### 1.4 與已知資料結構的落差（技術可行性查證）

查證 `models/knowledge_graph.py::SVOTriple`：**已經有** `source_sentence_start`／`source_sentence_end`（句子範圍，1-based）欄位（第216-217行），抽取時就會填。但查證 `services/svo_service.py::_create_fact_node()`（第1830-1905行）發現：**這兩個欄位目前沒有被寫進 Fact 節點**（Cypher `CREATE` 只寫了 `source_doc_id`／`source_svo_chunk_index`，第1885-1890行），只是呼叫端遺漏傳遞，不是能力不存在。每個 Fact 節點對應**恰好一次**特定三元組的抽取（`merge_triples_to_graph()` 第2039行註解：「每筆 citation 各自產生一個 Fact 節點，永不相互覆蓋或平均」），觸發 `_create_fact_node()` 呼叫的 `triple: SVOTriple` 物件在呼叫現場（第2049-2063行）本來就持有這兩個欄位。這個發現是方向B（見§3.2）技術可行性的依據。

---

## 2. 文獻與參考專案查證（2026-09-22，先於實作完成）

完整查證記錄：`docs/參考文獻/37_三元組條件限定詞與超關係表示/README.md`；已同步登錄 `docs/論文/02_文獻探討.md` § 3.1.3 三筆新條目。摘要：

| 文獻 | 角色 | 誠實強度 |
|---|---|---|
| Chen et al. (2023/2024) Dense X Retrieval | proposition 需「去脈絡化」（decontextualization）：必要修飾語要複述進原子單元本身，不能依賴外部脈絡才能理解 | 中——判準精神直接對應方向A，但原文處理的是段落內代名詞指代，非法規條件子句，具體做法為本專案自行設計 |
| Galkin et al. (2020) StarE（超關係知識圖譜） | 用顯式 qualifier（key-value pair）附掛在主三元組上，消歧義或限制事實有效範圍；實證顯示比純三元組表示法在連結預測任務上提升最高25 MRR points | 中高——超關係表示法是成熟研究方向，但論域（連結預測 vs. LLM抽取+檢索）不同，具體演算法不可套用，成本也高於方向A |
| Krótkiewicz et al. (2026) 法律規範顯式範圍表示 | 佐證「法律規範範圍應被顯式建模」是法律資訊學界仍在研究的問題 | 弱——僅摘要層級，全文未取得，不引用技術機制 |
| OPIEC 語料庫（Gashteovski et al. 2019） | OIE 領域普遍替三元組保留來源句子 provenance（佐證方向B 的 schema 補完在領域內不算特殊） | 弱——只佐證「保留 provenance」常見，「用 provenance 做同句聚合補償」本身查無先例 |

**誠實結論**：**沒有文獻直接研究本專案這個具體取捨**（抽取時避免遺漏 vs. 避免拆散丟條件）。上述文獻只能佐證「限定條件/範圍不該被丟」這個大原則在相鄰領域被認真對待，**不構成「這樣做一定有效」的證明**——是否有效要靠 §4 的定向重抽驗證來確認，不能只憑文獻類推就大規模採用，這與報告61 的一貫立場一致。

---

## 3. 候選設計方向

### 3.1 方向A（建議優先）：修 prompt，條件複述進每筆衍生三元組

在 `_svo_prompt()` 規則7後新增一條規則與反例：當一句話是「共同條件 → 一個或多個結果」的複合結構時，**條件要重複寫進每一筆結果三元組的 verb 裡**，不能只留在單獨一筆條件三元組上。例如 `57-AGGR7` 應變成：

```
{"subject": "最低開班人數", "verb": "（雇主依本法第二十四條第二項規定辦理訓練並申請訓練費用補助時）應達", "object": "五人"}
{"subject": "訓練時數", "verb": "（雇主依本法第二十四條第二項規定辦理訓練並申請訓練費用補助時）不得低於", "object": "八十小時"}
```

**成本/風險**：低。只改一個 prompt 字串＋加反例，跟規則6/7/8/9既有修法模式一致（report24/25/29/32一貫做法），不需要 schema 變更、不需要 backfill。

**已知取捨**：verb 文字變長、同一條件在多筆三元組裡重複出現，`fact_text`／embedding 會有一定冗餘；但每筆三元組單獨可解讀，直接對症診斷出的 gold span 缺陷。

**⚠️ 架構定位（2026-09-22 補記，使用者要求釐清）：規則10目前是全域生效，不是per-KG範圍**。查證 `core/kg_config/model.py`／`docs/論文/04_系統實作.md`（報告33 §6「尚未做」清單）確認：`KGConfig` + domain pack 機制（`generic` vs `taiwan-labor-law`）目前**已經**涵蓋生成端 prompt 前綴（`DomainConfig.system_context`）、輸出語言、切塊策略、多項數值門檻，**但 `services/svo_service.py::_svo_prompt()`（含所有規則1-10）仍是單一全域寫死函式，沒有per-KG覆蓋機制**——這是專案自己已記錄在案的已知缺口，非本次新發現：論文明講「抽取少樣本、守衛正則、DEDUP 門檻目前仍寫死」。**因此本次新增的規則10會套用到所有KG，不限於驗證用的KG#4**。

**與未來 `svo_fewshots` 機制的關係（使用者釐清：通用/特定的關係應該是「參數導入」，不是「整套換掉」）**：報告33 §3（`domain_packs/generic/svo_fewshots.json` 為空陣列、`domain_packs/taiwan-labor-law/svo_fewshots.json` 放勞動法規正反例組）本來就是設計成**覆蓋／參數注入模式**，不是「每個domain各自維護一份完整的`_svo_prompt()`」——這跟 `DomainConfig.system_context` 單一欄位覆蓋、`guard_profile` 用 token 清單組出守衛正則，是同一套設計原則（domain pack 覆蓋 generic、查無則 fallback，見論文 3.9.3）。若未來要把規則1-10移進這個機制，正確切法應該是：
- **留在共用骨架（skeleton）不動**：JSON輸出格式、rel_type合法清單、規則1-5（通則：型別選填、verb保留原文、confidence範圍等）、以及規則6-10**各自的通用原則本身**（例如規則10的「共同條件複述進每筆衍生三元組」這個邏輯本身跟法規領域無關，任何領域的複合句都適用）。
- **可抽成 `svo_fewshots` 注入參數**：規則6-10目前附帶的具體例句（婚假八日、事假十四日、中高齡者職業訓練補助等，皆為勞動法規詞彙）——這些才是真正domain-specific、應該讓`taiwan-labor-law`與`generic`各自替換的部分。
- 報告33 §5 R4已經記錄一個已知風險：`generic` pack 的 few-shot 若留空，qwen對「有規則但沒範例」的遵循度可能下降——這代表就算未來做了這個切分，`generic` domain 是否要放一組中性/抽象範例（而非完全空陣列）需要另外驗證，不能假設「拿掉範例只留規則」一定等效。
- **本階段不執行這個切分**：這是報告33 §6既定但待drain完成的路線圖項目，不在本次F任務範圍內，此處只記錄正確的架構原則供未來接手者參考，避免屆時又走回「每個domain整份複製`_svo_prompt()`」這種重寫式的錯誤設計。

### 3.2 方向B：Fact 節點補句子層級 provenance，檢索/組裝時撈同句兄弟 Fact

`_create_fact_node()` 新增 `source_sentence_start`／`source_sentence_end` 兩個扁平屬性（沿用 2026-08-24 幫 `article_no` 做過的同一套模式），既有 16826 筆 Fact 需要 backfill（`backfill_fact_nodes()` 已有先例可仿照）。檢索到任一 Fact 時，額外查詢「同一 `source_doc_id`＋`source_svo_chunk_index`＋句子範圍重疊」的其他 Fact 一起帶入 context——比 T3（報告62，整條文擴充）更精準，不會像整條文那樣拉進不相干的其他列舉項目、拉低 SNR。

**成本/風險**：中高。需要 schema 新增＋backfill＋檢索/組裝端新邏輯；且**救不了已經污染的 embedding**——`fact_text` 本身還是破碎的，語意檢索排序不會變好，只能改善「展示時的完整性」，不能改善「排名」。

**定位**：方向A解決「一開始別讓事實破碎」，方向B解決「已經破碎的事實在展示時補救」，兩者不互斥，但方向B成本明顯更高，本階段不做，留作方向A效果不足時的候選。

### 3.3 方向C：Fact 引入顯式 qualifier（超關係表示，非本階段候選但記錄在案）

比方向A更原則性的做法：把「依前項第一款規定」這類限定條件建模為 Fact 的顯式 qualifier（新屬性或獨立邊），而非複述進自然語言 verb 字串。有文獻佐證（Galkin et al. 2020），但需要 Fact schema 根本性改造（新增 qualifier 資料結構、抽取 prompt 改輸出格式、下游所有讀取 Fact 的程式都要適配），成本遠高於A/B，且沒有本專案技術路線（LLM prompt 抽取＋cosine 檢索）下的直接先例可循。**本階段不評估實作，僅記錄為長期候選**。

---

## 4. 驗證計畫（先做，不動全域）

1. **改 prompt**：在 `_svo_prompt()` 加入方向A的新規則與反例（覆蓋條件→多結果、以及列舉前言天數兩種案例，即 §1.2 的 `57-AGGR7`/`57-AGGR8`/`57-CANARY5` 型與 `18-Q5` 型）。
2. **定向重抽**：用既有 `scripts/kg/reextract_chunks.py`（不是全量重抽），對 §1.2 五個案例對應的 chunk 做定向重抽——先用 `scripts/kg/reextraction_manifest.py` 或直接查 `LawArticle` 節點定位這些條文（`D0080015`第2-4條、勞工職業災害保險及保護法第85條/第26條、就業保險促進就業實施辦法第6條/第24條、第101條等）各自對應的 chunk index。
3. **重新診斷**：對這批 chunk 重新跑一次 `retrieval_failure_diagnosis.py`（或等效的唯讀查詢），確認：
   - 目標 Fact 的 `fact_text` 是否包含之前丟失的限定條件；
   - 是否有連帶副作用（例如其他既有正確的 Fact 被改壞、embedding 距離大幅改變導致排名劣化）。
4. **不擴大範圍前的決策點**：定向重抽驗證通過（條件回來、無新增劣化）才考慮是否要對更大範圍（例如整個 KG）重抽；驗證不通過則檢討 prompt 措辭，不直接放棄方向A。
5. **全程不動**：全域 `top_k`、scope filter、Hybrid、`source_doc_cap`、BFS 預設名額、題庫、正式 KG 資料（定向重抽在既有 KG 上進行，比照報告57附錄C既有慣例）。

---

## 5. 待使用者核准事項（已於2026-09-22核准，見§6/§7）

1. ~~是否核准方向A的 prompt 修改與 §4 定向重抽驗證計畫？~~ ✅ 已核准，見 §6 結果。
2. 若驗證通過，是否進一步討論方向B（何時做、backfill 範圍）？→ **§6 結果顯示尚未到「驗證通過」的程度，方向B討論延後**。方向C 暫不需要現在決定。
3. 本報告的文獻查證內容（§2）已記錄於 `docs/論文/02_文獻探討.md`；**方向A/B/C 的設計本身尚未進論文第三章方法論**，待實際採用並驗證後再回寫，符合「未確定內容以報告形式儲存」的既有慣例。

---

## 6. 定向重抽驗證結果（2026-09-22，背景執行，對 KG#4 正式資料的即時異動）

**環境**：Neo4j（`bolt://localhost:17990`）與 Ollama（`http://127.0.0.1:11434`）皆可直接連線，無需 WSL fallback。執行前 `git status -sb` 乾淨，無其他 session 未提交的異動。

**Chunk 定位方法**：不信任舊版 `retrieval_failure_diagnosis.json` 的近似 `article_no`，改用「查詢 `Fact{kg_id}` 依 `source_doc_id` 分組後，用 gold span 的關鍵字子字串比對 `fact_text` 直接鎖定 `source_svo_chunk_index`」，並讀過完整 chunk 內容再確認，9個目標 chunk 皆高信心。

**執行**：`scripts/kg/reextract_chunks.py` 對5份文件共9個chunk定向重抽，**9/9 首次即 `completed`，0 failed**。

### 6.1 逐案結果（前後對照，literal fact_text）

| # | 案例 | 結果 | 說明 |
|---|---|---|---|
| 1 | `D0080015` c3（`57-DIST2`型，二至五日） | ✅ **修復** | 4筆Fact全部由「給予 特別休假」變成「給予 二至五日之特別休假」，含前言本身。3筆出現「因 給予二至五日之特別休假」這種語法冗贅（verb重複），內容不缺，屬瑕疵非缺陷 |
| 2 | `D0080015` c4（`18-Q5`型，三至七日） | ✅ **修復**，但有新副作用 | 前言本身補回「三至七日」。**新產生的副作用**：4筆裡3筆的verb literal出現「因……者」——疑似LLM把反例文字裡的刪節號占位符原樣抄進輸出（copy bias，本專案`docs/參考文獻/16_少樣本提示範例干擾/`已有這類失效模式的文獻先例），冗贅but不是資訊遺失 |
| 3 | `N0090058` c6（`57-AGGR7`） | ✅ **乾淨修復，教科書等級** | 原4筆條件與2筆數字互不相干；重抽後3筆，條件複述進兩筆數字三元組的verb，正是規則10設計的目標行為 |
| 4 | `N0060041` c26（`57-AGGR19`舊法） | ✅ **乾淨修復** | 原4筆含2筆無條件的裸結果；重抽後2筆，條件與結果合併成單一自足三元組 |
| 5 | `N0090055` c18（`57-AGGR8`主要目標） | ❌ **未修復＋新增退步** | 目標三元組「中央主管機關 得予 訓練費用補助」重抽後**完全沒變**，條件仍缺席。且**新出現一筆疑似角色互換錯誤**：原本「中央主管機關 並提供 訓練輔導協助」，重抽後變成「雇主 依前項規定 得提供訓練輔導協助」——主詞從中央主管機關換成雇主，與規則10邏輯無關，屬本次抽取的一般性雜訊 |
| 6 | `N0090055` c24（`57-AGGR8`次要） | ⚠️ **部分/語句破碎** | 新增「中央主管機關 依前項規定 訓練費用補助」——條件文字有進來，但verb漏了「得予」這個授予動作，語法不完整 |
| 7 | `N0050031` c84（`57-AGGR19`新法·雇主側） | ⚠️ **部分改善** | 結果句從「終止勞動契約 準用…」變成「雇主預告終止勞動契約時 準用勞動基準法規定預告勞工」，但3個並列的終止事由仍各自獨立、未連回結果——**規則10設計的是「單一條件→結果」，不是「多個並列條件→共享結果」，這個結構性缺口是預期內、不是bug** |
| 8 | `N0050031` c85（`57-AGGR19`新法·勞工側，**本案報告62/57原始診斷的主要標的，原本排名第1名**） | ❌ **未修復＋資料品質退步** | 目標三元組「職業災害勞工 終止勞動契約時 准用勞動基准法規定預告雇主」**實質內容跟修改前一樣，仍缺「依前項第一款規定」**。新出現一筆「職業災害勞工 依前項第一款規定終止 勞動契約」帶了條件文字，但配對到錯的謂語（單純「終止」，不是預告義務那句）——條件與結果依然分裂在兩筆，正是規則10想避免的失效模式。**新增退步**：「准用」「基准法」是**簡體字**混入這筆`fact_text`（應為「準用」「基準法」）——本專案已有簡體字白名單防護（`_to_traditional_selective`／`source_charset`，報告25發現4），這次卻沒攔到 |
| 9 | `N0050031` c101（`57-CANARY5`） | 🔴 **嚴重退步** | 重抽前3筆（雖破碎但各自帶部分條件細節：「未辦理保險」獨立一筆＋一筆雖語序凌亂但仍提到「本法施行前」「職業災害事故致死亡或失能」「依本法施行前職業災害勞工保護法第六條規定發給補助者」）。**重抽後坍縮成1筆**：「雇主 處以 補助金額相同額度之罰鍰」——**所有條件全部消失**（無「未辦理保險」、無「本法施行前」時間範圍、無「發給補助」這個前提），讀起來像「雇主一律會被罰這個金額」，比修改前明顯更差 |

### 6.2 淨結果

- **乾淨修復**：4/9（c3、c4、c6、c26）——規則10在「單一條件→結果」這種乾淨結構上確實有效。
- **未修復**：2/9（c18、c85）——**c85正是報告57/62/65最初診斷用的主要案例，重抽後實質內容沒有改善**。
- **部分/破碎**：2/9（c24、c84）。
- **新增退步**：4個（c18角色互換、c85簡體字混入、c85條件仍分裂、**c101嚴重退步**），其中 c101 是本次驗證裡最嚴重的問題——已存在的部分條件資訊反而在重抽後完全消失。

**誠實結論**：規則10對它設計針對的「單一共同條件→一個或多個結果」結構有效，但（1）不能處理「多個並列條件→共享結果」這種更複雜的結構（c84/c85，這本身在報告65設計時就沒宣稱涵蓋，屬已知邊界）；（2）**這次真實重抽出現了跟規則10邏輯無關的一般性抽取雜訊退步**（c18角色互換、c85簡體字洩漏、c101嚴重坍縮），這些不是規則10「做錯了什麼」，比較可能是`qwen2.5:7b`在這批重抽呼叫上的非決定性雜訊（報告20已載明溫度0.0不保證決定性，根因是batch size依賴），但fork未重跑驗證可重現性（超出授權範圍，且腳本本身9/9首次即completed、沒有觸發內建重試）。

**這不構成「驗證通過」**——4/9乾淨修復不足以支撐「大範圍套用」的判斷，尤其原始診斷的主要標的（c85）依然沒修好，且產生了一個嚴重退步（c101）。

---

## 7. 待使用者裁示（2026-09-22，取代原§5，KG#4資料已被即時異動，需要決定下一步）

**現況**：KG#4（`236903cf-055a-40a8-8923-b9d06601f3b7`，正式資料，供凍結基準與後續所有評測比較使用）的9個chunk已經被重抽覆蓋，其中1個（c101）明顯比修改前差、1個（c85）簡體字混入、1個（c18）疑似角色互換錯誤。**這些異動目前還留在KG裡，還沒有回復。**

**選項**：

1. **回復這9個chunk到重抽前的狀態**：需要用fork留存的證據檔（`.claude/tmp/report65_targeted_reextraction_20260922/before_target_reconstructed.json`／`after_target.json`／`all_facts_after.json`）評估能否可靠寫回——這些是「重建」的before快照，不一定是可以直接回灌 Neo4j 的完整格式，需要先確認技術上能不能乾淨復原，不能保證。
2. **只修復退步最嚴重的 c101，其餘維持現狀**：c101是唯一「明顯比修改前差」的案例，其餘（c18/c85）雖未改善但也不算比修改前更差（c85修改前後實質內容相同；c18是另一筆三元組的新雜訊，目標三元組本身沒被動到，需要進一步查證是否真的比修改前差）。
3. **維持現狀，不回復，把這次結果當診斷資料**：4/9乾淨修復証明規則10方向可行，但需要先解決「一般性抽取雜訊」與「多條件並列」這兩個問題再考慮擴大，這次的9個chunk留著當「已知有瑕疵」的樣本，之後方向A修正版重抽時一併處理。
4. **重新設計規則10或加安全閘門**：例如加一個確定性檢查（比照報告20/29既有的忠實性守衛模式），重抽後若偵測到條件數量比修改前減少、或出現簡體字，自動標記需人工複核，不靜默接受。

**我的初步判斷（供參考，非定論）**：選項2＋4合併——c101 的退步明確且嚴重，應優先處理（回復或重新用更嚴謹的方式重抽這一個chunk）；c18/c85維持現狀繼續當診斷樣本；規則10本身的「單一條件→結果」有效性已獲部分驗證，但**在有更好的忠實性防護（選項4）之前，不建議擴大重抽範圍**。方向A暫時判定為「部分驗證，需要加固後才能推廣」，不是「驗證通過可以推廣」。

**待你決定**：上述4個選項要選哪個（可複選/混合），以及是否要我先嘗試技術上能否回復 c101。

---

## 8. 選項2+4執行結果（2026-09-22，使用者已確認選2+4）

### 8.1 選項4：確定性防護閘門（commit `860654a`）

1. **已知簡體詞組修正**：新增 `services/svo_service.py::_fix_known_simplified_compounds()`，逐詞比對已知易錯詞組（`准用→準用`、`基准法→基準法`），補救 `_to_traditional_selective()` 逐字白名單的結構性盲點——「准」在「核准」是這個KG來源語料裡合法的繁體字，白名單因此保留它，但「准用」本應是「準用」，逐字比對抓不出「同一個字在特定詞組裡用錯」。已接進 `traditionalize_triples()`，對所有未來抽取／重抽生效。
2. **定向重抽內容流失防護閘門**：`scripts/kg/reextract_chunks.py` 新增重抽前後 `fact_text` 總字數快照比對，低於門檻（預設70%）時印出警告並寫入 `content_loss_flags.json`，**不自動阻擋或回復**（機械式字數比對不代表判斷一定對，只是提示需要人工核對）。這是報告65 §6.2 c101案例（task_queue.db狀態是`completed`但內容坍縮）的直接對症——光看佇列狀態抓不到這種退步。
3. 全套 pytest 1048 passed（+5新測試，含直接用c101真實數字驗證 `_content_loss_ratio()` 會抓到這個案例、用c85的簡體字驗證新詞組修正）。

### 8.2 選項2：c101（`57-CANARY5`）已修復並驗證

**方法**：不採用「回復」（fork留存的 `before.json` 因腳本bug被重抽後的狀態覆蓋，fork已誠實揭露並改用對話紀錄重建的 `before_target_reconstructed.json`，但那只有S-V-O字串、沒有完整Fact節點metadata，不足以可靠回灌）；也不採用「盲目重試碰運氣」——先試了一次自動重抽（`--retries 2`），結果從「條件全部消失」進步到「補回1/3個條件」（"雇主 本法施行前依法應為所屬勞工辦理參加勞工保險而未辦理 處以 補助金額相同額度之罰鍰"），但仍缺「勞工死亡或失能」與「依前法第六條發給補助」這兩個條件——證實qwen2.5:7b對這種三個條件鏈接的複合句不穩定，不是重試幾次就能穩定修好。

**改採**：直接核對 `svo_index.json` chunk 101 的原文（權威來源，第101條官方全文），手動撰寫**一筆完整自足**的三元組，逐字忠於原文所有條件，經**與正常抽取相同的 `merge_triples_to_graph()` 管線**寫入（真實 embedding、正常 Entity 連結、`SUPPORTED_BY→LawArticle`），不是繞過管線的特殊寫法：

```
雇主 本法施行前依法應為所屬勞工辦理參加勞工保險而未辦理，其勞工發生職業災害事故致死亡或失能，
經依本法施行前職業災害勞工保護法第六條規定發給補助者，處以 補助金額相同額度之罰鍰
```

三個條件（未辦理保險／勞工死亡或失能／依前法發給補助）與結果（罰鍰）全部保留在單一自足事實裡，比修改前的原始3筆破碎版本更完整（原始版本本身也是類別B式破碎，見報告57 §4.21）。已用查詢驗證這是目前 Neo4j 裡唯一一筆 c101 的 Fact，`revoke_chunk_facts()` 先清乾淨（`facts_deleted:1, edges_deleted:1`），沒有殘留孤兒資料。

**誠實聲明**：這是人工撰寫、透過既有管線程式碼路徑寫入的**一次性人工修正**，不是LLM抽取結果，`rel_type` 用 `RELATED_TO` 安全兜底（原始rel_type因fork的before.json被覆蓋而不可考）。若之後全量或更大範圍重抽這份文件，這筆手動修正會被覆蓋，需要屆時視規則10的加固程度決定是否重做。

### 8.3 未處理項（非本次選項範圍，供之後決定）

- `57-AGGR19`（c85）的簡體字混入（「准用」「基准法」）**尚未修補既有資料**——`_fix_known_simplified_compounds()` 只對未來抽取生效，不會retroactively修正已經寫進Neo4j的舊資料。技術上可以用同一個函式對這筆既有Fact的`fact_text`做一次性文字替換＋重算embedding，成本很低，但不在使用者選定的「2+4」範圍內，需要另外確認才動手。
- `57-AGGR19`（c85）條件依然分裂在兩筆Fact（「終止勞動契約時準用勞動基準法規定預告雇主」缺「依前項第一款規定」）——這是報告57/62最初診斷的主要標的，選項2只處理了c101，這題仍待規則10加固（例如擴充涵蓋「條件本身也被拆成獨立三元組」這種情況）或方向B/C。
- c18的角色互換雜訊（中央主管機關→雇主）未處理，維持現狀當診斷樣本（選項3精神）。

### 8.4 方向A的最終判定（本輪收尾）

**方向A（規則10）維持「部分驗證，需加固」的判定，不升級為「驗證通過可推廣」**。已完成：c101明確退步已修復＋兩個確定性防護閘門已上線並測試覆蓋。**尚未完成、不建議現在做**：擴大重抽範圍（全量或更多chunk）、修復c85的條件分裂問題本身。若之後要繼續，應該先讓規則10涵蓋「多個並列條件」這種結構（目前只驗證過「單一條件」），再搭配已上線的防護閘門做下一輪範圍更大的定向重抽。
