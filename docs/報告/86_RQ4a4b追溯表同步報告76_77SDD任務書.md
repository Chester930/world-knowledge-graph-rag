# 86 RQ4a／RQ4b追溯表同步報告76/77進度SDD任務書（**交付 Codex，改 `docs/論文/00_研究追溯對映表.md`，不改程式碼**）

**建立日期**：2026-09-26
**文件性質**：交付 Codex 執行用任務書。**設計成冷啟動、可自主執行完成，不需要往返確認**——本任務書已把要插入的確切文字都寫好，Codex 只需要核對現況與本任務書描述一致，再照著插入，不需要自行設計新的措辭或判斷取捨。改動範圍限定 `docs/論文/00_研究追溯對映表.md` 一個檔案，不碰程式碼、不需要 Neo4j/Ollama、不需要新文獻查證。
**分支／工作區**：`worktree-sdd-retrieval-comparison`，`D:\Users\666\Desktop\world knowledge graph rag\.claude\worktrees\sdd-retrieval-comparison`
**前置依賴**：報告76（guard_profile領域可插拔化，已完成、已獨立複驗、已push，commit `2da08c0`）、報告77（法律模態關係型per-KG擴充，已完成、已獨立複驗、已push，commit `b598f34`）。

---

## 0. 一頁摘要

`docs/論文/00_研究追溯對映表.md` 的 RQ4a（受控語意關係詞彙標準化）與 RQ4b（實體指代、別名與跨文件對齊）兩列，現況欄位停留在報告76/77之前的舊狀態。這兩份報告分別為 RQ4a（新增per-KG可擴充關係詞彙機制）與 RQ4b（把既有模糊合併守衛改成per-KG可配置）補上了新的工程進度，**但不是完整的RQ消融實驗**——判定結論（「RQ正式消融實驗未完成」）本身不變，只是把新增的機制據實補進「04／程式現況」欄。這跟報告83替GAP-S3-01/02補試驗結果進未來工作章節是同一種「讓文件跟實際進度同步」的維護性任務，範圍比報告80（RQ1整章填寫）小很多。

---

## 1. 具體修改內容（已寫好確切文字，直接核對後插入）

### 1.1 §2 研究問題總對映——RQ4a 列（目前在第35行附近，用內容比對找到正確行，不要用行號寫死）

**現行文字**（用來定位，核對完全一致再動手；若因檔案其他部分已變動導致行號或文字有出入，以文字內容比對為準，不要盲目照行號改）：

```
| RQ4a | 受控語意關係詞彙標準化 | 2.2.4；Schema.org、ConceptNet、OpenIE、AutoRE、KGGen | 3.3；工程機制另見 3.1.3 | `core/constants.py::SVO_REL_TYPES`、`services/svo_service.py`、`services/expand_governance_service.py` | 機制已實作；RQ 正式消融實驗未完成 |
```

**改為**（只改「04／程式現況」與「判定」兩欄，其餘欄位不動）：

```
| RQ4a | 受控語意關係詞彙標準化 | 2.2.4；Schema.org、ConceptNet、OpenIE、AutoRE、KGGen | 3.3；工程機制另見 3.1.3 | `core/constants.py::SVO_REL_TYPES`、`services/svo_service.py`、`services/expand_governance_service.py`；**per-KG可擴充關係詞彙（2026-09-25，報告77）**：`core/kg_config/model.py::RelTypeExtension`與`DomainConfig.rel_type_extensions`，`SVO_REL_TYPES`（ConceptNet 5.5官方35個核心關係）本身不動，透過`services/svo_service.py::_effective_rel_types()`／`_effective_rel_type_descriptions()`把per-domain擴充型別注入抽取端REJECT、BFS圖遍歷Cypher過濾、SIM/QSIM embedding比對三處；shipped defaults含4個台灣勞基法法律模態型別（`OBLIGATES`／`PERMITS`／`PROHIBITS`／`DEEMS`，命名靈感未經文獻查證，非ConceptNet來源），`generic` domain pack覆寫為空 | 機制已實作；per-KG關係詞彙擴充機制已實作（報告77）；RQ 正式消融實驗仍未完成 |
```

### 1.2 §2 研究問題總對映——RQ4b 列

**現行文字**（用來定位）：

```
| RQ4b | 實體指代、別名與跨文件對齊 | 2.2.5；UD、Text2KGBench、CORE-KG、entity alignment 文獻；**模糊合併守衛另見 Mrkšić 2016、Tosun et al. 2026、Chapman et al. 2001 NegEx（2026-09-05 新增，2.4.5 對應段落）** | 3.4；工程機制另見 3.1.4 | `pronoun_resolution_service.py`、`entity_registry_service.py`、`svo_service.py::merge_entity()`／`resolve_entity_name()`（模糊合併守衛：`_QUANTITY_PATTERN`／`_MEASURE_PATTERN` 量詞守衛〔報告25 §4 發現3/發現C，2026-09-03〕＋`_RANGE_COMPARATOR_PATTERN` 裸數字比較詞早退守衛〔報告29 §4.3，2026-09-05，commit `df2bc62`〕） | 部分實作；正式 NER 上游與端到端實驗未完成；⚠️ 剩餘缺口：不含任何量詞的範圍修飾短語（「每一型式」vs「每增加一種型式」）——對應的 `_SCOPE_MODIFIER_PATTERN`（報告29 §4.1）為設計提案、待全量重抽完成後才上線 |
```

**改為**（只改「04／程式現況」欄，在既有內容後面追加，其餘不動；「判定」欄不用改，因為守衛可配置化不影響NER/端到端實驗完成度的判定）：

```
| RQ4b | 實體指代、別名與跨文件對齊 | 2.2.5；UD、Text2KGBench、CORE-KG、entity alignment 文獻；**模糊合併守衛另見 Mrkšić 2016、Tosun et al. 2026、Chapman et al. 2001 NegEx（2026-09-05 新增，2.4.5 對應段落）** | 3.4；工程機制另見 3.1.4 | `pronoun_resolution_service.py`、`entity_registry_service.py`、`svo_service.py::merge_entity()`／`resolve_entity_name()`（模糊合併守衛：`_QUANTITY_PATTERN`／`_MEASURE_PATTERN` 量詞守衛〔報告25 §4 發現3/發現C，2026-09-03〕＋`_RANGE_COMPARATOR_PATTERN` 裸數字比較詞早退守衛〔報告29 §4.3，2026-09-05，commit `df2bc62`〕）；**per-KG可配置守衛（2026-09-25，報告76）**：`core/kg_config/model.py::GuardConfig`（`measure_units`／`range_trailing_comparators`／`range_leading_comparators`／`scope_modifier_words`／`enum_closed_values`），前述守衛正則改為「結構固定（中文數字字元類、間隔長度、序數/分數/小數/附表格式）＋domain token清單（可配置）」，shipped defaults逐字等於改動前寫死內容 | 部分實作；正式 NER 上游與端到端實驗未完成；守衛機制已改為per-KG可配置（報告76），但這是設定架構升級，不是`_SCOPE_MODIFIER_PATTERN`覆蓋率或NER端到端實驗的實質進展，原有「剩餘缺口」描述維持不變 |
```

**⚠️ 注意**：`_SCOPE_MODIFIER_PATTERN` 原本這句話已在既有文字裡標成「為設計提案、待全量重抽完成後才上線」，報告76已經把它做成可配置機制的一部分並已上線（見報告76 §T2「`_SCOPE_MODIFIER_PATTERN` 改為結構固定＋domain token清單」）。**需要判斷這句「待全量重抽完成後才上線」現在是否已經過時**——若你核對報告76內容確認 `_SCOPE_MODIFIER_PATTERN` 機制本身已隨報告76上線（只是「可配置化」而非「新內容上線」），請把這句改成準確反映現況的說法（例如「`_SCOPE_MODIFIER_PATTERN` 機制本身已上線且已可配置化，範圍修飾短語覆蓋率是否需要擴充仍是獨立問題」），不要保留一句已經不準確的舊陳述。這是本任務書唯一需要你自行判斷措辭的地方，其餘 1.1/1.2/1.3 都是直接插入已寫好的文字。

### 1.3 §3 由方法到程式的逐項追溯——「受控關係型別與未知型別仲裁」列

**現行文字**（用來定位）：

```
| 受控關係型別與未知型別仲裁 | ConceptNet、Schema.org；AutoRE、KGGen、零樣本關係分類文獻 | ConceptNet 是關係詞彙參考；`EXPAND` 治理流程是自有設計 | `core/constants.py::SVO_REL_TYPES`、`svo_service.py`、`expand_governance_service.py`、`expand_worker.py` | `test_svo_service.py`、`test_expand_governance_service.py`、`test_expand_worker.py` | 工程機制已實作；RQ4a 比較實驗未完成 |
```

**改為**（只改「04／程式現況」「測試證據」「現況與可宣稱範圍」三欄）：

```
| 受控關係型別與未知型別仲裁 | ConceptNet、Schema.org；AutoRE、KGGen、零樣本關係分類文獻 | ConceptNet 是關係詞彙參考；`EXPAND` 治理流程是自有設計；`RelTypeExtension` 機制是本專案原創設計，命名靈感未經文獻查證 | `core/constants.py::SVO_REL_TYPES`、`svo_service.py`、`expand_governance_service.py`、`expand_worker.py`、`core/kg_config/model.py::RelTypeExtension`（報告77，2026-09-25） | `test_svo_service.py`、`test_expand_governance_service.py`、`test_expand_worker.py`；`tests/core/test_kg_config.py`／`tests/services/test_svo_service.py`（報告77 per-KG擴充相關測試） | 工程機制已實作；per-KG可擴充關係詞彙機制已實作（報告77，`SVO_REL_TYPES`本身未變動）；RQ4a 比較實驗未完成 |
```

---

## 2. 明確不要做的事

- **不要**修改除 §2 RQ4a/RQ4b 兩列與 §3「受控關係型別與未知型別仲裁」一列以外的任何內容——包含檔案開頭的「更新：YYYY-MM-DD」歷史記錄列表也不要動（**除了新增一行，見下）。
- **不要**新增任何文獻查證或引用新文獻。
- **不要**修改 `core/constants.py::SVO_REL_TYPES` 本身或任何程式碼。
- **不要**宣稱 RQ4a／RQ4b 的正式消融實驗已完成——本次只是補上已完成的工程機制，判定結論裡「未完成」的部分要維持誠實，不能因為補了新機制就升級判定等級。
- **不要**修改第 01/02/03/04 章、`docs/參考文獻/`、`文獻與專案查核表.md`。

## 3. 完成定義

1. `git diff --stat` 只顯示 `docs/論文/00_研究追溯對映表.md` 一個檔案有異動。
2. §2 RQ4a、RQ4b 兩列與 §3「受控關係型別與未知型別仲裁」列都依 §1 的確切文字更新（§1.2 的 `_SCOPE_MODIFIER_PATTERN` 陳述除外，那處需要你核實後用準確措辭取代，不是逐字照抄）。
3. 檔案開頭的「更新：YYYY-MM-DD」歷史列表新增一行 2026-09-26 的說明，簡述本次同步報告76/77進度到RQ4a/RQ4b（比照既有格式）。
4. 沒有任何其他檔案被觸碰，沒有新增文獻引用。

---

## 4. 給 Codex 的指令（可直接貼上）

> 請執行 `docs/報告/86_RQ4a4b追溯表同步報告76_77SDD任務書.md` 的 §1（1.1-1.3）。這是純文件同步任務，只改 `docs/論文/00_研究追溯對映表.md` 一個檔案，不碰程式碼。任務書已經把要插入的確切文字寫好（§1.1、§1.3 直接照抄插入即可），唯一需要你自行判斷措辭的地方是 §1.2 提到的 `_SCOPE_MODIFIER_PATTERN`「待全量重抽完成後才上線」這句——請核對報告76內容，確認這句是否已過時，過時的話換成準確反映現況的說法。
>
> 目的：RQ4a（報告77的per-KG可擴充關係詞彙）與 RQ4b（報告76的守衛per-KG可配置化）兩份已完成、已獨立複驗的報告，進度還沒反映進追溯表——比照 RQ1（報告80）的做法，補進去，但**不要**把判定結論升級成「RQ已完成」，只是誠實補上「新增了什麼機制」，正式消融/端到端實驗未完成的結論維持不變。
>
> 完成後在檔案開頭新增一行 2026-09-26 更新說明。確認 `git diff --stat` 只涉及這一個檔案。commit 前不需要額外詢問，但 push 需要使用者另行同意。
