# 13. 基準題庫真實查詢驗證報告（10 題基準 + 5 題示範）

> **本報告所有結果皆為對真實 Neo4j（`bolt://localhost:17990`）與真實 Ollama
> （`qwen2.5:7b` / `nomic-embed-text`）執行後的真實輸出，走與正式
> `/agent/chat` 端點完全相同的程式碼路徑（`routers/agent.py::chat()`）。
> 沒有任何一題的回答、三元組筆數、耗時是模擬或臆測的——原始輸出存於
> `demo_report_raw.txt`（題1-10）與 `demo_report_batch3_raw.txt`（題11-15）
> （worktree 根目錄，未加入版控，僅供本次核對）。**
>
> 題 1-10 為 `docs/報告/12_知識圖譜抽取與問答Demo操作手冊.md` 既有的官方
> 題庫；題 11-15 是使用者要求「多找幾題、湊到15題」後，由 Claude 親自
> 打開 `workspace/` 對應文件的 `original.md` 核對過正確答案才新增的問題
> （與 [`docs/報告/14_延伸測試集驗證與語料庫品質審計報告.md`](14_延伸測試集驗證與語料庫品質審計報告.md)
> 題 11-25 是同一批工作的產物，分成兩份報告各湊足 15 題）。

## 0. 背景與範圍

因應 2026-08-19 專案說明會議，需求為：

1. 估算 § 3.1.4 事實節點（Fact node）對現有勞動法 demo 語料回填所需時間。
2. 評估目前查詢機制（BFS 圖遍歷 + 3.2 §c 關係連結 + 3.1.4 §a 語意 Fact
   檢索）是否足以應付明日演示。
3. 提供一個可透過終端機真實輸入問題、取得真實 Agent 回答的工具。
4. 針對 `docs/報告/12_知識圖譜抽取與問答Demo操作手冊.md` 既有的 10 題題庫，
   實際執行一次，產出真實測試報告。

本報告涵蓋第 3、4 項的完整交付；第 1、2 項的結論摘要整理於下方 §1，供
與 §3、§3.1 的實測結果對照。

## 1. § 3.1.4 回填時間估算 + 查詢機制現況（摘要）

- **回填不可行（demo 前）**：現有 125 份文件、1874 個 chunk 皆是在
  `source_doc_id` 從未被賦值（3.1.4 §c 修復前）的狀態下抽取的。§b 的
  `backfill_fact_nodes()` 依賴既有 `citations_json` 裡的
  `source_svo_chunk_file` 回填 `source_doc_id`，但該欄位只在單一文件夾內
  唯一，跨 125 個資料夾會撞名，無法安全回推真正的來源文件。**唯一乾淨的
  做法是全量重新抽取，估計 24-30 小時**，明日之前不可行。折衷方案（3-5
  份短文件重新抽取，約 1-3 小時）尚待確認是否要做。
- **查詢機制現況**：BFS 圖遍歷（`bfs_query`）與 3.2 §c 查詢時關係連結
  （`resolve_query_relation_type` + `_filter_triples_by_relation_type`）
  皆已上線且運作正常；語意 Fact 檢索（`vector_search_facts`）程式碼正常，
  但因上述回填限制，目前兩個 demo KG 的 `Fact` 節點數皆為 0，**語意檢索
  在明日演示中必然回傳空結果，不是 bug，是已知限制**。
- 本節實測（§3、§3.1）進一步發現：即使 BFS 本身能運作，**種子實體字面
  比對（`_find_seed_entities`，已知的「暫時方案」）在多個真實問題上導致
  嚴重雜訊**，詳見 §5。

## 2. CLI 工具

新增 `ask_question.py`（repo 根目錄），可直接在終端機輸入真實問題、取得
真實 Agent 回答，不需另外啟動 uvicorn：

```bash
python ask_question.py --list-kg
python ask_question.py --kg-id <uuid> "問題文字"
python ask_question.py --kg-id <uuid> --hops 1 "問題文字"   # 可調 BFS 跳數
```

同時擴充了 `/agent/chat` 的 SSE 串流，新增 `event: sources` 事件（見
`routers/agent.py::_serialize_sources()`），把本次實際檢索到的 BFS 三元組
與語意 Fact 結構化輸出，供人工核對「答案到底有沒有圖譜依據」——這也是
明日演示「顯示來源供評審審核」需求的後端基礎（前端尚未接線，屬後續工作）。

## 3. 十題完整結果

全部使用 `/agent/chat` 預設參數（`svo_hops=2`, `top_k=5`, `use_svo=True`），
與正式 demo 端點行為一致。「符合既有題庫預期」欄位是與
`docs/報告/12_知識圖譜抽取與問答Demo操作手冊.md` 既有記錄（2026-07-28／
08-07 驗證）的比對結果。

| # | 問題 | 耗時 | BFS 三元組 | 核心結論 | 符合既有題庫預期？ |
| --- | --- | --- | --- | --- | --- |
| 1 | 二人制飛航組員時間上限 | 47.8s | **0** | 誠實拒答，退回一般知識 | ❌ **迴歸**——題庫記錄為✅精準命中（14hr/10hr），本次種子比對完全落空 |
| 2 | 住院醫師排班前提 | 54.0s | 75 | 大致合理但摻雜大量無關雜訊三元組 | 🟡 大致相符 |
| 3 | 保全業 84-1 條工時 | 47.3s | 69 | 退回一般 12hr 規定，未抓到精確數字 | ✅ 符合（題庫本就記錄為🟡部分命中） |
| 4 | 台鐵乘務輪班間隔 | 43.5s | 265 | 正確答出「連續八小時」 | ✅ 符合 |
| 5 | 生理假天數 | 42.3s | 675 | **答錯：「一年最多16天（4天/次×4次）」** | ❌ **嚴重不符**——正確答案為一年3天，此為確認過的數字幻覺 |
| 6 | 勞基法36條4項調整例假 | 28.2s | 67 | 正確答出客運貨運/旅行業、最長連續工作12天 | ✅ 完全符合 |
| 7 | 颱風天工資 | 72.4s | 3169 | 核心結論正確（得不給付），但夾帶未經圖譜驗證的分級细节 | 🟡 核心正確，細節需查證 |
| 8 | 試用期工資 | 42.6s | 2588 | 圖譜未命中相關事實，靠一般知識答對結論 | 🟡 結論正確但非圖譜依據 |
| 9 | 全勤獎金產假扣除 | 46.3s | 1736 | 圖譜完全未撈到相關事實，靠一般知識推理出大致正確結論 | 🟡 比題庫記錄的「雜訊中撈到相關邊」更差——這次連相關邊都沒撈到 |
| 10 | 拒戴安全帽後果 | 64.4s | 1466 | 核心正確（可能解僱、情節重大），但夾帶大量未經圖譜驗證的推測 | ✅ 核心正確 |

**題1-10 小計**：10 題全部成功執行（無崩潰、無逾時失敗）；語意 Fact 檢索
10 題皆為 0 筆（如 §1 所述，預期行為）；`resolved_rel_type` 只有題 2
命中（`HAS_PREREQUISITE`），其餘 9 題皆為 `QNOMATCH`。

## 3.0 RAG 對照組（同一 10 題，真實執行）

依使用者要求，對同一 10 題另外跑一次 RAG 路徑做真實對照——**誠實標註**：
這不是 `docs/報告/08_三軌混合檢索架構與標準化RAG設計報告.md` 完整定義的
「軌道2：標準化RAG」（該設計要求對**指代消解後的標準化句子**算向量），
而是權衡時間成本後，直接沿用既有 `sentence_embeddings.json`（**指代消解
前的原始句子**向量，已查證非本次臆測）做單句級 cosine 檢索＋
`svo_index.json` 上下文擴展的折衷版本，嚴格說更接近「軌道1：傳統RAG」
加一點上下文擴展。搜尋範圍涵蓋整個 KG 全部 125 份文件（982／2541 筆
句子向量），不是只在已知正確的文件裡找，與知識圖譜路徑的搜尋範圍公平
對照。新增 `build_standardized_rag_index.py`／`standardized_rag.py`／
`run_rag_comparison.py`，原始輸出見 `run_rag_comparison_raw.txt`
（worktree 根目錄，未加入版控）。

| # | 問題 | RAG 耗時 | RAG 核心結論 | 對照 KG 結果 | 誰的答案較好 |
|---|---|---|---|---|---|
| 1 | 飛航組員時間上限 | 19.9s | 誠實拒答：檢索到的段落與問題無關 | 誠實拒答（BFS=0） | 平手（雙方皆未命中正確文件） |
| 2 | 住院醫師排班前提 | 16.3s | 誠實拒答：完全沒檢索到相關文件 | 🟡 大致相符（BFS 撈到部分相關但混雜雜訊） | KG 略勝 |
| 3 | 保全業84-1條工時 | 14.5s | ✅ **精準命中**：10hr/240hr/48hr/288hr 全部正確 | 🟡 只答出籠統12hr，未抓到精確數字 | **RAG 明顯勝出** |
| 4 | 台鐵乘務輪班間隔 | 19.5s | ❌ 答錯：被另一份文件的通用11hr規則混淆，答成11小時 | ✅ 正確答出連續八小時 | KG 勝出 |
| 5 | 生理假天數 | 12.5s | 誠實拒答：完全沒檢索到相關文件 | ❌ 嚴重幻覺：答16天（正確3天） | **RAG 勝出**（誠實拒答優於幻覺） |
| 6 | 勞基法36條4項調整例假 | 24.4s | ✅ 正確答出客運貨運/旅行業、最長連續工作12天 | ✅ 正確答出同樣內容 | 平手（皆正確） |
| 7 | 颱風天工資 | 18.6s | ✅ 精準命中且簡潔，無編造細節 | 🟡 核心正確但夾帶未驗證的分級細節 | **RAG 略勝**（更精簡、無編造） |
| 8 | 試用期工資 | 20.1s | 檢索到正確文件但該段落本身未含答案，誠實拒答 | 🟡 結論正確但非圖譜依據（靠一般知識猜對） | 各有問題，難分高下 |
| 9 | 全勤獎金產假扣除 | 23.3s | ✅ **精準命中**：找到KG完全沒撈到的正確文件，答案完整正確 | 🟡 完全沒撈到相關事實，靠一般知識答對結論 | **RAG 明顯勝出** |
| 10 | 拒戴安全帽後果 | 28.3s | 答得籠統，未命中「情節重大得予免職」的具體條文 | ✅ 核心正確（可能解僱、情節重大） | KG 勝出 |

**總計**：10 題中 RAG 明顯勝出 2 題（3、9）、略勝 1 題（7）、KG 勝出 2 題
（4、10）、平手 2 題（1、6）、各有問題 2 題（2、8）。**最值得注意的發現**：
即使沒有做指代消解標準化、只是最陽春的句子級向量檢索，RAG 在題 3、9 都
找到了 KG 的字面比對種子（`_find_seed_entities`）完全找不到的正確文件——
向量檢索對「措辭不同但語意相關」的召回能力，補足了字面比對的已知弱點；
但 RAG 也有自己的失敗模式（題4的跨文件混淆、題8/10的正確文件命中卻抓錯
段落），不是單方面優於知識圖譜路徑，是兩種不同、互補的檢索方式。

## 3.1 補充題 11-15（湊足15題，已核對原文正確答案）

| # | 問題 | 耗時 | BFS 三元組 | 已核對正確答案 | 系統實際回答摘要 | 判定 |
| --- | --- | --- | --- | --- | --- | --- |
| 11 | 出差往返交通時間算不算工作時間 | 89.5s | 437 | 非雇主監督下不計入；適逢正常工時工資照給；工時外雇主指派駕駛載運則計入 | 「通常不算，除非停留工作」——方向大致對但漏掉後兩個條件細節 | 🟡 部分正確，非圖譜依據 |
| 12 | 計算平均工資不計入的期間 | 64.8s | 434 | 7款明確列舉（計算事由當日/職災醫療中/50條2項減半/天災不可抗力/普通傷病假/性平法各假別/留職停薪） | 自行推測「病假事假/年終獎金/技術生不計入」，與真正的7款清單不符 | ❌ 錯誤，非圖譜依據 |
| 13 | 適用四週變形工時的行業 | 49.7s | 60 | 住宿餐飲業/保全業/醫療保健服務業/社會福利服務業/百貨公司業等 | 檢索到的是另一份文件（勞基法34條但書）的內容，未命中正確文件，只能答「需查閱公告」 | ❌ 檢索命中錯誤文件，答案空泛 |
| 14 | 工資可否不經同意改實物/禮券折抵 | 56.8s | 3350 | 不可以，需經勞工同意 | 結論正確（不可以），但引用的法條（勞基法第22條扣薪爭議）與本題實際依據不符 | 🟡 結論正確，引用法條不精確 |
| 15 | 勞保生育給付條件與標準 | 59.5s | 2810 | 條件：滿280/181/84日；標準：分娩費30日+生育補助費60日，按平均月投保薪資 | **嚴重幻覺**：編造「基本工資75%，上限30,960元、下限4,800元」「產前假14天、產後假70天」，三個數字全部虛構 | ❌ **確認過的數字幻覺（單題編造3個假數字）** |

**題11-15 小計**：0 題完全正確、2 題結論方向對但細節/法條有誤、3 題錯誤，
其中題15是本次測試中**單題編造數字最多**的一次（同時虛構比例、上下限
金額、天數共4個具體數字）。

## 4. 完整回答與三元組樣本

完整逐題原始輸出（含每題前 15 筆三元組樣本、完整回答全文）見
`demo_report_raw.txt`（題1-10）與 `demo_report_batch3_raw.txt`（題11-15，
與 [14號報告](14_補充題庫真實查詢驗證報告_2026-08-18.md) 題25共用同一次
執行）。兩者皆本次執行產生，未加入版控。如需重新產生：

```bash
python run_demo_report.py > demo_report_raw.txt 2>&1        # 題1-10
python run_demo_report_batch3.py > demo_report_batch3_raw.txt 2>&1  # 題11-15 + 14號報告題25
```

## 5. 關鍵發現（依嚴重性排序）

### 🔴 發現 1：題 5、題 15 出現確認過的數字幻覺，且不在既有「誠實拒答」設計的保護範圍內

`_build_prompt()` 目前只有在**完全沒有撈到任何事實**（`fact_lines` 為空）
時，才會附加「沒有依據就不要臆測具體數值」的指示。題 5 的 BFS 撈到了
675 筆三元組（其中確實包含正確的「生理假不併入病假」這條邊），所以走的
是「有事實」分支——但該分支的 prompt 完全沒有數字幻覺防護指示。結果
LLM 在**看起來有圖譜依據**的情況下，自行編造「每次不得超過4日，一年
最多16天（4天/次×4次）」，正確答案其實是一年3天。題 15（勞保生育給付）
更嚴重：BFS 撈到 2810 筆三元組全部無關，LLM 在同一個回答裡連續編造了
4 個具體數字（給付比例75%、上限30,960元、下限4,800元、產前假14天、
產後假70天），全部與原文（按平均月投保薪資給付分娩費30日+生育補助費
60日）無關。這比「完全沒依據、誠實拒答」更危險，因為表面上像是有憑
有據。

**建議**：明日演示避免使用題 5、題 15，或使用前先確認是否要修這個
prompt 分支（把數字幻覺防護指示也加進「有事實」分支）——但這是程式碼
變更，依專案慣例應先討論後實作，本報告只如實記錄現況，未擅自修改。

### 🔴 發現 2：BFS 種子字面比對雜訊規模比先前記錄的更嚴重，且不穩定

10 題中有 6 題撈到 500 筆以上三元組（題 7 甚至 3169 筆），比題庫文件
（`docs/報告/12_...md` 的題 9 案例）記錄的「50 條邊」規模大上一到兩個
數量級。根本原因（已用真實查詢驗證，非臆測）：`_find_seed_entities()`
的字面比對沒有排除高頻通用詞——例如「工資」「雇主」「主管機關」這類
詞在 125 份勞動法文件中幾乎每份都出現，一旦被比對為種子，2-hop BFS
會直接炸開到數千筆不相關三元組。題 1（飛航組員）用真實查詢驗證與
`_find_seed_entities()` 直接呼叫的結果一致：只撈到 3 個種子（工資／
雇主／有——其中「有」是單字雜訊實體），且 2-hop BFS 撈到 0 筆——與題庫
記錄的「✅精準命中」相反，代表**這題目前無法重現先前驗證過的結果**，
明日演示前應重新確認是否要抽換題目。

實測 hops=1 可將題 5 的雜訊從 3036 筆壓到 486 筆（見對話記錄），但仍然
偏大，且本報告的正式 10 題結果全部採 hops=2（與正式 demo 端點行為一致），
未套用此緩解，如實呈現目前預設設定下的真實狀況。

### 🟡 發現 3：多題答案雖結論正確，但並非真正由圖譜事實支撐

題 8、9 是最明顯的案例：BFS 分別撈到 2588、1736 筆三元組，全部與問題
無關（都是「工資」「直轄市政府」等高頻詞牽出的雜訊），LLM 最終能答對
結論純粹是靠自己的訓練知識，而非圖譜依據——這與 demo 想傳達的「有依據
時清楚引用」的敘事有落差，演示這兩題時應誠實說明。

### 🟡 發現 4：LLM 輸出頻繁夾雜簡體字

`qwen2.5:7b` 在多題回答中間夾雜簡體字（如「根据」「这」「结论」「关键」），
儘管 prompt 已明確要求台灣地區用語。這是本地小模型指令遵從度的已知限制
（`docs/報告/12_...md` 已記錄類似現象），非本次新發現，但實測樣本更完整，
建議明日演示前抽選幾題確認呈現效果是否可接受。

### ⚪ 發現 5：關係連結（3.2 §c）在真實問題上的命中率偏低

15 題中只有 1 題（住院醫師排班）成功解析出 `resolved_rel_type`，其餘皆為
`QNOMATCH`（優雅降級，未篩選）。這與程式碼本身文件記錄的已知限制一致
（把整個問題字串而非抽取出的動詞片語餵給比對函式），非新問題，僅供佐證
「這個限制在真實資料上確實有感」。

### 🟡 發現 6：即使來源文件確認存在且內容正確，檢索仍可能完全命中錯誤文件

題 13 是明確案例：正確答案所在的文件（`勞動基準法第三十條第二項第三項
及第三十條之一指定行業公告`）已由 Claude 親自讀取確認內容正確、且該
文件確實在 KG 內，但實際查詢時 BFS 撈到的三元組全部來自另一份不相關
文件（勞基法34條但書公告），導致系統完全沒有機會看到正確答案，只能
給出「需查閱公告」的空泛回應。這比「有正確事實但被雜訊稀釋」（見
14號報告發現4）更前一步——是連撈都沒撈到對的文件。

## 6. 給明日演示的具體建議

1. **避免現場示範題 5、題 15**（生理假天數／勞保生育給付）——已確認會
   產生錯誤數字，且看起來像有圖譜依據，風險最高。
2. **題 1、13 需要重新確認或抽換**——種子比對／檢索完全落空或命中錯誤
   文件，無法重現正確結果。
3. 題 7、8、9、10、11、14 可以示範，但建議比照題庫文件 #9 的既有做法，
   主動說明「結論正確、但圖譜貢獻有限／夾雜未驗證細節」，把這當成誠實
   侷限的教材，而不是隱藏起來。
4. 題 3、4、6 是目前最穩、最適合首選示範的題目——三元組筆數合理
   （69/265/67），答案內容與題庫記錄一致。
5. 語意 Fact 檢索（3.1.4 §a）明日必然是 0 筆，如果被問起，可如實說明是
   「回填尚未執行，非程式錯誤」（§1 已有完整根因說明可用）。

## 7. 如何手動取得完整逐字回答

本報告的表格是摘要與判定，不是逐字全文——**如需查看任何一題真正完整、
未經摘要的原始回答，直接重新執行即可**，以下指令會走與正式 demo 相同
的程式碼路徑，對真實 Neo4j／Ollama 產生真實輸出（需要 `kg2-neo4j`
Docker 容器與本地 Ollama 皆已啟動）：

**單題手動輸入（互動查詢）**：
```bash
python ask_question.py --list-kg   # 列出可用的 KG id
python ask_question.py --kg-id <kg_id> "問題文字"
```
兩個 demo KG 的 id：`deb24e7c-c012-4398-9261-c813cb60b197`（排班與工時
法規）／`2ae9d28b-3c5e-424f-9ea4-c3cbe59a2934`（請假與薪資法規）。

**整批重跑本報告題目**（會印出每題完整回答＋來源三元組，非摘要）：
```bash
python run_demo_report.py          # 題1-10
python run_demo_report_batch3.py   # 題11-15（與14號報告題25共用同一次執行）
```
輸出直接印在終端機，可另外用 `> 檔名.txt 2>&1` 導出成檔案保存。

**§ 3.0 RAG 對照組**（同 10 題的 RAG 路徑完整回答）：
```bash
python build_standardized_rag_index.py   # 先建立索引（純讀取既有向量，數秒完成）
python run_rag_comparison.py             # 印出每題 RAG 完整回答＋檢索到的段落
```
