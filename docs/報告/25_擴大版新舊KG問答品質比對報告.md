# 25．擴大版新舊KG問答品質比對報告

**日期**：2026-09-02
**背景**：[22_乾淨重跑新舊KG問答品質比對報告](22_乾淨重跑新舊KG問答品質比對報告.md) 只用了新舊KG皆完成的 5 題（且集中在 D0080015、N0030006 第7條、N0090051、F0040034 四份文件），樣本偏小、主題偏窄。[24_事實清單自然語言化機制設計報告](24_事實清單自然語言化機制設計報告.md) 完成後，新KG `c15949bf-cf96-46ad-90a2-a8579d7b20b4`（下稱「新KG」）已有 **18 份小中型文件整份跑完**、`natural_text` 全KG回填（覆蓋率約 95%）。本報告用**先前報告 18–24 從未測過的 8 份文件、8 道全新題目**做擴大版三方比對。

本報告要回答：**主題與文件範圍擴大之後，「乾淨重跑＋全部修正＋事實清單自然語言化」的新KG，問答品質是否仍穩定優於舊KG？相對於純LLM的價值主張是否成立？**

## 1. 測試設計

延續報告 18／22 的方法論，三個條件：

- **[A]** KG Agent（`use_svo=True`）對新KG `c15949bf`（乾淨重跑後全部修正＋自然語言化回填）
- **[B]** KG Agent（`use_svo=True`）對舊KG `76bc98ff-2cbd-447e-a087-7f2df6898655`（凍結快照，修正陸續上線期間累積到 49% 時凍結，**未經自然語言化回填**）
- **[D]** 純LLM基準（`use_svo=False`，完全不檢索）

8 題涵蓋 8 份**報告 18–24 未測過**的小中型文件，全部經 `task_queue.db` 查證在**新舊兩KG皆 100% 抽取完成**（每份 c/f/p 皆為 N/0/0 且兩KG數字一致），比較才對稱。每題答案均可對法規原文**逐字查證對錯**（數量、期間、條件）。

| 題號 | 涉及文件 | 查證要點 |
|---|---|---|
| 1 | N0030011 事業單位僱用女性勞工夜間工作場所必要之安全衛生設施標準（§3） | 安全門/安全梯夜間不得上鎖；通道不得堆置物品 |
| 2 | N0030018 育嬰留職停薪實施辦法（§2） | 每次原則≥6個月；30日以上未達6個月以2次為限；30日以上須10日前提出 |
| 3 | N0030025 勞動基準法第四十五條無礙身心健康認定基準及審查辦法（§4） | 未滿6歲≤2h、6–12歲≤3h、12–15歲≤4h；未滿6個月每次≤30分鐘 |
| 4 | N0060007 高溫作業勞工作息時間標準（§7、§6） | 原有工資不得減少；黑球溫度50度以上須供給熱防護設備 |
| 5 | N0060012 精密作業勞工視機能保護設施標準（§3、§9、§10） | 每日凝視合計2h以上；連續2h給至少15分鐘休息；明視距離約30公分 |
| 6 | N0060065 女性勞工母性健康保護實施辦法（§2、§10、§14） | 保護期間＝得知妊娠之日起至分娩後1年；血中鉛10μg/dl以上為第三級；紀錄至少保存3年 |
| 7 | N0070020 危險性機械及設備檢查費收費標準（§3、§8、§7） | 型式檢查每型式8000元；每增一型加收4000元；證明補發/換發每張800元；休息日加收50% |
| 8 | N0080016 原住民待工期間職前訓練經費補助辦法（§4、§6、§7） | 訓練時數以300小時為度；補助每人每小時100元為限；訓練結束後30日內請款核銷 |

**技術備註**：同報告 22，直接呼叫 `routers.agent.chat()` 並消費其 `StreamingResponse.body_iterator`，不透過 `main.py` 啟動伺服器（避免其 `lifespan` 的全域背景 worker 誤動凍結的舊KG）。抽取 drain 腳本測試期間維持暫停。LLM 為本地 `qwen2.5:7b`。腳本：`run_expanded_kg_comparison.py`。

## 2. 逐題結果

### 題1｜N0030011 夜間工作安全設施：安全門可否上鎖、通道可否堆物

- **[A] 新KG**：「通道在夜間工作時間內不得堆置物品」**答對**；「安全門/安全梯可否上鎖」答「資料未明確記載，無法確認」（原文 §3 有明文「不得上鎖」，屬**漏檢**）。
- **[B] 舊KG**：兩問皆未給有效答案；把「通道不得堆置物品」這條檢索到了，卻在結論自我推翻（「未明確說明通道內其他地方是否允許堆放…無法確認」）。答案含 `（PLACE）` 佔位符洩漏。
- **[D] 純LLM**：以「從常理推測」得到方向大致正確的結論（不宜上鎖、通道應暢通），但明確聲稱「並未在勞動基準法等主要法規中明確規定」（**錯**，N0030011 有專門明文），且全篇不確定語氣。

**判定：[A] 略優於 [B]，[D] 依據錯誤**。A 至少一問答對且無錯誤；B 檢索到卻用不上；D 方向對但否認法規存在。

### 題2｜N0030018 育嬰留職停薪：每次期間、次數限制、提前天數

- **[A] 新KG**：「每次原則≥6個月」**對**、「30日以上未達六個月以二次為限」**對**；「30日以上須提前幾天」答「未找到，無法確認」（原文為 10 日前，屬**漏檢**）。3 問 2 對 1 拒答，**無錯誤**。
- **[B] 舊KG**：「每次≥6個月」**對**；次數規範拒答；「30日以上要**提前五天**提出」——**錯誤**（正解 10 日前；5 日前是「未滿30日」的規定，張冠李戴）。
- **[D] 純LLM**：「原則上至少為六個月」對；「沒有法規明確規定申請次數」**錯**；「應提前**十五日**提出」**錯誤**（憑空數字）。

**判定：[A] 勝**。A 無錯誤數字，B 與 D 各有 1–2 個篤定的錯誤數字。

### 題3｜N0030025 未滿15歲工作者：每日工時三段、未滿6個月每次限制

- **[A] 新KG**：6–12歲≤3h **對**、12–15歲≤4h **對**、未滿6個月每次≤30分鐘 **對**；**漏了「未滿6歲≤2小時」**這一段。3 段講對 2 段＋特別限制，無錯誤。
- **[B] 舊KG**：同樣講對 12–15歲、6–12歲、未滿6個月三項，但措辭自相矛盾（一邊列出 6 歲以上未滿 12 歲 3 小時、一邊說「沒有提到年滿六個月至未滿十二歲」），同樣漏「未滿6歲2小時」。
- **[D] 純LLM**：**全部年齡段幻覺**——「未滿六個月屬未出生胎兒不適用」「未滿七歲2小時」「未滿十四歲4小時」「未滿十五歲6小時」，並稱「未針對每次工作時長設額外限制」（正解有 30 分鐘限制）。

**判定：[A] 略優於 [B]（同樣不完整，A 措辭較乾淨），[D] 嚴重幻覺**。

### 題4｜N0060007 高溫作業：工資可否減少、黑球溫度門檻

- **[A] 新KG**：黑球溫度「攝氏50度以上」**答對**；但「原有工資可否減少」——**同一句話裡逐字引用了「勞工其原有工資不得因為實施本標準後降低工作時間而減少」，結論卻是「資料未明確記載，無法確認」**。生成端把已檢索到並引用的事實自我否定。答案另含錯字（「拉工」「違據」）與 `（ PLACE ）` 佔位符洩漏。
- **[B] 舊KG**：檢索到 211 條三元組（全題最多），卻兩問皆拒答；三元組嚴重破碎（「原有工資不得減少減少之工作時間」疊字、把「事實清單」講成「電話」、`（PERSON）（PRODUCT）` 佔位符），「黑球溫度的具體數值」直接遺失（未自然語言化，「五十度」沒進到可用文字）。
- **[D] 純LLM**：「工資一般不可以隨之減少」方向對；「須接近攝氏**60度**以上」——**錯誤**（正解 50 度）。

**判定：[A] 勝**。A 至少答對黑球溫度；B 檢索量大卻零產出，是新舊KG差距最明顯的一題（自然語言化讓數字得以保留）；D 數字幻覺。

### 題5｜N0060012 精密作業：凝視時數、休息、明視距離

- **[A] 新KG**：「每日凝視合計2小時以上」**對**、「連續2小時給至少15分鐘休息」**對**；「明視距離約幾公分」拒答（正解約 30 公分，屬**漏檢**）。
- **[B] 舊KG**：與 [A] **完全同級**——同樣答對前兩項、明視距離拒答。
- **[D] 純LLM**：「沒有明確規定每日凝視時數」**錯**；引用「勞動基準法**第26條**」休息制度（**幻覺**，且本題法源非勞基法）；「通常建議30至45公分」沾到邊但非法定、範圍錯。

**判定：[A] ≈ [B] 平手，[D] 幻覺**。此題自然語言化未帶來差異（兩項答對的事實在舊KG的破碎三元組裡也還讀得懂）。

### 題6｜N0060065 母性健康保護：保護期間、血鉛第三級、紀錄保存年限

- **[A] 新KG**：「得知妊娠之日起至分娩後一年」**對**、「紀錄至少保存三年」**對**；血中鉛第三級數字拒答（正解 10μg/dl，屬**漏檢**；另有錯字「鈾作業」）。3 問 2 對 1 拒答。
- **[B] 舊KG**：「保護期間」**對**；血鉛拒答；**「紀錄保存期限，資料未明確記載，無法確認」——這一項新KG答對、舊KG答不出**。3 問 1 對 2 拒答。
- **[D] 純LLM**：「預產期前38周至產後6週」**錯**、「血中鉛50微克/公升以上為第三級」**錯**、「至少保存5年」**錯**——**三個數字全部篤定幻覺**。

**判定：[A] 優於 [B]（多答對「保存3年」一項），[D] 全錯**。

### 題7｜N0070020 檢查費：型式檢查費、每增一型加收、證明補換發、休息日加收

- **[A] 新KG**：型式檢查每型式**8000元**對、證明補發/換發每張**800元**對、休息日**加收50%**對；「每增加一種型式加收」答「**新臺幣八千元**」——**錯誤**（正解 4000 元）。**追查確認這是抽取階段汙染**：新KG 內該條 `natural_text` 就是「…每增加一種型式，加收新臺幣八千元」，全KG 搜尋「四千」零命中——SVO 抽取把 §3「加收四千元」誤記成「八千元」（§3 第1項基礎費 8000 元的數字被錯接到第2項子句；數值忠實性核對因「八千」確實逐字出現在同一 chunk 而未攔下）。4 問 3 對 1 錯。
- **[B] 舊KG**：型式檢查費答「**新臺幣二千元**」**錯誤**（正解 8000；2000 是熔接程序規範那項，張冠李戴）；每增一型加收「四千元」**對**（此項舊KG反而對）；證明 800 元對；休息日「加收**百分之一百五十**…無法確認」**錯誤**（把「加收50%」讀成「150%」再拒答）。4 問 2 對 2 錯。
- **[D] 純LLM**：全部「沒有明確列出／建議查詢主管機關」——**誠實全拒答**，0 對 0 錯。

**判定：[A] 勝**（3/4 對；唯一錯誤來自抽取階段而非生成端）。B 有 2 個實質錯誤。D 誠實但零資訊。

### 題8｜N0080016 原住民職前訓練補助：訓練時數、每小時補助上限、核銷期限

- **[A] 新KG**：三項**全部答「資料未明確記載，無法確認」**。**但追查確認三項事實都在新KG內**：`訓練時數（概念）以三百小時為度`、`补助经费额度（概念）以每人每小时新台币一百元为限` 皆為現存關係（N0080016 在新KG共 35 條關係）。這是**純粹的 BFS 漏檢**——事實在庫、抽取正確、自然語言化正常，但 BFS 從本題種子實體出發 1–2 跳內沒接到這些節點（`訓練時數` 節點只透過 `RELATED_TO` 連到一個懸空概念，連通度極低）。至少沒有幻覺。
- **[B] 舊KG**：三項同樣全部拒答。
- **[D] 純LLM**：訓練時數「無特定規定」、補助額度「無固定標準」；「應在訓練結束後**三十日內**完成請款及經費核銷」——**猜對 1 項**。

**判定：[A] ≈ [B]（皆檢索失敗），[D] 靠猜命中一項略勝**。此題是全篇對新KG最不利的結果，暴露 BFS 種子選擇／圖連通度是目前的真正瓶頸。

## 3. 總結

### 3.1 新KG vs 舊KG：8 題無一題舊KG勝出

| 題 | [A]新KG | [B]舊KG | A vs B |
|---|---|---|---|
| 1 | 通道對／上鎖漏檢 | 措辭混亂自我否定＋佔位符洩漏 | **A 略勝** |
| 2 | 2對1拒答，無錯誤 | 1對＋「提前5日」張冠李戴 | **A 勝** |
| 3 | 3段對2段＋特別限制 | 同上但措辭矛盾 | **A 略勝** |
| 4 | 黑球溫度對／工資逐字引用卻拒答 | 211條三元組破碎、雙拒答、數值遺失 | **A 勝（差距最大）** |
| 5 | 2對1拒答 | 與A完全同級 | 平 |
| 6 | 期間＋保存3年 對 | 僅期間對，保存年限也拒答 | **A 勝** |
| 7 | 3/4對（1錯來自抽取汙染） | 基礎費8千→2千、50%→150% 兩錯 | **A 勝** |
| 8 | 3項全漏檢（事實在庫） | 3項全漏檢 | 平（皆失敗） |

**新KG勝 5、平 3、負 0**。自然語言化回填的效益主要體現在「舊KG把數量丟進破碎三元組後答不出來、新KG答得出來」（題4 黑球溫度、題6 保存年限、題7 基礎費）。題5 顯示當事實在舊KG的三元組裡「還讀得懂」時，自然語言化不帶來額外差異——它是**下限保障**，不是全面提升。

### 3.2 KG系統 vs 純LLM：價值主張成立——以「不知道」取代「篤定的錯」

| | 實質正確項 | **篤定幻覺項** | 誠實拒答 |
|---|---|---|---|
| [A] 新KG | 題1–7 共約 14 項數量/條件答對 | **1**（題7「加收八千」，且源自抽取階段） | 題8 全部＋各題零星漏檢項 |
| [D] 純LLM | 約 3 項（題4方向、題8核銷期限猜中、題1方向） | **≥8**（題2「15日」、題3全年齡段、題4「60度」、題5「26條」、題6「38週/50μg/5年」） | 題7 全部 |

純LLM在題2/3/4/5/6 **全部產生講得斬釘截鐵的錯誤數字**。新KG 8 題只有 1 個實質錯誤數字，且追查證實是 SVO 抽取把「四千」記成「八千」、非生成端問題。**KG系統的核心價值——在沒有把握時輸出「資料未明確記載」而非自信幻覺——在擴大題組下依然穩固成立**。KG勝 6、平 1、純LLM勝 1（題8）。

### 3.3 效能

| 條件 | 平均耗時 | 平均 BFS 三元組數 |
|---|---|---|
| [A] 新KG | 247s | 46 |
| [B] 舊KG | 356s | 66 |
| [D] 純LLM | 29s | — |

舊KG較慢主因 BFS 三元組量大（題4 211條、題5 123條）卻命中不準；新KG因 `natural_text` 較精簡、檢索較聚焦（題7 僅 5 條即足夠且 0 句未接地）。本地 `qwen2.5:7b` 單次 KG 回答 4–8 分鐘，仍是端到端體驗的主要瓶頸。

## 4. 本輪新發現（待後續處理，非本報告修正範圍）

1. **BFS 漏檢是目前最大瓶頸（題8）**：事實已正確抽取並自然語言化、就在圖裡，BFS 卻接不到。低連通度節點（`X -[RELATED_TO]-> 懸空概念`，內容全塞在 `natural_text`）尤其容易被漏。這比生成端問題更值得優先處理。
2. **生成端過度保守（題4[A]）**：已檢索到、已逐字引用的事實仍被結論判成「資料未明確記載」。與 [18](18_三管線檢索策略與抽取方法比較報告.md) grounding-regen 過度修正、[22](22_乾淨重跑新舊KG問答品質比對報告.md) 題3 同源。**⚠️ 2026-09-02 修正定性**：針對 Q8 做的 3 輪生成鏈診斷（`diagnose_finding2_q8.py`）發現，發現1+5 修好檢索後、Q8 端到端的失效**其實不是生成端過度保守**——三筆答案事實根本沒進 prompt（見發現6），草稿說「未提及」是對的。發現2 仍是真實存在的獨立問題（題4[A] 的工資案例是乾淨的發現2：事實在 18 行清單裡卻被否定），但它**不是 Q8 的阻塞點**。優先序因此調整（見 §6 ⑤）。
3. **抽取數值錯接（題7）**：「加收四千元」被抽成「加收八千元」。數值忠實性核對（[20](20_抽取數值忠實性核對機制設計報告.md)）只驗「數字是否逐字出現在 chunk」，攔不下「數字出現了但接錯子句」。需要子句層級的對位檢查。
4. **簡繁轉換洩漏（題8）**：`qwen2.5:7b` 把部分輸出悄悄轉成簡體（`补助经费额度`、`训练`、`经费`）。來源原文為繁體。**→ 已修（§6 發現4）**：`natural_text` 由發現5 回填清乾淨；抽取端 subject／verb／object 加即時選擇性轉繁（`_to_traditional_selective()`，來源語料當白名單、保留「雇」「托」）＋ Entity 名稱／`fact_text` 回填（c15949bf：改名 503、簡繁撞名 merge 43）。
5. **佔位符洩漏仍在（題1[B]、題4[A][B]）**：`（PLACE）`、`（PERSON）`、`（PRODUCT）`、`（概念）` 進到最終答案。報告24 階段4 對「殘缺三元組跳過 LLM 改寫」的決策，代價是這些被跳過的關係保留了 `X（概念） … （概念）` 模板標記，直接洩漏。新KG（自然語言化後）比舊KG少很多，但未歸零。
6. **事實清單組裝的兩個缺陷（2026-09-02 生成鏈診斷逐步定位，題8）**：發現1+5 修好檢索後、Q8 端到端仍答「資料未明確記載」，診斷發現三筆答案事實根本沒進 prompt——**不是發現2**（草稿說「未提及」對它拿到的清單來說是對的）。逐步定位出兩個獨立缺陷：
   - **(6a)〔Q8 實際阻塞點〕殘缺過濾過度**：`_split_fact_lines()`（沿用 `_merge_fact_lines` 2026-08-27 的「殘缺三元組過濾」）直接看 `object` 欄位是否為空字串——但 Q8 三筆答案事實是 `X -[RELATED_TO]-> (懸空概念)` 主謂結構、payload 在 verb、object 為 `""`（`vector_search_facts` top-20 裡 #4／#8／#13），被這個過濾器一路靜默丟掉。修正：改看渲染後的文字（`_is_contentful_line()`），只丟「空字串」或「只有 subject」的行。
   - **(6b)〔通用問題〕排列丟棄檢索排名**：`_arrange_fact_lines()` 整批用 `_score_lines_by_embedding()` **重新排序**、截斷到 `_FACT_LINE_TRUNCATE_K=18`，丟棄 `vector_search_facts()` 已算好的問題相關性 KNN 排名，讓短答案事實被 N0080016 第5條 8 行「訓練計畫書應包括…」樣板擠掉。根因：語意 Fact 經過真正的問題相關性檢索，BFS 三元組無任何問題相關性排序（`bfs_query()` 無 `LIMIT`／無評分）。
   - **已實作修正**（⑤，見 §6）：(6a) 殘缺過濾改看渲染文字；(6b) BFS 鄰居剪枝（SAGE）＋語意 Fact 優先佔位＋RRF 融合排序（Cormack et al. 2009）。文獻見 [`docs/參考文獻/21_圖遍歷與向量檢索結果融合/`](../參考文獻/21_圖遍歷與向量檢索結果融合/README.md)。
7. **`qwen2.5:7b` 生成品質雜訊**：錯字（拉工／違據／鈾作業／明显）、疊字（「不得減少減少」）、簡繁混用、把「事實清單」講成「電話」。非本次範圍但影響可讀性與可信度觀感。

## 5. 誠實侷限

- 8 題、每條件 1 次，未做多次重跑；[20](20_抽取數值忠實性核對機制設計報告.md) 已確認 `qwen2.5:7b` 存在批次相依的非決定性，個別題的判定可能在重跑下浮動 ±1。整體趨勢（新KG ≥ 舊KG、KG ≫ 純LLM 於數量題）樣本已足夠明確。
- 「判定」為人工對照法規原文，非盲評；評分者知道哪個條件是哪個。
- 舊KG 只完成 49% 就凍結，這 8 份小文件剛好都在已完成範圍內；若換成新KG獨有的較大文件，無法做 A/B。
- 本報告只比問答輸出，未逐條稽核兩KG的三元組品質差異（那是 [21](21_抽取管線稽核與修正報告.md) 的範圍）。

## 6. 待辦

> **2026-09-02 進度**：文獻與參考專案盤整完成——發現1／2 用既有 `docs/參考文獻/19`（Jin et al. 2025）即可，發現3 是唯一真文獻缺口（候選：Huang et al. 2026 entailment-aware triple extraction、GenRES），發現2 找到錨點 RefusalBench（Muhamed et al. 2025, arXiv:2510.10390）。追查後發現1 根因重新定性為「檢索 K 太小＋脆弱前置閘＋佔位符污染」，非低連通度節點問題。實作順序定案：①發現1 → ②發現5+4 → ③發現3 → ④發現2。

- [~] **① 發現1（2026-09-02）**：分兩次修正。
  - commit `1215f8b`：`ChatRequest.top_k` 預設 5→20、`le` 10→50；`_filter_triples_by_source_doc_ids()` 加歸零守衛。
  - Q8 條件A 真實重測發現只做上述會引入新問題——`top_k=20` 讓 20 筆語意 Fact 裡混入 13 筆跨文件事實，`_relevant_doc_ids_from_facts()` 範圍被稀釋，LLM 抓跨文件的「訓練時數不得低於八十小時」答成錯誤數字（比原本「資料未明確記載」更糟）。
  - 收緊 commit（本 commit）：`_relevant_doc_ids_from_facts(top_n=5)` 只用最高分前 5 筆推導範圍；新增 `_filter_facts_by_source_doc_ids()` 讓語意 Fact 清單本身也套文件範圍；共用邏輯抽到 `_scope_by_source_doc_ids()`。618 項測試通過。
  - **定位**：發現1 修正解決「檢索召回＋跨文件雜訊」，**Q8 端到端仍未修復**——「以三百小時為度」因 `（概念）` 佔位符污染 embedding 仍排不進 top-20（待②發現5），已檢索到的核銷/金額兩筆仍被生成端判「資料未明確記載」（待④發現2）。發現 1／2／5 綁死，端到端驗證待②疊上後再測。
- [x] **② 發現5＋4（2026-09-02）**：
  - `_verbalize_fact()` 移除 `（型別）` 括號（不改資料的 bge-m3 cosine 對照實驗：去標記後正確事實對問題 cosine +0.03～+0.10、跨文件雜訊 +0.006）；`_naturalize_triple()` 輸出加 OpenCC `s2tw`；`_merge_fact_lines()` fallback 改直接串接＋`_strip_type_markers()` 防禦性清除；新增 `backfill_fact_text_embeddings()`／`backfill_natural_text_traditionalize()` 兩個回填批次任務；`opencc-python-reimplemented` 加進 requirements。625 項測試通過。
  - **c15949bf 回填**：4934/4990 個 Fact 節點重寫 `fact_text`+`fact_embedding`（93 分鐘）、1131 條邊 `natural_text` 轉繁。
  - **Q8 重測（發現1+5 疊起來）**：三項答案事實全部進 top-20 且乾淨——「訓練時數 以三百小時為度」從排 >20 進到 #13、「一百元為限」#11→#8 且轉繁、「三十日內核銷」穩定 #4。**檢索面完全修復。**
  - **但端到端仍失敗**：`語意Fact=13`（三項事實都在 `vector_search_facts` 回傳裡），回答三項全「資料未明確記載」。
- [x] **⑤ 發現6（2026-09-02，順序調整——原④發現2 提前，因診斷推翻假設）**：Q8 生成鏈診斷（`diagnose_finding2_q8.py`／`verify_finding6_q8.py`，事實清單完全確定性）發現進 prompt 的清單一筆答案事實都沒有——**不是發現2**。逐步定位出兩個獨立缺陷並修正：
  - **(6a)〔Q8 實際阻塞點〕殘缺過濾過度**：`_split_fact_lines()` 看 `object` 欄位是否為空——Q8 三筆答案事實（`X -[RELATED_TO]-> (懸空概念)`、payload 在 verb、object=`""`）被一路靜默丟掉。修正：新增 `_is_contentful_line()`，改看渲染文字；去重補上渲染文字比對（key 在 object 空時無法比對）。
  - **(6b)〔通用〕`_arrange_fact_lines()` v2**：BFS 鄰居剪枝（`_score_lines_by_embedding` 排序、只留 `_BFS_KEEP_MAX=18`，SAGE）→ 語意 Fact 依 `vector_search_facts` 檢索順序優先佔位（BFS 保底 `_MIN_BFS_SLOTS=4`）→ `_rrf_order()` RRF 融合排序（`_RRF_K=60`，Cormack et al. 2009）→ zigzag。`_split_fact_lines()` 分來源回傳；`_build_prompt`／`_build_constrained_prompt`／`chat()` 呼叫端同步。
  - 文獻（`docs/參考文獻/21_圖遍歷與向量檢索結果融合/`，新增）：RRF（Cormack et al. 2009 SIGIR，全文精讀）、GraphRAG 綜述鄰居爆炸稀釋（Han et al. 2024/2025）、SAGE 展開後剪枝（Titiya et al. 2026）、Calibrated Fusion 問題陳述（Bacellar 2026）。
  - **Q8 端到端重測（發現1+5+6 全疊，2 輪）**：三筆答案事實全部進 prompt（BFS 27 / 語意 12 → 排列 18），**兩輪都三項全對**（訓練時數以三百小時為度、每人每小時一百元為限、訓練結束後三十日內請款核銷），接地 3/3 全支持、無限制性重生成。**Q8 第一次端到端答對，且跨 2 輪穩定**（不像報告 20/22/24 的非決定性）。發現 1（top_k+文件範圍）、發現 5（型別括號+簡繁）、發現 6（殘缺過濾+RRF 排列）三者疊起來把 Q8 從「三項全不知道」修到「三項全對」——單獨任何一個都不夠。
- [~] **⑥ 發現2（2026-09-02，已診斷、設計已定、尚未實作——使用者將接續）**：對題4[A]（N0060007 高溫作業）做 3 輪生成鏈診斷（`C:\Users\666\.claude\jobs\efb89cec\tmp\diagnose_finding2_q4.py`）。
  - **根因（3 輪同一模式）**：發現1+5+6 修好後，`勞工其原有工資不得…減少`（§7）與 `黑球溫度五十度以上…`（§6）兩個關鍵事實**都在 prompt 的 18 行清單裡**，**草稿三輪都兩部分全對、正確引用事實**。失效在「接地核對 → 限制性重生成」迴圈：
    1. 接地核對把**非事實陳述句**判為「未接地」——引言句（`根據提供的事實…`）、model 把問題當 markdown 標題回貼（`**…原本的工資可以跟著減少嗎？**`）。技術上對（沒被事實支持），但它們不是需要被支持的 claim。
    2. `chat()` 觸發條件 `any(not c.supported for c in grounding)`——任一句未接地即重生成，不分「未接地的事實主張」（真問題）與「未接地的非主張句」（無害）。
    3. 限制性重生成（CoVe factored、看不到正確草稿）在嚴格約束下，對「五十度以上」黏在實體名裡的彆扭事實，有時答成「資料未明確記載」——**把正確草稿改壞**。
  - **✅ 已實作（2a，主修）＋Q4 端到端重測 0/3 → 2/3**（commit `fb01bde`）：`ClaimGrounding` 加 `is_claim: bool = True`（缺省 True＝分類不出來時當主張、保守）；`_grounding_prompt` 加指示，每句先判 `is_claim`（含數字／期限／金額／條件／結論的具體斷言＝true；引言／標題／問題回貼／過渡語＝false），`is_claim=false` 時 `supported` 一律 true；`verify_fact_grounding` 解析、`is_claim=False` 強制 `supported=True`；`chat()` 觸發條件 `any(not c.supported …)` → `any(c.is_claim and not c.supported …)`；`event: grounding` SSE 加 `is_claim` 欄位。5 新測試、630 全通過。**Q4 端到端（走真正的 `chat()`，3 輪）**：ROUND 2/3 引言句／問題標題被正確判非主張 → 未接地主張數 0 → 不觸發重生成 → 正確草稿直接送出（兩部分全對）；ROUND 1 草稿有一句被判未接地的主張 → 觸發重生成 → 限制性 prompt 對「五十度以上黏在實體名裡」的彆扭事實仍過度修正（2b 範圍）。
  - **設計（2b，未做、先觀察）**：重生成觸發後仍是整段重寫。若要再進一步，考慮「只改未接地的那幾句」的定向修訂（CoVe 建議不給草稿——需權衡）。ROI 遞減，未列入本輪。
  - **文獻**（`docs/參考文獻/22_生成端過度保守與選擇性拒答/`，新增）：RAGAS Faithfulness（Es et al. 2023，🟢 `docs/參考文獻/05`，「先抽事實主張、再逐一核對」＝`is_claim` 分類的直接依據）；**Context-faithful Prompting**（Zhou et al. 2023，🟢 EMNLP Findings，arXiv:2303.11315，已下載全文精讀）——框定 knowledge-conflict / prediction-with-abstention 兩面向，**但 §4.4 明確指出 opinion-based prompt 對小模型會「把可回答問題的不確定預測改成 I don't know」、加重過度拒答，`qwen2.5:7b` 為小模型故本次不採用**；RefusalBench（Muhamed et al. 2025，arXiv:2510.10390，已下載待精讀，selective refusal 校準基準）；"Not All Needles Are Found"（arXiv:2601.02023）、Copy-Paste（arXiv:2510.00508）搜尋層級。
- [~] **⑦ 發現3（抽取論元／子句錯接）——§7 收尾重跑確認 Q2、Q7 皆屬此類**：
  - **Q7**：`每增加一種型式，加收新臺幣四千元` 被抽成「加收**八千元**」（把 §3 第1項基礎費 8000 的數字錯接到第2項子句）。
  - **Q2**：`未滿三十日：於五日前提出` 被抽成「**三十日以上** 於五日前提出」（把第1款主詞複製到第2款謂語）。c15949bf 同時存在錯的 `RELATED_TO {verb:'於五日前提出'}` 與對的 `HAS_PREREQUISITE {verb:'於十日前提出'}` 兩條邊，主詞都是「三十日以上」。
  - **性質**：皆為 SVO 抽取的**列舉式條文子句錯接**——數字/謂語逐字正確但綁到錯的主詞/子句。數值忠實性核對（[20](20_抽取數值忠實性核對機制設計報告.md)）只驗「數字逐字出現在 chunk」，攔不下這類。
  - **✅ 已實作（2026-09-03，commit 待補）**：文獻盤整——Huang et al. (2026) entailment-aware（per-triple 蘊涵核對、不一致即丟棄，方法論直接先例，僅書目層級無免費全文）、SRVF（Li, Miao, Zhou et al. 2024, AAAI 2025, arXiv:2412.07289，驗 rationale＋doc-level 逐三元組 retain/discard，訓練式故取其精神）、atomic-propositions（Pommeret et al. 2026, arXiv:2604.02866，列舉句先拆再抽但對強 LLM 反效果，改採事後子句核對）三篇下載進 `docs/參考文獻/18`。實作 `_quantity_mis_bound_to_clause()`（`services/svo_service.py`）——把報告20 的字詞層級核對擴充到子句層級綁定：對三元組 verb＋object 的每個數量片語 Q，當 (1) subject 正規化長度 ≥ 4 且在原文有歸屬子句、(2) subject 歸屬子句與 Q 歸屬子句完全不相交、(3) subject 歸屬子句帶著「同單位、不同值」的競爭數字——三者皆成立才丟棄。條件 (3) 防「數字在共用句幹、條件在列舉項目」的正常法條被誤殺（報告20 既有 case 驗證無迴歸）。子句切分依句末標點＋「一、」「（一）」「1.」列舉標記，優先用 `original_sentences`。真實 chunk 驗證：N0030018 §2「五日前」錯接邊、N0070020 §3「八千元」錯接邊，`_quantity_mis_bound_to_clause()` 皆判 True、對照的正確邊皆判 False。已對這兩份 targeted 重抽（`revoke_chunk_facts()` + `_process_one()` 真實 production 路徑）。
  - **收尾修正（真實重抽發現）**：全 64 份文件已有版本＋兩份 targeted 重抽都清乾淨後，Neo4j 直查仍殘留簡體 subject 的錯接邊（`育婴留職停薪期间三十日以上 -[於五日前提出]->`、`每增加一种型式 -[加收]-> 八千元`）。根因：`qwen2.5:7b` 對繁體輸入偶發輸出**簡體 subject**（報告25 §4 發現4），簡體 subject 在繁體 `original_sentences` 裡永遠找不到歸屬子句 → `_quantity_mis_bound_to_clause()` 被「subject 無歸屬子句」守衛靜默略過。修正：`_normalize_for_binding()` 加 `_to_traditional()`（與發現5 共用 OpenCC `s2tw`），比對前兩邊都轉繁。加一項簡體 subject 攔截測試（共 8 項）。修正後 N0030018 §2 / N0070020 §3 再 targeted 重抽。
  - 8 新單元測試（含兩真實案例點檢＋端到端 drop＋短主詞略過＋共用句幹不誤殺＋簡體 subject）＋全套測試通過。文件見 `docs/論文/03_系統設計與方法論.md` §3.1.4。
  - **收尾修正2——實體去重過度合併（真實重抽發現，比發現3 更根本）**：發現3 的子句層級核對在 merge **前**跑，拿到的三元組本身是對的（`extract_svo_triples_with_completeness_check()` 連跑 4-6 次都輸出 `每增加一種型式，加收 → 新臺幣四千元`）。Q7 的錯誤實際發生在 `merge_triples_to_graph()` → `resolve_entity_name()`（DEDUP4）：
    - **object 側**：`新臺幣四千元` 與 c15949bf 既有的 `新臺幣八千元` 只差一字，`_edit_ratio`（`difflib.SequenceMatcher`）＝**0.833** ≥ `ENTITY_DEDUP_EDIT_RATIO_THRESHOLD` 0.70 → 誤併，邊接到 `八千元`。**已修（使用者 2026-09-03 選定）**：`resolve_entity_name()` 加守衛——`name` 含「數字＋單位」（`_QUANTITY_PATTERN`）時只准精確比對，跳過編輯距離／cosine。`四千/八千`、`五日/十日`、`三十日以上/未滿三十日` 都不再被模糊合併。2 新測試。
    - **subject 側（尚未修，屬 「實體混合＋embedding 過度合併」調查的範疇）**：正確三元組 `每一型式 收取 → 八千元` 的 subject `每一型式` 與孤兒節點 `每增加一種型式`（前一次壞抽取遺留）`_edit_ratio`＝**0.727** ≥ 0.70 → `每一型式` 被併進 `每增加一種型式`，變成 `每增加一種型式 → 八千元`。`每一型式`/`每增加一種型式` 不含數字、守衛不適用。單純調高門檻會連「台積電/台積電公司」（0.75）這種正常變體去重一起打壞。清掉孤兒節點＋乾淨重抽兩個 chunk 可暫時繞過（乾淨抽取的正確 subject 是長句「同時申請多種型式且皆屬相同…者」、不會產生 `每增加一種型式` 節點）；治本需要為此語料重新校準 DEDUP 門檻或加「差異字是否為區別性語素」判斷，另立追辦。
- [x] **發現4：抽取端簡體洩漏（2026-09-03，使用者選定「來源語料當白名單」）**：`qwen2.5:7b` 對繁體輸入偶發輸出簡體 subject／verb／object，污染 Entity 名稱（c15949bf 內 3954 個節點有 710 個含簡體）、拖累檢索字面比對、跟繁體同義節點分裂（`事业单位`／`劳工`／`中央主管机关` 等 43 個簡繁同名）。
  - **即時路徑**：`traditionalize_triples(triples, _kg_source_charset(kg_folder))` 接進 `_process_one()` merge 前。**白名單必要性**：OpenCC 會把 `雇→僱`、`托→託` 也轉，但勞基法原文用「雇主」（144 次）、育嬰留停辦法原文是「停托」——`_to_traditional_selective()` 只轉「OpenCC 會改、且不在該 KG 繁體來源文件用字集」的字（`職`／`經`／`嬰` 轉、`雇`／`托` 留）。
  - **回填**：`backfill_traditionalize_entity_names()`（純改名＋簡繁撞名時搬邊：SVO 事實邊 union `citations_json`、其餘同型別同端點的邊丟、沒有的重建、`DETACH DELETE` 簡體節點）；`backfill_fact_text_embeddings()` 加 `source_charset` 參數收 `fact_text` 殘留。真實回填 c15949bf：純改名 503、撞名 merge 43、白名單保護免誤轉 164。
  - 邊 `natural_text` 由發現5 的 `backfill_natural_text_traditionalize()`（全轉）處理、已 0 簡體，本輪不動。7 新單元測試。文件見 `docs/論文/03` §3.1.4 `DEDUP4`。
- [x] **發現C：實體去重過度合併——分段量詞（2026-09-03，診斷 Q3 後從「B：BFS 漏檢」轉向）**：診斷 Q3（`diagnose_q3_bfs.py`）發現漏檢**不是 BFS 接不到**——`vector_search_facts` top-20 正確撈到全部 4 段（未滿6歲→二小時 #5 score 0.849、6-12歲→三小時 #3、12-15歲→四小時 #4、未滿6個月→三十分鐘 #1）。真正阻塞：實體去重把三段年齡主詞併成一個節點。c15949bf 裡 `年齡未滿六歲者`／`年齡六歲以上未滿十二歲者` **根本不存在**，只有 `年齡十二歲以上未滿十五歲者`，三段工時上限（二/三/四小時）全接它身上、`natural_text` 寫錯（「年齡十二歲以上未滿十五歲者每日不得超過二小時」）。`_edit_ratio(年齡六歲以上未滿十二歲者, 年齡十二歲以上未滿十五歲者)`＝0.800 ≥ 0.70 → edit 併；`年齡未滿六歲者`(ratio 0.60) → cosine ≥ 0.88 併。數量守衛擋不住（`_QUANTITY_PATTERN` 單位表沒有「歲」）。
  - **修（使用者 2026-09-03 選定「擴量詞守衛到分段量詞」）**：新增 `_MEASURE_PATTERN`（比 `_QUANTITY_PATTERN` 廣，多收 `歲|人|名|週|度|種|類|條|款|項|點`），`resolve_entity_name()` 守衛從 `_QUANTITY_PATTERN.search` 換成 `_MEASURE_PATTERN.search`。`_QUANTITY_PATTERN` 不動（不碰發現3／報告20）。守衛在 edit + cosine 迴圈之前、兩條模糊路徑都跳過、只准 exact。
  - **回填**：revoke + `_process_one()` 重抽 N0030025 chunk 4（三段年齡條文都在這 chunk）。重抽後 `年齡未滿六歲者→二小時`／`年齡六歲以上未滿十二歲者→三小時`／`年齡十二歲以上未滿十五歲者→四小時` 三個獨立節點、`natural_text` 皆正確。
  - 4 新測試（`_MEASURE_PATTERN` 為 `_QUANTITY_PATTERN` 超集、年齡分段不合併）。Q3 端到端見 §7.3。
- [ ] **仍未涵蓋**：不含任何量詞的短法律語素（`每一型式` vs `每增加一種型式` `_edit_ratio`＝0.727）——治本要「差異字是否為區別性語素」判斷或 DEDUP 門檻校準（Fellegi & Sunter 方法論、需驗證集），另立「實體混合＋embedding 過度合併」調查。
- [ ] 既有 KG 尚未因本輪發現全面重新抽取（發現3 targeted 重抽 N0030018／N0070020；發現C 重抽 N0030025 chunk 4；發現4 走回填）；新KG `c15949bf` 抽取仍暫停在 986/3303（使用者要求維持暫停）。

## 7. 收尾重跑：8 題條件A before/after（2026-09-03）

發現 1/5/4/6/2a 全部落地（commits `1215f8b`~`fb01bde`）＋ c15949bf 回填後，用同一組 8 題、同一份正解、走真正的 `chat()` 端點重跑條件A（新KG），與 §2 的原始條件A 逐題對照。條件D（純LLM）不受本輪修正影響、§2/§3 已有數據；條件B（舊KG 凍結）資料面未回填、比較意義有限，皆不重跑。

| 題 | 文件 | BEFORE（§2 條件A） | AFTER（全套修正） | 變化 |
|---|---|---|---|---|
| 1 | N0030011 夜間安全設施 | ◐ 通道對／上鎖漏檢 | ✅ 兩項全對（不得上鎖＋通道不得堆置） | **↑** |
| 2 | N0030018 育嬰留停 | ◐ 2對1拒答（**無錯誤**） | ✗ 1對／次數混亂／「提前**五日**」**錯誤**<br>→ **發現3 修正後 ✅**（見 §7.3） | **↓ 後 ↑↑** |
| 3 | N0030025 未滿15歲工時 | ◐ 漏「未滿6歲≤2h」 | ◐ 同（且混淆「6歲/6個月」）<br>→ **發現C 修正後 ✅**（見 §7.3） | **持平 後 ↑↑** |
| 4 | N0060007 高溫作業 | ◐ 黑球溫度對／**工資逐字引用卻拒答** | ✅ 兩項全對 | **↑** |
| 5 | N0060012 精密作業 | ◐ 明視距離漏檢 | ✅ 三項全對（含明視距離約30公分） | **↑** |
| 6 | N0060065 母性健康保護 | ◐ 血鉛第三級拒答 | ✅ 三項全對（措辭有小矛盾瑕疵） | **↑** |
| 7 | N0070020 檢查費 | ◐ 「加收4千」被抽成「8千」 | ◐ 同（抽取錯接＝發現3）<br>→ **發現3 修正後 ✅**（見 §7.3） | **持平 後 ↑** |
| 8 | N0080016 原住民訓練補助 | ✗ **三項全拒答** | ✅ **三項全對** | **↑↑** |

**淨結果（發現1/5/4/6/2a 落地後）：5 題改善、2 題持平（3/7）、1 題「回歸」（Q2）。**
**再疊發現3（子句層級核對＋數量實體去重守衛＋targeted 重抽）後：Q2、Q7 轉 ✅。**
**再疊發現C（`_MEASURE_PATTERN` 分段量詞守衛＋重抽 N0030025 chunk 4，2026-09-03）後：Q3 轉 ✅。8 題全數 ✅。**

### 7.1 Q2「回歸」追查——不是本輪程式回歸，是暴露的抽取壞資料

生成鏈診斷（`diagnose_q2_regression.py`）＋ Neo4j 直查確認：c15949bf 內有一條**抽取錯誤**的邊 `育嬰留職停薪期間三十日以上 -[RELATED_TO]-> "" {verb:'於五日前提出'}`——N0030018 §2 原文第二款主詞是「**未滿三十日**」（十日前是第一款「三十日以上」的規定）。這與 Q7 是同一類（發現3，見 §6 ⑦）。

**為什麼 §2 是「拒答」、§7 變「答錯」**：§2 條件A 用舊預設 `top_k=5`，這兩條矛盾事實排在第 5-6 名、撈不到 → 模型誠實拒答；發現1 把 `top_k` 提到 20 後，**對的（十日前）和錯的（五日前）都進了 prompt**，LLM 看到「三十日以上→十日前」與「三十日以上→五日前」互相矛盾 → 有時挑錯。**檢索修正本身是對的——它把原本被過小檢索窗口遮住的壞資料暴露出來。** 正解是發現3（子句層級對位檢查 + 重抽），非退回檢索修正。

### 7.2 整體結論

檢索側三層修正（發現1 候選數＋文件範圍、發現5 型別括號＋簡繁、發現6 殘缺過濾＋RRF 分來源排列）＋生成側 `is_claim` 觸發收緊（發現2 2a）疊起來，把 8 題條件A 的「有漏檢／拒答」項大幅收斂——原本 8 題全是 ◐/✗，現在 5 題 ✅、Q8 從全拒答到全對。剩下的 2 個持平題（Q3 已知漏檢、Q7 抽取錯接）與新暴露的 Q2（抽取錯接）都指向**同一個下一步：發現3（抽取論元／子句錯接的檢查機制 + 重抽）**。

### 7.3 發現3 落地後 Q2／Q7 端到端重測（2026-09-03）

發現3 的完整落地（見 §6 ⑦：子句層級綁定核對＋簡繁正規化＋數量實體去重守衛）＋ N0030018 §2／N0070020 §3 targeted 重抽後，走真正的 `chat()` 端點，Q2／Q7 各重測 2 次：

| 題 | 目標事實 | 修正前（§7 表） | 修正後 run 1 | 修正後 run 2 |
|---|---|---|---|---|
| Q2 | 三十日以上→提前**十日**（非五日）；每次原則≥6個月；未達6個月以2次為限 | ✗「提前五日」錯 | ✅「三十日以上→十日前」；「每次原則六個月」；「以二次為限」 | ✅「三十日以上→十日前」；「每次原則六個月」 |
| Q7 | 型式檢查每型式**8000**；每增一型加收**4000**；補發/換發每張**800**；休息日加收**50%** | ◐「加收4千」被抽成「8千」 | ✅ 四項全對（8000／4000／800／50%），有一次限制性重生成 | ✅ 四項全對，**免重生成** |

**Q2** 的「五日前↔三十日以上 張冠李戴」與 **Q7** 的「四千↔八千」在兩輪重測皆已消失。追查揭露這其實是**三層問題疊加**：
1. **LLM 子句錯接**（發現3 本體，`_quantity_mis_bound_to_clause()`）——這一層在真實重抽時 `extract_svo_triples_with_completeness_check()` 連跑 4-6 次都輸出正確三元組，反而不是主因；作為守門機制保留。
2. **簡繁混用**（`qwen2.5:7b` 對繁體輸入偶發輸出簡體 subject）——簡體 subject 在繁體 `original_sentences` 找不到歸屬子句 → 綁定核對被守衛靜默略過。修：`_normalize_for_binding()` 加 `_to_traditional()`。
3. **實體去重過度合併（Q7 真因）**——正確三元組 `每增加一種型式，加收 → 新臺幣四千元` 在 `merge_triples_to_graph()` → `resolve_entity_name()` 被 `_edit_ratio`（`新臺幣四千元` vs 既有 `新臺幣八千元` ＝0.833 ≥ 門檻 0.70）誤併成 `八千元`。修（使用者 2026-09-03 選定）：`resolve_entity_name()` 對含「數字＋單位」的 `name` 只准精確比對。

⚠️ **尚未涵蓋**：非數字的短法律語素（`每一型式` vs 孤兒節點 `每增加一種型式` ＝0.727 ≥ 0.70）同樣會被模糊合併——本輪靠「刪孤兒節點＋乾淨重抽」繞過；單純調高門檻會打壞「台積電/台積電公司」（0.75）這種正常變體去重，此語料的 DEDUP 門檻校準另立追辦（見 §6，「實體混合＋embedding 過度合併」調查的範疇）。

11 新單元測試（8 子句核對＋2 數量守衛＋1 簡體 subject）＋全套 641 測試通過。

### 7.4 發現C 落地後 Q3 端到端重測（2026-09-03）

`_MEASURE_PATTERN` 分段量詞守衛 + 重抽 N0030025 chunk 4（三段年齡條文）後，走真正的 `chat()` 端點，Q3 重測 2 次：

| 題 | 目標事實 | 修正前（§7 表） | 修正後 run 1 | 修正後 run 2 |
|---|---|---|---|---|
| Q3 | 未滿6歲→**2h**；6-12歲→**3h**；12-15歲→**4h**；未滿6個月每次→**30分鐘** | ◐ 漏「未滿6歲≤2h」、混淆「6歲/6個月」 | ✅ 四段全對、**免重生成** | ✅ 四段全對、**免重生成** |

三段年齡工時上限＋未滿六個月限制在兩輪皆正確、無混淆。KG 直查：重抽後 `年齡未滿六歲者→二小時`／`年齡六歲以上未滿十二歲者→三小時`／`年齡十二歲以上未滿十五歲者→四小時` 為三個獨立節點、`natural_text` 皆對段。（run 2 的答案文字有 `qwen2.5:7b` 自己生成時的簡體字，屬生成端、非 KG 資料，與發現4 修的抽取端簡體是兩回事。）

15 新單元測試（8 子句核對＋2 數量守衛＋1 簡體 subject〔發現3〕＋4 分段量詞守衛〔發現C〕）＋全套 648 測試通過。**8 題條件A 全數 ✅**（Q1/4/5/6/8＝發現1-6，Q2/Q7＝發現3，Q3＝發現C）。
