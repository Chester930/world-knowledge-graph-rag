# 31．論文正文跨章一致性稽核報告

**日期**：2026-09-07
**範圍**：`docs/論文/01_緒論.md`、`02_文獻探討.md`、`03_系統設計與方法論.md`、`04_系統實作.md` 之間的跨章一致性；純檢查與文件更新，不更動專案程式碼。
**對照基準**：本地 `master` `fd0c390`（報告 18–29 之程式碼與文件皆已 ff-merge 進 master；`routers/agent.py`／`services/svo_service.py`／`services/verification_service.py` 的報告 25§4／26／27／28／29 實作皆在庫）。
**方法**：逐章通讀四章，交叉比對 `00_研究追溯對映表.md`、`03_變更紀錄.md`、報告 23–29、參考文獻夾 19–25，並直接以 `git log`／`grep` 核對程式碼實況。

---

## 0. 總體判斷

三個結構性成因造成目前的跨章落差：

1. **02_文獻探討 的索引（§2.1.1）與正文實質凍結在 2026-09-01**——報告 25§4／26／27／28／29 新增或精讀的文獻（參考文獻夾 21、22、23、部分 24/25）沒有在第二章的家。
2. **04_系統實作 §4.7（查詢與問答）落後 03 §3.6 約一個月**——03 §3.6 有約 15 項 ✅ 實作標記，04 §4.7.2 只記到「方案 B」（2026-08-28），報告 23／24（部分）／25§4／26／27／28 的生成端機制在 04 幾乎全缺。
3. **01_緒論 與 00_README.md 是慢性落後者**——ConceptNet 關係型別數（33 vs 35）、RQ6 狀態行皆未跟上 2026-08 之後的訂正（符合 `project_ch123_consistency_audit.md` 記錄的既有模式）。

03 §3.6 反而最新；但 §3.2§b／§3.7 因報告 27 的自訂政策（待 c15949bf 全量重抽 B 驗證後才一次更新）而刻意未動，且完全缺報告 28（複合問題分解）。

---

## 1. 類別 (a)：01/02 提及，但 03 未規劃、04 未實作

純 (a) 落差很少——01/02 對「預留」標記相當謹慎。可注意者：

| 編號 | 項目 | 現況 | 判定 |
|---|---|---|---|
| a-1 | 社群偵測／Global Search（階層式 Leiden） | 01 §1.4.1 明確排除項 #4；03 §3.1 總覽圖畫了 `COMM` 節點（虛線）但無任何細部設計；04 完全未提。 | 內容一致（已排除），但總覽圖留了一個零內容的懸空節點——建議在圖註補一句「僅為工程借鏡方向、非 RQ、無對應設計與實作」。 |
| a-2 | AIS 可追溯性量化 | 01 §1.1.4／§1.2 指定 AIS 為 RQ3／RQ4a／RQ4b 共同次要指標；03 §3.8 只有「可追溯性承諾」一句、無「如何對本系統量測 AIS」的方法段；04 只有 `sources`／`surface_form`／`citations_json` 資料基礎。 | 依「預留」屬第五章，可接受；但 03 缺一個把 01 的 AIS 承諾接到本系統資料結構的橋接小段。列為第五章撰寫時的待辦，本次不強制補。 |
| a-3 | 多型態結構化輸入（TextProjection 雙軌、JSON/DB/API） | 01 §1.1.5 + 02 §2.6.4（~20 行）+ 03 §3.1.5（~65 行 SDD）→ 04 §4.0.1 明確「未實作」。 | 三章都誠實標未實作、內部一致，**不算缺陷**。但這是「跨 01/02/03 大篇幅、零程式、非 RQ」的最大一塊，讀者追 RQ→設計→實作時 §3.1.5 沒有 §4 對應（只有免責聲明）。維持現狀，僅在此記錄。 |

---

## 2. 類別 (b)：03 規劃了、01/02 未鋪陳，或違反自訂分工

| 編號 | 項目 | 03 位置（已實作） | 02/01 落差 | 處置 |
|---|---|---|---|---|
| b-1 | 法規領域結構化（`Document`／`LawArticle` 節點、法條感知切塊、`effective_date` 時序錨定） | §3.1.2 `ChunkingStrategy`／`ArticleAwareChunking`；§3.5「文件／法條層級的時序錨定」（de Martim 2025 SAT-Graph RAG、Akoma Ntoso、Sonowal & Sadhu 2025、R2RML／ontop）。04 §4.2.2 已有節點層＋`import_leave_scheduling_dataset.py` | 02 §2.6.4 只有通用 W3C 映射標準，**無**法律文件結構表示／SAT-Graph RAG／結構感知法律切塊文獻；01 完全未提法規領域特化（緒論定位為多場景通用） | 02 新增小節收納法律結構化文獻 |
| b-2 | 事實層級向量化（`Fact` 節點／KAPING／LightRAG／HippoRAG／G-Retriever 四篇光譜） | §3.1.4 §a 把完整 4 篇文獻討論寫在 3.1 子節內——**違反 03 開頭自訂「3.1 只描述機制、文獻集中在對應 RQ 小節」的分工**。04 §4.5.3 有實作 | 02 無對應節——只有 §2.4.8（RQ6 剪枝節）一句帶過 G-Retriever；參考文獻夾 12 無 ch2 的家 | 02 新增小節（歸 RQ1 檢索精準度脈絡）收納四篇；03 §3.1.4 §a 文獻討論改為指標引用該新節 |
| b-3 | 實體節點向量化去重的現代文獻（Enamorado 2019 fastLink、Linacre 2022 Splink、`neo4j-labs/llm-graph-builder`、zingg） | §3.1.4（DEDUP4 效能改造段）引用；參考文獻夾 13 存在 | 02 §2.6.3 只有 Wikidata＋Knowledge Vault（事實層級），無實體節點去重的 Fellegi-Sunter 現代脈絡 | 02 §2.6.3 補一段實體節點去重的現代文獻 |
| b-4 | 「結構化條件過濾」作為第四種檢索能力 | §3.5（L983）提出；§3.5（L985）自承未按設計實作（改用 `vector_search_facts()` 來源訊號替代） | 01 的 7 層架構無此層、02 無鋪陳 | 懸空設計概念——在 03 §3.5 該段補「此為方向記錄，實際以既有語意檢索訊號替代，未獨立實作」的收斂註記（03 §3.5 L985 其實已有，僅需確認措辭）；01/02 不強制補 |
| b-5 | 02 §2.4.5 範圍修飾詞守衛敘述與程式碼不符（同時是 02 內部矛盾） | 03 §3.1.4（L484）正確標「設計討論中未實作」 | 02 §2.4.5（L256）把「範圍修飾詞守衛（增加／額外／再／又／另等）」敘述為既有機制，但那是報告29 §4.1，**尚未實作**（待全量 drain）；同段後面又 hedge「若後續實作另立報告」 | 02 §2.4.5 改標「設計中／未實作」，並明確區分報告29 §4.3（數字比較詞，已實作）與 §4.1（範圍修飾詞，未實作） |
| b-6 | 04 未實作 03 所規劃 | RQ2 ConceptNode 路由、RQ3 正式自我精煉迴圈、RQ5 時序、RQ6 L2 剪枝 | 03／04 雙方皆已明確標「未完成」 | **非缺陷**，列出供第五章實驗組別對照 |

---

## 3. 類別 (c)：04 已實作、03/02/01 未同步（最大宗）

| 編號 | 機制（程式碼實況） | 03 現況 | 04 現況 | 02 現況 | 處置 |
|---|---|---|---|---|---|
| c-1 | **報告27 BFS L0+L1 剪枝**（2026-09-04，commit＋9 測試；門檻 2026-09-05 §6.2 單題調校）：`bfs_query()` hops 改「最大允許跳數」＋自適應（先 1-hop，去重後 `<_BFS_EXPAND_WHEN_BELOW=8` 且 hops≥2 才擴展）；`scope_doc_ids` Cypher 範圍下推＋空結果 fallback；`per_seed_limit`(`_BFS_PER_SEED_LIMIT`，現行 30)；`_drop_hub_seeds()`(`_SEED_MAX_DEGREE`，現行 100)；`ChatRequest.svo_hops` 預設 2→1 | §3.2§b 仍寫「⚠️ 現行完全不截斷」；§3.7 仍寫「本節內容目前不存在對應實作」；兩節 2026-09-01 更正段仍說「無任何上限」 | §4.7.1 仍寫「bounded BFS…跳數限制內」且只描述 2026-08-27 版來源篩選 | §2.4.8 **已有承認註記**（「報告27 已分層實作 L0+L1，正文狀態同步待全量重抽驗證後一次更新」） | 03 §3.2§b／§3.7、04 §4.7.1、01 §1.2 RQ6 狀態行、`00_研究追溯對映表` §2 RQ6 皆標「L0/L1 已實作（2026-09-04，查詢端），L2 及全量重抽 B 驗證待補」——與報告27 §7 政策相容，措辭比照 02 §2.4.8 既有註記 |
| c-2 | **報告28 複合問題子問題分解 M0+M1**（2026-09-04/05，665 測試，真實 LLM 前後對照）：`routers/agent.py` `_split_into_subquestions()`（規則式、以「？」為界、零額外 LLM 呼叫）、`_generate_decomposed_answer()`（逐子問題獨立生成）、`_generate_decomposed_constrained_answer()`、`chat()` 接線；M0 prompt 補強 | **完全沒有這一節** | **完全未提** | **無對應段**——參考文獻夾 `23_複合問題分解與自相矛盾修正`（Least-to-Most Zhou 2023 ICLR、Self-Ask Press 2022，皆全文精讀）無 ch2 的家 | 03 §3.6 新增小節「§生成端複合問題子問題分解」；04 §4.7 新增段落；02 §2.4.7 補文獻段 |
| c-3 | **報告29 §4.3 數字比較詞守衛 `_RANGE_COMPARATOR_PATTERN`**（2026-09-05，commit `df2bc62`，master，670 測試，端到端驗證通過，使用者核准破例提前上線）：`resolve_entity_name()` 早退守衛（裸數字＋以上/以下/以內/未滿/超過）；修正報告26 §4 #3 真正根因（Q8「多跳推理」實為 DEDUP 過度合併） | §3.1.4 `GUARD4`（L482-484）只寫 `_QUANTITY_PATTERN`／`_MEASURE_PATTERN` | §4.5.1 未提 | §2.4.5 沒提 §4.3（只把 §4.1 範圍修飾詞敘述為既有機制，見 b-5） | 03 §3.1.4 `GUARD4` 補 `_RANGE_COMPARATOR_PATTERN`；04 §4.5.1 同步；02 §2.4.5 補「§4.3 已實作」（併入 b-5 一起處理） |
| c-4 | **報告25 §4 發現6 `_arrange_fact_lines()` v2／RRF 融合**（BFS 鄰居剪枝→名額分配→`_rrf_order()` RRF→zigzag） | §3.6（L1052-1060）已寫 ✅ | **§4.7 未提** | **無對應段**——參考文獻夾 `21_圖遍歷與向量檢索結果融合`（Cormack RRF 2009 全文精讀、Han GraphRAG survey 2501.00309、SAGE Titiya 2026） | 04 §4.7 補段；02 §2.4.7 補文獻段 |
| c-5 | **報告25 §4 發現2a 接地核對 `is_claim` 三分類** | §3.6（L1062-1066）已寫 ✅ | **§4.7.2 只記到方案 B（2026-08-28），無 `is_claim`** | **無對應段**——參考文獻夾 `22_生成端過度保守與選擇性拒答`（Zhou 2023 Context-faithful Prompting、Muhamed 2025 RefusalBench；ch2 只有 Wen 2025 abstention 綜述） | 04 §4.7.2 補段；02 §2.4.7 補文獻段 |
| c-6 | **報告23 生成端排序 `_score_lines_by_embedding()`／`_litm_reorder()` zigzag** | §3.6（L1026-1034）已寫 ✅ | **§4.7 未提** | 文獻部分 02 §2.4.7 已有 Liu／Jin／Nogueira ✓ | 04 §4.7 補段（併入 c-4） |
| c-7 | **報告25 §4 發現5 `_verbalize_fact()` 移除型別括號＋OpenCC 簡→繁** | §3.1.4 §a（L555-561）已寫 ✅ | §4.4.3／§4.5.3 只提 `natural_text`，未提型別括號移除／簡繁正規化 | — | 04 §4.4.3／§4.5.3 補一句 |

---

## 4. 具體數值／狀態不一致（可直接校正）

| 編號 | 問題 | 正確值／狀態 | 落後處 |
|---|---|---|---|
| N1 | ConceptNet 受控關係型別數：33 vs 35 | **35**（程式碼 `core/constants.py::SVO_REL_TYPES`；02 §2.1.1、02 §2.4.4 L215、03 §3.1.3、03 §3.3 皆已 35） | 01_緒論 L81／L84／L141「33 類」；02 §2.4.4 **L202**「33 類」（與同節 L215「35」矛盾）；00_README.md L27／L28「33 類」 |
| N2 | 01 §1.2 RQ6 狀態行「🔴 尚無 Hub 偵測…BFS 遍歷本身無截斷」；`00_研究追溯對映表` §2 RQ6「沒有 Hub 偵測」 | 報告27 後已有 seed 級 hub 偵測（`_drop_hub_seeds`）＋per-seed 上限，「無截斷」已非事實 | 01 §1.2 RQ6 列、`00_研究追溯對映表` §2／§3 RQ6 列 |
| N3 | 02 §2.4.5 範圍修飾詞守衛敘述為既有機制、實為未實作 | 報告29 §4.1 未實作、§4.3 已實作 | 02 §2.4.5 L256（見 b-5／c-3） |

---

## 5. 修正計畫與執行順序

本次全部為文件修正，不動程式碼。依序：

1. **04 §4.7 回灌**：補 c-4／c-5／c-6 的生成端機制（RRF 融合、`is_claim`、embedding 排序＋zigzag），與 03 §3.6 對齊。
2. **03 §3.6 新增「複合問題子問題分解」小節 + 04 §4.7 新增段落 + 02 §2.4.7 補 folder 23 文獻段**（c-2）。
3. **03 §3.1.4 `GUARD4` 補 `_RANGE_COMPARATOR_PATTERN` + 04 §4.5.1 同步 + 02 §2.4.5 修正 b-5**（c-3）。
4. **02 補文獻小節**：法律文件結構化（b-1）、事實層級向量化 KAPING/LightRAG/HippoRAG/G-Retriever（b-2，並把 03 §3.1.4 §a 的文獻討論改為指標引用）、實體節點去重現代脈絡（b-3）；folder 21／22 文獻段（c-4／c-5）併入 §2.4.7。
5. **數值校正 N1／N2／N3**：01（×4）、02 §2.4.4 L202、00_README（×2）、`00_研究追溯對映表` RQ6。
6. **報告27（c-1）同步**：03 §3.2§b／§3.7、04 §4.7.1、01 §1.2 RQ6、`00_研究追溯對映表` RQ6 皆標「L0/L1 已實作（查詢端），L2 及全量重抽 B 驗證待補」。
7. **a-1 圖註補一句**（社群偵測懸空節點）。

執行紀錄見本報告 §6（隨修正逐項回填）與各章對應的 `03_變更紀錄.md` 條目。

---

## 6. 執行紀錄（2026-09-07，於 worktree `worktree-thesis-consistency-audit`）

- [x] **Step 0**：本報告建立。
- [x] **Step 1**：`04_系統實作.md` 新增 §4.7.3「生成端事實清單組裝、排序與複合問題分解」——涵蓋 c-4（`_arrange_fact_lines()` v2／RRF）、c-5（`is_claim` 三分類）、c-6（`_score_lines_by_embedding()`／zigzag）；§4.7.1 新增「BFS 遍歷階段 L0＋L1 剪枝」段（c-1）。
- [x] **Step 2**：`03_系統設計與方法論.md` §3.6 新增小節「§生成端複合問題子問題分解」（`_split_into_subquestions`／`_generate_decomposed_answer`／`_generate_decomposed_constrained_answer`，M0＋M1）；`02_文獻探討.md` §2.4.7 新增「複合問題子問題分解文獻」段（Least-to-Most、Self-Ask），§2.1.1 索引新增 6 列。
- [x] **Step 3**：`03` §3.1.4 `GUARD4` mermaid 節點＋新增 ✅ 段落補 `_RANGE_COMPARATOR_PATTERN`（報告29 §4.3），另補 ⚠️ 段記錄 `_SCOPE_MODIFIER_PATTERN`（§4.1，未實作）；`04` §4.5.1 補守衛群描述；`02` §2.4.5 改「若後續實作另立報告」為「§4.3 已實作／§4.1 未實作」的明確分層。
- [x] **Step 4**：`02` 新增 §2.6.5（事實層級向量化與檢索：KAPING／LightRAG／HippoRAG／G-Retriever）、§2.6.6（實體節點向量化去重：Fellegi-Sunter／fastLink／Splink／llm-graph-builder／zingg）、§2.6.7（法規領域文件結構化：Akoma Ntoso／Sonowal & Sadhu 2025／de Martim 2025 SAT-Graph RAG／R2RML／ontop）；§2.4.7 另補「BFS 三元組與語意 Fact 檢索結果的融合排序」（RRF／Han survey／SAGE）與「生成端過度保守與選擇性拒答」（Context-faithful Prompting／RefusalBench）兩段；§2.1.1 索引補對應列；`03` §3.1.4 §a 文獻段標註「完整回顧改由 2.6.5 承載」。
- [x] **Step 5**：ConceptNet 33→35 校正——`01` L81／L84／L141、`02` §2.4.4（原 L202）、`00_README.md` L27／L28；`00_研究追溯對映表` 更新日期＋RQ3／RQ4b／RQ6 列。
- [x] **Step 6**：報告27 c-1 狀態同步——`03` §3.2§b（mermaid＋新增 ✅ 段）、`03` §3.7（改為 L0/L1/L2 三層現況表）、`04` §4.7.1、`01` §1.2 RQ6 列＋風險提示、`00_研究追溯對映表` §2／§3 RQ6 列。門檻值採現行程式碼實況（`_SEED_MAX_DEGREE=100`、`_BFS_PER_SEED_LIMIT=30`，2026-09-05 §6.2 由 200／60 收緊）。
- [x] **Step 7（a-1）**：檢視後判定**不需另補圖註**——`03` §3.1 總覽圖圖註（原文 L49）已明確寫「社群偵測與全局查詢是工程借鏡型改善…本論文不將其列為研究問題，故圖中以虛線標示、不展開細部 Behavior Tree」，語意已足；僅在本報告記錄結論。

**未處理（刻意，非疏漏）**：
- a-2（03 缺 AIS 對本系統的量測橋接小段）——屬第五章實驗設計範疇，待第五章撰寫時補。
- a-3（§3.1.5 多型態輸入無 §4 對應）——三章內部一致、皆誠實標未實作，非缺陷。
- b-4（「結構化條件過濾」懸空設計）——`03` §3.5 L985 既有的收斂註記已說明「未按設計實作、改用既有語意檢索訊號」，措辭足夠，未再改。
- b-2 的「把 `03` §3.1.4 §a 文獻討論整段搬到 `02`」——僅加指標註記，保留 `03` 內的設計脈絡摘要，避免高交叉引用段落搬遷風險。

**尚待使用者/後續處理**：worktree 分支需 fast-forward 進 `master`（本 session 不推 master）。全套測試未跑（本次純文件、未動程式碼；若要保險可 `pytest` 一輪確認無誤觸）。
