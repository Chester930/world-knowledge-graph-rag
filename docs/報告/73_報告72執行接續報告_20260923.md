# 報告72 執行接續報告（2026-09-23）

本文件記錄報告72 S0–S3 執行中途依使用者要求暫停的狀態，供下一輪直接續跑；不代表 S0–S3 已完成，也不改寫 20260920 的歷史凍結基準。

## 1. 執行位置與版本

- worktree：`D:\Users\666\Desktop\world knowledge graph rag\.claude\worktrees\sdd-retrieval-comparison`
- branch：`worktree-sdd-retrieval-comparison`
- HEAD：`2086ee6ba3b34e5e8e3f9cc2f73f424ed2af4cf8`
- 開始前 worktree：乾淨；主 checkout `master` 未被修改。
- 新題庫兩份鏡像 SHA-256：`23f8c06fa7f4459e2bc64531ef80c62f6e00a25b779a0abb7eb4e81933b3aa84`
- KG：`236903cf-055a-40a8-8923-b9d06601f3b7`；Fact `16,826`；Entity `12,296`；task queue `3,307 completed`
- 生成／評分模型：`qwen2.5:7b`；embedding：`bge-m3`；Windows Ollama endpoint：`http://127.0.0.1:11434`
- Ollama 版本：`0.34.3`。舊凍結 manifest 記錄 `0.34.2`，模型 digest 相同；本次已把差異記入新基準 manifest。
- 已知限制仍適用：共用 generator/judge 是 pilot；`57-AGGR18` role mismatch 泛化尚未完成，Atomic Accuracy 可能高估。

開工前沒有可呼叫的 `ListAgents` 工具。唯讀程序檢查看到一個同 worktree 的 Claude session process，但其 transcript 約兩小時未更新；開始評測前 Ollama 沒有載入模型。該 session 未被本次評測使用。暫停時只停止本次啟動的評測程序，沒有停止 Neo4j 或 Ollama 服務。

## 2. S0 已完成部分

新基準目錄：`data/eval/baseline_runs/20260923_rebased/`

已建立：

- `frozen_manifest.json`：記錄本次程式碼、題庫 hash、KG 狀態與環境差異。
- `s0_questions.json`：42 題 eligible 題目。
- `stage_s0_r1/manifest.json`：K arm、`runs=1`、`query_timeout_s=900`、shared judge pilot。
- `stage_s0_r1/records.json`：第一輪已完成 `39/42` 題，均可解析且均無 harness error。

已完成題目最後一題為 `57-AGGR16`；尚未完成：

1. `57-AGGR17`
2. `57-AGGR18`
3. `57-AGGR19`

評測在 `57-AGGR16` 完成後、下一題開始前停止。原本的長時間程序已確認終止；暫停沒有新增 Neo4j 寫入，也沒有修改生產程式碼。

## 3. 下一輪續跑順序

先補完 S0 第一輪：

```powershell
$env:WORKSPACE_DIR='D:/Users/666/Desktop/kg-runtime'
$env:NEO4J_URI='bolt://localhost:17990'
$env:NEO4J_PASSWORD='kg2_test_2026'
$env:OLLAMA_BASE_URL='http://127.0.0.1:11434'

python scripts/eval/frozen_baseline_stage.py remaining `
  --frozen-dir data/eval/baseline_runs/20260923_rebased `
  --records data/eval/baseline_runs/20260923_rebased/stage_s0_r1/records.json `
  --emit data/eval/baseline_runs/20260923_rebased/s0_questions_after_pause.json

python scripts/eval/frozen_baseline_stage.py run `
  --frozen-dir data/eval/baseline_runs/20260923_rebased `
  --questions data/eval/baseline_runs/20260923_rebased/s0_questions_after_pause.json `
  --out data/eval/baseline_runs/20260923_rebased/stage_s0_r1_resume1
```

再以 `adaptive_repeat.py` 依既定品質門檻處理第一輪與補完輪的合併 records：

```powershell
python scripts/eval/adaptive_repeat.py `
  --bank data/eval/test_cases.json `
  --records A=data/eval/baseline_runs/20260923_rebased/stage_s0_r1/records.json `
  --records B=data/eval/baseline_runs/20260923_rebased/stage_s0_r1_resume1/records.json `
  --eligible-ids data/eval/baseline_runs/20260923_rebased/frozen_manifest.json `
  --audit-file data/eval/baseline_runs/20260923_rebased/s0_audit_sample.json `
  --seed 20260920 `
  --summary-out data/eval/baseline_runs/20260923_rebased/s0_adaptive_summary_r2.json `
  --emit-questions data/eval/baseline_runs/20260923_rebased/s0_questions_r2.json
```

後續依 `next_run_ids` 續跑第 2／3 次，每一批使用新的 `stage_s0_rN` 輸出目錄；不得覆寫既有結果。所有輪次完成後，彙整出 S0 新基準 summary、逐題配對結果與信賴區間，再開始 S1。

## 4. 尚未開始的工作

- S0 adaptive repeat：尚未開始。
- S1 K1b：尚未開始；尚未建立 per-KG `factlist.min_bfs_slots` 覆寫，也沒有跑 `--k-top-k 40` 候選。
- S2 article expansion：尚未開始；沒有修改 `_arrange_fact_lines()` 或任何全域預設。
- S3 chunk-RAG B0／B1／D：尚未開始。
- 完整 pytest：尚未開始；應在 S1/S2 程式碼異動完成後執行指定命令。
- 報告72的正式新增結果章節：尚未回寫，待 S0–S3 結果完成後補入。

## 5. 邊界與驗收提醒

續跑時維持報告62 §11.1 的採用規則，不因 S0 新數字或候選結果改門檻；S1/S2/S3 都要以本目錄的 S0 新基準配對。持續使用 `--query-timeout-s 900`、同一 KG、同一題庫、同一生成模型與評分器設定。完成回報必須再次提醒 `57-AGGR18` 的 role mismatch 規則泛化缺口，以及共用 generator/judge 僅屬 pilot。
