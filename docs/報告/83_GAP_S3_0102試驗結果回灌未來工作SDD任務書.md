# 83 GAP-S3-01/02試驗結果回灌論文未來工作章節SDD任務書（**交付 Codex，改 docs/論文/，不改程式碼**）

**建立日期**：2026-09-26
**文件性質**：交付 Codex 執行用任務書。範圍極小——只更新 `docs/論文/07_結論與未來工作.md` 既有的兩個項目點（GAP-S3-01／GAP-S3-02），並視情況在 `docs/論文/06_結果與討論.md` §6.4 補一句。不碰程式碼、不需要 Neo4j/Ollama。
**分支／工作區**：`worktree-sdd-retrieval-comparison`，`D:\Users\666\Desktop\world knowledge graph rag\.claude\worktrees\sdd-retrieval-comparison`
**前置依賴**：報告80（已把 GAP-S3-01／GAP-S3-02 寫進 §7.4，當時兩者都還沒試過）、報告81（GAP-S3-02 獨立模型盲審交叉驗證，已完成）、報告82（GAP-S3-01 上下文組裝離線消融原型，已完成）。三份報告皆已獨立複驗。

---

## 0. 一頁摘要

**問題**：`docs/論文/07_結論與未來工作.md` §7.4 目前對 GAP-S3-01／GAP-S3-02 的措辭是報告80寫的，語氣是「這是兩個**尚未嘗試**的方向」（例如「後續若實作必須先做獨立校準」「應做獨立標註者評分…而不是把規則R結果當作最終真值」）。**但這兩件事現在都已經各自做過一次小規模試驗**（報告81／82），結論都是「目前證據不支持投入更大工程」。若不更新，論文會讓讀者誤以為這兩個方向完全沒人碰過，跟實際進度不符。

**要做的事**：把這兩個項目點的措辭，從「尚未嘗試的未來方向」改寫成「已用最小成本試驗過、結果不足以支持投入、但保留為未來方向」，並附上報告81/82的具體數字與結論引用。**不是刪掉這兩個未來工作項**——兩個方向本身沒有被證偽，只是目前證據不足，措辭要精確反映這個狀態，不能寫成「已排除」或「已證明無效」。

---

## 1. 具體修改內容

### 1.1 GAP-S3-01（`docs/論文/07_結論與未來工作.md`）

現行文字保留（設計背景、報告58的文獻誠實訂正說明仍然成立，**不要刪除**），在後面補充報告82的試驗結果，至少涵蓋：

- 報告82 已完成離線消融原型（不是完整 `ContextBundle` 實作）：對報告78 診斷出的 5 題 Stage 3 生成端遺漏案例（`18-Q1`／`18-Q6`／`57-AGGR14`／`57-COREF3`／`57-DIST2`），各自把 K arm 原有事實清單加上 B1 還原出的 1 個原始法規段落，重新生成 2 次，用既有 `AtomicScorer` 離線評分。
- 結果：10 次生成中 `is_perfect=True` 僅 **3/10**，原本缺失的目標 span 被判定支持 **6/10**（3/5 題至少一次）；只有 `18-Q6` 穩定 **2/2** 完整通過，其餘題目不穩定、未改善、或伴隨答案冗長化（如 `57-COREF3` 長度增加約175%）與表面形式問題（如 `57-AGGR14` 因簡繁字差異被逐字評分器誤判）等副作用。
- 明確結論（引自報告72 §13.4）：**「目前證據不支持投入報告58的完整工程」**——但這不是永久否定，樣本只有 5 題 × 2 次、只測了單一段落的組合方式；若未來有更大樣本、且控制好評分器表面形式與生成非決定性的比較證據，仍可重新評估。

### 1.2 GAP-S3-02（`docs/論文/07_結論與未來工作.md`）

現行文字保留，補充報告81的試驗結果：

- 報告81 已用報告62 §14.6 建立的獨立模型盲審機制（`granite4.2:3b`、`qwen3.5:4b`，盲審、不接觸評分器或規則R的判定），對 S3 落差題的 11 個 missing span 案例做第三方查核。
- 結果：兩模型跟原評分器的一致率分別為 **3/11（27.3%）** 與 **9/11（81.8%）**，跟規則R的一致率為 **5/11（45.5%）** 與 **9/11（81.8%）**，**兩模型彼此只有 5/11（45.5%）一致**——沒有形成穩定的第三方標準。
- 明確結論（引自報告72 §12.4）：本次交叉驗證**「未能提供有力佐證」**，沒有理由宣稱規則R重判後的 `−6` 題落差比原始 `−8` 題更接近真值；報告72 §12.4 建議後續應對 `18-Q6`、`57-DIST2` 做**人工**法規語意判定，而非繼續依賴自動化工具消解這個不確定性。

### 1.3（選配，視你判斷是否需要）`docs/論文/06_結果與討論.md` §6.4

- 若你認為 §6.4 現有的「規則R不是正式評分器」那一條限制敘述，補一句「後續嘗試用獨立模型盲審交叉驗證同樣未能收斂（報告81）」能讓限制章節更完整，可以加；若你認為這樣會讓 §6.4 與 §7.4 內容重複過多，也可以不加，但要在 commit message 說明你的判斷理由。**這是唯一一處給你裁量空間的地方，其餘 1.1／1.2 必須依規格執行。**

---

## 2. 明確不要做的事

- **不要**刪除或大幅改寫 GAP-S3-01／GAP-S3-02 現有的背景說明文字（報告58文獻誠實訂正、規則R樣本內設計性質等）——只在後面補充試驗結果，維持既有段落結構。
- **不要**把這兩個方向寫成「已排除」「已證明無效」——正確措辭是「目前證據不支持投入，保留為未來方向」。
- **不要**修改 `docs/論文/00_研究追溯對映表.md`、`05_實驗設計與評估.md`——本次只動 §7.4（與選配的 §6.4），不涉及 RQ1 追溯表或實驗設計章節本身。
- **不要**修改第 01/02/03/04 章、任何程式碼、`docs/參考文獻/`。
- **不要**新增任何文獻查證——本任務只是把已完成、已複驗的報告81/82結果轉寫成論文措辭，不涉及新文獻。

## 3. 完成定義

1. `git diff --stat` 只顯示 `docs/論文/07_結論與未來工作.md`（若你判斷需要，含 `06_結果與討論.md`），沒有其他檔案被觸碰。
2. GAP-S3-01／GAP-S3-02 兩點都補上了報告82／報告81的具體數字與結論，且數字逐字對照過報告72 §12/§13、報告81、報告82，不是憑印象轉述。
3. 兩點的措辭都精確反映「已試驗、證據不足、非永久否定」，沒有寫成「已排除」。
4. 既有背景文字（報告58文獻訂正、規則R性質說明）保留未被誤刪。

---

## 4. 給 Codex 的指令（可直接貼上）

> 請執行 `docs/報告/83_GAP_S3_0102試驗結果回灌未來工作SDD任務書.md` 的 §1。這是小範圍論文編修，只更新 `docs/論文/07_結論與未來工作.md` 現有的 GAP-S3-01／GAP-S3-02 兩個項目點（§6.4 是否補充由你自行判斷），不碰程式碼。
>
> 背景：這兩個項目點是報告80寫的，當時兩者都還沒試過。現在報告81（GAP-S3-02 獨立模型盲審交叉驗證）與報告82（GAP-S3-01 上下文組裝離線消融原型）都已完成並獨立複驗，結論都是「目前證據不支持投入更大工程」，但都不是永久否定。請把這個實際進度補進這兩個項目點，**保留既有背景文字，不要刪除或大改結構**，只在後面加上試驗結果與明確結論（任務書 §1.1/§1.2 已列出要引用的具體數字，寫作前請自行打開報告72 §12/§13 核對逐字準確性，不要只憑任務書摘要轉述）。
>
> 措辭紀律：這兩個方向是「已試驗、目前證據不足、保留為未來方向」，不是「已排除」或「已證明無效」——兩份報告都留了「若有更大樣本/更嚴謹設計可重新評估」的但書，論文措辭要如實反映這點。
>
> 不要修改 `00_研究追溯對映表.md`、`05_實驗設計與評估.md`、第01-04章、任何程式碼或文獻檔案。完成後確認 `git diff --stat` 範圍正確。commit 前不需要額外詢問，但 push 需要使用者另行同意。
