# 第七章：結論與未來工作

> 狀態：🟡 已回灌 RQ1 的 B0／B1 對照結論；其餘 RQ 依完成度列為限制或未來工作。

## 7.1 研究總結

本章優先回應第一章的 RQ1-RQ3 核心承諾；目前已能對 RQ1 的 B0／B1 對照下結論，其餘未完成項目仍明確列為限制或未來工作。

## 7.2 研究貢獻重申

TODO：呼應 1.3。

## 7.3 研究限制

以下限制呼應第六章 §6.4，並把已完成的 RQ1 對照與仍未完成的強基準區分開來：

- **RQ1 對照基準**：B1（當代文字 RAG 強基準，見 5.4.1）已完成，因此 RQ1 的結果可以支持「KG 相較無檢索有增益」的判定，但不能支持「KG 相較做得夠好的 chunk-RAG 有整體優勢」；原評分器下 KG 相對 B1 為 −8 題，規則 R 敏感度分析後仍為 −6 題，且後者的配對顯著性不再成立。B2（Agentic RAG）仍未對照，故不能把 RQ1 延伸為已涵蓋完整 Agentic RAG 的結論。
- **RQ3 完成度**：僅實作生成端一次性糾正（接地核對 → 限制性重生 → 2b 定向修訂），未實作 3.6 節設計目標的「擴大 BFS 回補檢索 + 多輪迭代」自我精煉迴圈。2026-09-09 報告 38 partial-KG 窗口驗證顯示：生成端一次性糾正（2b）「無害但無感」，端到端正確性增益主要來自檢索側（乾淨重抽＋範圍下推）而非重生成側——已據此停止再優化 2b（見 3.6）。
- **單一 LLM 判斷循環性**：生成端與其下游多個判斷關卡均由 `qwen2.5:7b` 擔任（見 3.8），specialist 失效與生成者＝核對者共謀風險，視 5.6.1 驗證結果決定折扣幅度。
- **增量更新**：不具備圖層級增量更新，文件變更走重抽（見 4.9）。
- **RQ4a/RQ4b/RQ5/RQ6**：依實際完成度分別歸入限制或 7.4 未來工作。

## 7.4 未來工作

TODO：v1 `04_對標NotebookLM_不足分析與完全超越方案.md` 中未納入本論文正式驗證範圍的方案，都歸類到這裡，包括但不限於：

- 方案二：多模態 Layout-Aware 表格解析
- 方案四：社群自動導讀與見解生成器
- 方案七：階層式社群摘要全局檢索
- 方案八：自適應圖優化與人類反饋學習
- 若 RQ6（向量剪枝）最終未納入正式貢獻，也歸入此處，並說明已完成的設計程度；RQ5（時序管理）與 RQ4a/RQ4b 尚未完成的部分同樣依實際進度歸入限制或未來工作。
- **檢索文件範圍的自舉缺陷（2026-09-09，報告 38 §4.1；⚠️ 動機案例經兩輪訂正，見 `docs/報告/41_...md` §9）**：現行 `_relevant_doc_ids_from_facts()` 以 `vector_search_facts()` 的 top-N 反推 BFS 的文件範圍（§3.2 §b L1、§4.7.1），一般性風險是當問句與一大群「概念相近但語意不同」的事實都近時會反推出錯範圍、濾掉正解——這仍是本論文 §1.1 已引用、但尚未接進方法章的兩個既有現象——**「檢索漂移」（Retrieval Drift，Ma et al., 2026）**與**「Lost-in-Retrieval」（Zhu et al., 2025，ACL 2025）**——在本系統查詢端的理論性展現。**原先援引的實測案例（報告 38 Q5 grounding 0/17）的診斷經查證更新**：曾一度被（後來已推翻的）報告42 訂正為生成端問題；直查原始檢索資料後確認**檢索端診斷方向本來就是對的，只是真因比本項描述的「範圍下推」更上游**——目標事實在 `vector_search_facts()` 全域語意排名僅第 41 名，`top_k=20` 根本撈不到，範圍下推邏輯（無論新舊）都無米下鍋。**這代表 Q5 不屬於本項「範圍下推」修法能解決的問題**，需另立一個獨立的未來工作項：Fact-RAG 語意排名健壯性（候選方向：比照 B1 的 hybrid BM25+embedding 檢索、大幅提高候選 `top_k` 交下游處理、或結構化欄位查詢）。本項修法本身（報告41／L9 V1）已完成設計並跑過獨立 harness 驗證（8 題 7 題命中 gold、1 筆 regression 26-Q7，未達 regressed=0 驗收標準），是否接線待決，但不再由 Q5 佐證。未來工作維持：(1) 檢索範圍改用 BFS seed／concept 路由命中的文件，含如何處理 26-Q7 的 regression；(2) 精讀 Zhu et al.（2025）的子問題／實體錨定機制，評估其對本系統分解路徑（§3.6 §M1）的可遷移性；(3) 對「效力起算 vs 期間起算」這類近義概念做更細的檢索區分；(4) **新增**：Fact-RAG 全域語意排名對跨文件近義雜訊的健壯性（Q5 的實際修法方向）。

- **暫名 GAP-S3-01：法律條文上下文呈現與生成引用率**：報告78 §10.3 顯示，chunk-RAG 保留完整法條段落時，條文中的共享結果、款次與「準用／前項」等跨句綁定較不易被生成端遺漏；KG 事實清單拆成獨立行後，可能出現事實已進 prompt 但答案仍遺漏或抹平的情況。`docs/報告/58_圖遍歷端到端關聯Chunk提領與雙軌上下文組裝SDD任務書.md` 已有相關設計提案，但該報告本身已誠實訂正其文獻佐證有過度宣稱；提案中的具體參數（例如提領上限 3–5 個 chunk）是未驗證的假設值，後續若實作必須先做獨立校準，不能把報告58當成已建立的理論或實證基礎。
- **GAP-S3-02：語意 span 評分器的表面形式脆弱性**：`18-Q6` 與 `57-DIST2` 在規則 R 下翻盤，顯示答案可能已表達正確法效果，卻因「合計／累計」或「給予／可獲得」等表面差異被判為 missing。規則 R 仍只是報告62 §14.11 已裁示不採用的樣本內診斷工具；若要更確定 KG 是否真的沒有增益，應做獨立標註者評分、擴大題組或其他獨立評分方式與檢定力設計，而不是把規則 R 結果當作最終真值。
