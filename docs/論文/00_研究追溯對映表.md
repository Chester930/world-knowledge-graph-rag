# 研究追溯對映表：文獻、方法、專案、程式與測試

> 更新：2026-09-09（跨章一致性稽核回灌報告25§4／26／27／28／29——見 `docs/報告/31_論文正文跨章一致性稽核報告.md`；報告 32／33／34／35／36 已於 §5.2／§5.3.6／§5.4.1／line 65 各自回灌；本次回灌報告 38 §4／§4.1／§6 的 A–D——見 `docs/論文/03_變更紀錄.md` 2026-09-09「報告 38」條）
>
> 更新：2026-09-14（回灌報告 39/41/42/43/45/46/49/50 全量實證發現與 30-32 號文獻：納入自適應檢索行為樹 BT、確定性接地守衛〔Maynez 2020 內生性幻覺、Dahl 2024 法律引用幻覺〕、Fact-RAG 語意排名健壯性兩路修正〔§3.2 §e，Louis 2024, Reuter 2025 DRM, Cormack 2009 RRF〕、法規結構感知切塊與主旨錨定〔§3.1.2，Prior 2026, Ford 2026, Contextual Retrieval〕、原始法規 exact_span 原子真值〔Min 2023 FActScore〕與 M1~M4 三階段生命週期成本矩陣；26-Q5 上游檢索失效已於 §3.2 §e 立案修復，確定性守衛動機改引報告42 §7.4 之 K-2b 條號幻覺案例）
>
> 更新：2026-09-16（**回灌報告 51-59 尚未進入本表的評測架構與設計提案**——先前僅入庫`文獻與專案查核表.md`／`docs/報告/`工作筆記，本表長期落後，此次補回索引層級的追溯條目，正文段落改寫仍待後續，見 §5「目前發現的追溯缺口」新增第4點）：①原子事實評估與解耦評測架構（第72列）擴充為納入Context Quality矩陣（Context Recall／SNR／Chain Completeness，報告57任務A）與`mechanism_tags`十維標籤／`trap_claim_spans`拒答假陽性修復（報告57任務B/§4.3）；②新增第73列「上下文精煉：同源多樣性抑制與檢索鑑別力增強」，對應報告57 §7（KG角色再收斂：從推理鏈建構者轉為上下文精煉與路由者）與today's `source_doc_cap`／`vector_search_facts(hybrid=True)`兩個prototype；③新增文獻7篇（`docs/參考文獻/33`／`34`／`35`號資料夾：Think-on-Graph 2.0、KAG、PARIS、EasyEA、MMR、Ross et al. 2026、DF-RAG，見報告58/59核實訂正與文獻查證）尚未回灌02文獻探討正文，僅列入`文獻與專案查核表.md`。
>
> 本表是 01–04 章之間的追溯索引。它把「理論依據」、「參考專案」、「第三章方法」、「第四章實作」與「測試證據」分開標示，避免把概念上的參考誤寫成直接採用，也避免把已完成的工程機制誤寫成已完成的研究實驗。

## 1. 對映規則

| 標記 | 意義 |
|---|---|
| 理論／文獻 | 支撐為什麼採用某個方法、問題定義或評估方式的學術來源。 |
| 直接專案／套件 | 程式實際 import、呼叫或依賴的開源專案／套件。 |
| 架構參考 | 借鏡資料流、介面或系統概念，但本專案沒有直接使用其程式碼。 |
| 方法啟發 | 文獻或專案提供設計方向；目前實作是本專案自行設計的變體。 |
| 自有設計 | 找不到可誠實宣稱為直接來源的單一專案；應標示為本研究的工程設計，而不是硬湊參考專案。 |
| 已實作 | 有目前程式入口，且有測試或可由程式碼直接核對。 |
| 部分實作 | 有底層機制，但正式研究流程、上游接線或端到端實驗尚未完成。 |
| 設計預留 | 01–03 有研究設計或文獻依據，但目前沒有可執行的完整程式入口。 |

章節縮寫：`01` = 緒論、`02` = 文獻探討、`03` = 系統設計與方法論、`04` = 系統實作。

## 2. 研究問題總對映

| 研究問題 | 01 研究問題 | 02 文獻依據 | 03 方法位置 | 04／程式現況 | 判定 |
|---|---|---|---|---|---|
| RQ1 | KG-BFS 與純向量 RAG 的任務依賴性差異 | 2.2.1、2.2.2、2.2.3、2.6.5.1（Louis 2024, Reuter 2025 DRM, Cormack 2009 RRF）；GraphRAG、PathRAG、LightRAG 等；B0/B1 對照＝DPR（Karpukhin 2020）／RRF（Cormack 2009）／Han 2025；B2 對照＝ReAct（Yao 2023）／Adaptive-RAG（Jeong 2024）／Fan 2026 | 3.2§b、3.2§c、3.2§e；§5.4.1（B0/B1/B2 強基準） | `services/svo_service.py::bfs_query()`、`vector_search_facts()`；`routers/agent.py::chat()`；**B0/B1 檢索側＝`services/baseline_rag_service.py`＋`build_baseline_chunk_index.py`（2026-09-10，+12 測試）；B2 證據蒐集側＝`services/agentic_baseline_service.py`（2026-09-10，+13 測試，`retrieve`/`reflect` 注入）；§5.3 掃描 harness＝`run_chunk_size_sweep.py`** | KG-BFS＋Fact 檢索已接線；B0/B1 檢索側＋B2 證據蒐集側已實作、生成側共用函式＋真實 reflect prompt＋harness＋跑數未完成；完整基準比較尚未完成 |
| RQ2 | 多知識庫路由的效能與取捨 | 2.2.1、2.2.2；GraphRAG、semantic-router／namespace 類架構 | 3.2§a | `repositories/concept_repo.py` 已有 ConceptNode 向量索引／KNN 基礎；`services/concept_engine.py::route_kgs()` 尚未完成路由接線 | 基礎已實作；完整研究功能未完成 |
| RQ3 | 自我精煉檢索是否降低幻覺 | 2.2.1；Self-RAG、FLARE、IRCoT；生成端強化另見 2.4.7（RAGAS Faithfulness、Context-faithful Prompting、Least-to-Most、Self-Ask、RRF） | 3.6（含「§生成端複合問題子問題分解」） | 已有 confidence、bounded BFS、Fact 檢索、事實接地核對（`is_claim` 三分類）＋限制性重新生成、`_arrange_fact_lines()` v2 融合排序、規則式複合問題分解（M0＋M1）；尚無正式「評估→改變檢索→再次評估」迴圈（無信心評分、無擴大 BFS 分支、無多輪迭代收斂）。**2026-09-09 報告 38 partial-KG 窗口驗證：生成端 2b 定向修訂「無害但無感」，端到端增益主要來自檢索側，使用者確認停止再優化 2b（見 3.6）** | 生成端強化已實作；RQ3 正式自我精煉迴圈未完成 |
| RQ4a | 受控語意關係詞彙標準化 | 2.2.4；Schema.org、ConceptNet、OpenIE、AutoRE、KGGen | 3.3；工程機制另見 3.1.3 | `core/constants.py::SVO_REL_TYPES`、`services/svo_service.py`、`services/expand_governance_service.py` | 機制已實作；RQ 正式消融實驗未完成 |
| RQ4b | 實體指代、別名與跨文件對齊 | 2.2.5；UD、Text2KGBench、CORE-KG、entity alignment 文獻；**模糊合併守衛另見 Mrkšić 2016、Tosun et al. 2026、Chapman et al. 2001 NegEx（2026-09-05 新增，2.4.5 對應段落）** | 3.4；工程機制另見 3.1.4 | `pronoun_resolution_service.py`、`entity_registry_service.py`、`svo_service.py::merge_entity()`／`resolve_entity_name()`（模糊合併守衛：`_QUANTITY_PATTERN`／`_MEASURE_PATTERN` 量詞守衛〔報告25 §4 發現3/發現C，2026-09-03〕＋`_RANGE_COMPARATOR_PATTERN` 裸數字比較詞早退守衛〔報告29 §4.3，2026-09-05，commit `df2bc62`〕） | 部分實作；正式 NER 上游與端到端實驗未完成；⚠️ 剩餘缺口：不含任何量詞的範圍修飾短語（「每一型式」vs「每增加一種型式」）——對應的 `_SCOPE_MODIFIER_PATTERN`（報告29 §4.1）為設計提案、待全量重抽完成後才上線 |
| RQ5 | 事實時序保留與查詢過濾 | 2.2.6；T-GRAG、Graphiti 等 | 3.5 | 治理提案有 `resolved_at` 等一般系統時間戳；但 Fact／關係沒有有效起訖時間、衝突版本與時間條件查詢 | 有一般時間欄位；RQ5 核心功能未完成 |
| RQ6 | Hub Node 的向量引導圖剪枝 | 2.2.7；2.4.8；PathRAG；G-Retriever（prize 機制，12_）；CatRAG 僅能作概念參考，不能當作已核實直接專案；RRF 融合另見 Cormack et al. 2009（2.4.7／2.6 段） | 3.7；查詢端 L0/L1 見 3.2 §b；L2 prototype 見報告27 §7.2 | **L0/L1 查詢端剪枝已實作**（報告27，2026-09-04；門檻於 2026-09-05 §6.2 單題敏感度測試後由 degree 200／per-seed 60 收緊為 100／30）——`bfs_query()` 自適應跳數、`scope_doc_ids` 範圍下推、`_drop_hub_seeds()`（degree > `_SEED_MAX_DEGREE=100` 樞紐種子剔除）、`per_seed_limit=_BFS_PER_SEED_LIMIT=30`。**L2（向量引導 prize 剪枝）prototype 已落地**（2026-09-08，`bfs_query()` 三個 keyword-only 參數 `prize_top_k`／`question_vector`／`embedding_provider`，預設 None＝零行為變化；懶惰擴展時對候選 2-hop 邊算 `cosine(問題向量, natural_text)` 取 top-`_BFS_PRIZE_TOP_K`）；**`chat()` 未接線、dormant**。**RQ6 正式研究貢獻＝L2 接線 + k 校準 + 相對 naive 基準消融仍未完成**，全量重抽 B 驗證未完成，門檻常數僅單題驗證 | L0/L1 已實作；L2 prototype 已落地但未接線；RQ6 核心（L2 接線 + 消融）未完成 |

## 3. 由方法到程式的逐項追溯

| 03 方法／功能 | 理論／文獻（02） | 專案關係 | 實際程式入口（04） | 測試證據 | 現況與可宣稱範圍 |
|---|---|---|---|---|---|
| 文件解析、URL／多媒體轉純文字 | 2.4.1 的文件解析與 RAG ingestion 文獻 | `parser/` 是本專案程式；解析器不是宣稱照抄單一論文 | `parser/core.py`、`services/ingestion_service.py`、`parser/chunk_writer.py` | `tests/parser/`、`tests/services/` 中 ingestion／parser 測試 | 已實作；可宣稱輸入與 chunk staging 已接線 |
| 多型態來源與結構保留 | 2.6.4：JSON-LD 1.1、R2RML、Direct Mapping、SHACL、PROV-O、Data on the Web Best Practices、JSON Schema | W3C／官方規格是設計依據；四層資料模型、MappingPlan 與 TextProjection 是本專案自有整合設計，未直接採用其 runtime | 03 §3.1.5；目前尚無 `SourceAdapter`／`SchemaRegistry`／`CanonicalRecord` 的正式程式入口 | 尚無結構化來源 adapter 測試 | 設計已補入 01–03；尚未實作，不得在 04 宣稱 JSON／SQL／API 匯入已完成 |
| 句子感知切塊 | Dense X Retrieval、chunking 相關文獻（02.4.1） | 方法為本專案的 sentence-aware 變體；不是某個套件的直接實作 | `parser/core.py::sentence_aware_chunking()` | parser／ingestion 測試 | 已實作；chunk size／overlap 仍是工程參數 |
| CMP／prototype 文件分類 | Snell et al. ProtoNet；Reimers & Gurevych 的 sentence embedding | `semantic-router` 僅為架構類比；本程式是自行實作 centroid／prototype cosine | `services/classify_service.py::compute_kg_prototype()`、`classify_document()` | `tests/services/test_classify_service.py` | 已實作；閾值尚未由正式實驗校準 |
| AUTO／SUGGEST／POOL 三層分流 | 信心分層與 human-in-the-loop 設計脈絡 | Paperless-ngx 是產品流程參考，不提供本專案的三層演算法；分層規則是自有設計 | `services/classify_service.py`、`routers/staging.py`、`models/knowledge_graph.py` | classify／staging 相關測試 | 已實作；不能寫成 Paperless-ngx 的直接功能移植 |
| UMAP＋HDBSCAN 暫存池分群 | McInnes et al. HDBSCAN、UMAP | `hdbscan` 與 `umap-learn` 是直接套件依賴；BERTopic 是流程架構參考 | `services/cluster_service.py::cluster_vectors()`、`analyze_staging_pool()` | `tests/services/test_cluster_service.py` | 已實作；小樣本分支與參數仍需實驗評估 |
| 主導子群命名 | Khandelwal et al. 的 dominant-subcluster 命名方向 | BERTopic 是主題命名／管線參考；LLM 命名 prompt 是自有工程 | `services/cluster_service.py::_suggest_name()` | cluster service 測試 | 已實作候選生成；不是無人監督自動建 KG |
| 人工確認與分群落地 | human-in-the-loop 工程脈絡 | 無需硬指定單一學術專案；確認 API 是自有設計 | `routers/staging.py::analyze_clusters()`、`confirm_cluster()` | staging／cluster 測試（若路由測試未覆蓋，需補列） | 已實作；前端調整後以確認內容為準 |
| 抽取任務佇列與恢復 | 增量處理／可靠背景工作的工程脈絡 | SQLite queue、Worker 狀態機是自有設計；不是宣稱採用 Spark 或某個商用隊列 | `services/task_queue_service.py`、`services/extraction_worker.py`、`main.py::lifespan()` | `test_task_queue_service.py`、`test_extraction_worker.py` | 已實作；壓力與多 Worker 競爭尚未做正式評估 |
| SVO／OpenIE 抽取 | OpenIE；AutoRE、KGGen 作為抽取與 schema 方向參考 | LLM provider 與 JSON parser 是本專案實作；沒有直接使用 AutoRE／KGGen 程式碼 | `services/svo_service.py::extract_svo_triples()`、`svo_preprocessing_service.py` | `test_svo_service.py`、preprocessing／chunking 測試 | 已實作抽取管線；依 provider 輸出品質而定 |
| 受控關係型別與未知型別仲裁 | ConceptNet、Schema.org；AutoRE、KGGen、零樣本關係分類文獻 | ConceptNet 是關係詞彙參考；`EXPAND` 治理流程是自有設計 | `core/constants.py::SVO_REL_TYPES`、`svo_service.py`、`expand_governance_service.py`、`expand_worker.py` | `test_svo_service.py`、`test_expand_governance_service.py`、`test_expand_worker.py` | 工程機制已實作；RQ4a 比較實驗未完成 |
| 代名詞／指代前處理 | Universal Dependencies；coreference／entity linking 文獻 | POS／正則與可選 LLM 消解是本專案混合設計；若使用 spaCy，只能標 direct dependency | `services/pronoun_resolution_service.py::resolve_coreference_pipeline()` | `test_pronoun_resolution_service.py` | 機制已實作；正式 extraction 目前未完整注入 mentions |
| 實體別名與標準名提升 | entity alignment、Text2KGBench、CORE-KG | HippoRAG／KG schema 可作架構參考；頻率與長度提升規則是自有設計 | `services/entity_registry_service.py`、`svo_service.py::merge_entity()` | `test_entity_registry_service.py`、`test_svo_service.py` | 部分實作；需把 NER 上游與門檻實驗補齊 |
| 事實合併與 citation 保留 | Knowledge Vault、Wikidata 的事實／來源觀念 | Neo4j 是直接儲存專案；citation JSON、Fact 節點雙層模型是自有設計 | `svo_service.py::merge_triples_to_graph()`、`backfill_fact_nodes()` | `test_svo_service.py`、document record 測試 | 已實作；citation 排序仍有限制 |
| Fact 向量化與語意檢索 | KAPING、HippoRAG、G-Retriever、LightRAG | Neo4j vector index 是直接使用；檢索組合是本專案變體 | `svo_service.py::embed_svo_chunks()`、`vector_search_facts()` | `test_svo_service.py`、embedding／repository 測試 | 已實作；尚非完整 RQ1 基準實驗 |
| 文件識別碼與跨階段 Chunk 對齊 | 可追溯資料管線的一般設計 | 決定性 UUID 是自有設計，沒有對應單一專案 | `services/document_record_service.py::document_uuid()` | `test_document_record_service.py` | 已實作；是 Fact／Chunk 對映的必要工程修正 |
| KG 內 BFS（自適應跳數＋範圍下推） | GraphRAG、PathRAG、LightRAG 的圖檢索脈絡；報告27 | Neo4j 是直接專案；BFS 查詢、去重、L0/L1 剪枝是本專案實作 | `services/svo_service.py::bfs_query()`（`hops` 語意／`scope_doc_ids`／`per_seed_limit`）、`routers/agent.py::_drop_hub_seeds()` | `test_svo_service.py`、`tests/routers/test_agent.py`（含 L0/L1 共 9 項新測試） | 已實作；目前限定單一 KG、literal/語意 fallback seed；全量重抽 B 驗證與門檻校準待補 |
| 問句關係型別連結 | STAGG、Falcon、SLING 等 entity／relation linking 脈絡 | 沒有直接移植上述專案；embedding＋LLM 仲裁為本專案變體 | `svo_service.py::resolve_query_relation_type()`、`routers/agent.py::chat()` | `test_svo_service.py`、`test_agent.py` | 已接線；查詢排序與多跳精煉未完成 |
| 生成端事實清單組裝、排序與融合 | KAPING（2.6.5）、Lost-in-the-Middle／Jin et al. zigzag（2.4.7）、RRF（Cormack et al. 2009，2.4.7）、GraphRAG 綜述鄰居爆炸（Han et al. 2024/2025，2.4.7） | RRF `k=60` 有文獻背書；`_BFS_KEEP_MAX`／名額分配／截斷 K 為本專案佔位值 | `routers/agent.py::_split_fact_lines()`／`_arrange_fact_lines()`／`_score_lines_by_embedding()`／`_rrf_order()`／`_litm_reorder()`／`_is_contentful_line()` | `tests/routers/test_agent.py`（arrange 系列） | 已實作；K 值敏感度校準（報告25 §6）待第五章 |
| 事實接地核對＋限制性重新生成 | RAGAS Faithfulness（Es et al. 2023，2.5）、Chain-of-Verification（Dhuliawala et al. 2023）、拒答綜述（Wen et al. 2025）、Context-faithful Prompting／RefusalBench（2.4.7） | RAGAS Faithfulness 演算法由離線指標延伸為線上執行期核對；`is_claim` 三分類、CoVe factored 為本專案變體 | `services/verification_service.py::verify_fact_grounding()`、`routers/agent.py::chat()::_stream()`／`_build_constrained_prompt()`／`_targeted_correction()` | `test_verification_service.py`、`test_agent.py` | 已實作（方案 B＋`is_claim`＋2b 定向修訂）；只重新生成一次、非多輪收斂。**partial-KG 窗口（報告 38 §4，2026-09-09）已驗：2b 達設計目的（不再把對草稿改壞）但端到端無感（`regenerated` 比例未降、轉正題主靠乾淨重抽），使用者確認停損**；joint-vs-factored-vs-2b 三臂 A/B 仍為第五章消融待辦 |
| 生成端複合問題子問題分解 | Least-to-Most（Zhou et al. 2023）、Self-Ask／Compositionality Gap（Press et al. 2022）（2.4.7） | 捨棄兩篇的 LLM 分解與循序依賴鏈；規則式分句＋各自獨立生成為本專案自有設計 | `routers/agent.py::_split_into_subquestions()`／`_generate_decomposed_answer()`／`_generate_decomposed_constrained_answer()`／`chat()` | `tests/routers/test_agent.py`（分解系列） | 已實作（M0＋M1）；真實驗證樣本數小，不宣稱完全解決 |
| 事實自然語言化與遺漏防護 | WebNLG（Gardent et al. 2017）、Retrieve-Rewrite-Answer（Wu et al. 2023）、FABLES／RHO（2.4.7）；索引時預存先例 microsoft/graphrag `community_report.py` | 即時路徑＋一次性回填批次任務比照 `backfill_fact_nodes()` 雙軌；規則式遺漏偵測為報告20「量詞接地核對」的鏡像 | `svo_service.py::_naturalize_triple()`／`backfill_natural_text()`／`_naturalization_dropped_quantity()`、`routers/agent.py::_merge_fact_lines()` | `test_svo_service.py`、`test_agent.py` | 已實作＋全 KG 回填；遺漏偵測尚未套用到已回填的既有邊 |
| ConceptNode 跨 KG 路由 | GraphRAG／多 KG namespace 設計脈絡 | semantic-router／WhyHow.ai 可作架構參考；目前沒有可宣稱的直接採用 | `repositories/concept_repo.py` 僅有索引／KNN；`services/concept_engine.py::route_kgs()` 為 stub | repository／agent 測試只能證明基礎，不證明正式路由 | 尚未實作，RQ2 不可宣稱完成 |
| 自我精煉檢索 | Self-RAG、FLARE、IRCoT | 這些是 trigger／iteration 參考專案；尚未實作 graph-specific loop | 目前無正式入口；`routers/agent.py::chat()` 明確為暫時單次流程 | 無 RQ3 端到端測試 | 設計預留，RQ3 不可宣稱完成 |
| 時序事實 | T-GRAG、Graphiti | 為研究設計參考，未直接採用其 schema 或 runtime | 目前無時序寫入／過濾入口 | 無 RQ5 測試 | 設計預留，RQ5 不可宣稱完成 |
| 向量引導圖剪枝（L2，懶惰擴展時對候選 2-hop 邊算 cosine 取 top-k） | PathRAG；G-Retriever prize 機制（He et al. 2024，12_）；CatRAG 僅作待核實概念參考 | 沒有直接採用 PathRAG／G-Retriever 程式碼；L2 為 prize 機制扁平化簡化（無 PCST、無 GNN） | prototype：`bfs_query(prize_top_k=, question_vector=, embedding_provider=)`（預設 None＝零行為變化，報告27 §7.2）；`chat()` 未接線 | `test_svo_service.py`（L2 prototype +2；L0/L1 共 9 項） | L0/L1 已實作；**L2 prototype 已落地但 `chat()` 未接線**；L2＝RQ6 正式貢獻（接線 + 消融）仍不可宣稱完成 |
| BFS 遍歷延遲與扇出控制（L0/L1） | 報告27；GraphRAG 綜述（Han et al. 2024/2025，2.4.7 段）「鄰居爆炸」問題陳述 | Neo4j Cypher；自適應跳數與範圍下推為本專案實作 | `services/svo_service.py::bfs_query()`（`hops` 語意／`scope_doc_ids`／`per_seed_limit`）、`routers/agent.py::_drop_hub_seeds()` | `test_svo_service.py`／`test_agent.py`（9 項新測試） | 已實作（查詢端）；全量重抽 B 驗證與門檻校準待補 |
| 設定分層與領域可插拔化（`KGConfig`／`ConfigSource`／domain pack／逐階段評分閘控生命週期） | Hydra／OmegaConf／Kustomize／spaCy config（分層組合、領域 fallback）；Dynaconf／pydantic-settings（可插拔多來源）；DSPy（metric-driven per-module 優化）；Czitrom 1999（OFAT vs 析因）；config drift（膨脹緩解）——見 2.6.8 | pydantic-settings 為直接依賴（`core/config.py` 已用）；`KGConfig`／`ConfigSource`／domain pack 組合與「逐階段評分閘控客製化生命週期」為本論文自有設計（後者查無有命名的既有 pattern） | **第 1／2／3a／5 步已實作**（2026-09-08，`c15c468`…本批）：`core/kg_config/`（`KGConfig` frozen model + `ConfigLoader` 四層 merge + `ConfigSource`：`Dict`／`FileConfigSource`）；查詢端 θ 家族 + 生成端 `system_context`（5 個 prompt builder）改讀 `cfg`；`config/kg/<id>.*` 覆蓋 per-KG 值、`config/domain_packs/<name>.json` 為領域包，`KnowledgeGraph.domain_pack` 欄（Neo4j 節點屬性，經既有 update 端點）選包，`chat()` 據此載入。第 3b（`_svo_prompt` few-shot）／4（DEDUP 門檻+守衛）待主 drain；5b（`kg_profile` schema 版本號）＋逐階段評分閘控生命週期尚未做；**2026-09-14 新增 `ChunkingConfig` 分區（3.9.7），已實作＋測試，尚未接線**。詳見 3.9 / 4.10 / `docs/報告/33` | `tests/core/test_kg_config.py`（golden test：`KGConfig()` == live 常數 + `domain.system_context` == `_TAIWAN_CONTEXT_INSTRUCTION`；deep-immutability；合併語意）＋ `test_agent.py`／`test_svo_service.py`／`test_kg_repo.py`（cfg 接線、domain pack 選擇、per-KG profile 生效 +8）；多 KG 併發 smoke 待第 3b/4 後 | 部分實作（第 1／2／3a／5 步）；工程架構決策，不另立 RQ；逐階段評分閘控生命週期為自提方法論框架（見 3.9.5）；跨領域 domain pack 有效性列為第七章未來工作，不在本論文實證宣稱範圍 |
| 自適應檢索行為樹（Adaptive Retrieval BT，場景分層動態路由） | Adaptive-RAG（Jeong et al. 2024）、Fan et al. 2026；報告 39/42 實證之 Pareto 最優邊界分析 | Behavior Tree 架構沿用專案工程慣例；Type-A~E 場景梯度與動態退化/升級為本專案自有設計 | 03 §3.2 §d；`services/query_classifier.py`、`services/adaptive_retrieval_service.py`（**2026-09-14 已實作並通過單元測試，`chat()` 尚未接線**；`QueryClassifier` 的 Type-C 判斷規則目前對題庫過擬合，正式接線前需重新設計） | `tests/services/test_adaptive_behavior_tree.py`（5 項） | 設計已規範入 03 §3.2 §d 與 05 §5.2；模組已實作、尚未接線，非 Pareto 最優之現行架構 |
| 確定性接地守衛（日期起算錨點、條號、數值區間、禁止斷言） | Maynez et al. (2020) 內生性幻覺、Dahl et al. (2024) 法律引用幻覺（見 02 §2.4.7.1，文獻 30）；RefusalBench（2024）、Jitkrittum et al.（2023）專門化判定失效理論；報告42 §7.4 實測 K-2b 條號幻覺與數值誤植（原引 26-Q5 語意平滑化案例已依報告41 §9 訂正，該案例真因是檢索端排名，非接地核對失效） | `cn2an` 為直接依賴；四道規則式確定性守衛為本專案自有設計，取代純 LLM 軟性自我查驗 | 03 §3.6 §確定性接地守衛；`services/deterministic_guard_service.py`、`services/refusal_guard.py`（**2026-09-14 已實作並通過單元測試，`chat()` 尚未接線**，現行接地核對仍是既有 `verify_fact_grounding()` LLM Judge） | `tests/services/test_deterministic_guards.py`（6 項） | 四道守衛已實作、尚未接線；起算日錨點守衛為前瞻性防護（防範 Maynez 2020 內生性平滑化），條號／數值守衛有報告42 §7.4 真實案例佐證（吻合 Dahl 2024 引用幻覺） |
| Fact-RAG 語意排名健壯性修正（選項A BM25/Fulltext 混合 ＋ 選項B 法規代碼前綴） | 2.6.5.1：Louis et al. (2024) 通用向量與 BM25 混合互補、Reuter et al. (2025 DRM, SAC)、Cormack et al. (2009) RRF 融合；文獻 31（`docs/參考文獻/31_Fact-RAG語意排名健壯性/`） | Neo4j fulltext index 為直接儲存功能；RRF 演算法為自有函式（參考 Cormack 2009）；前綴注入為自有工程設計（參考 SAC Reuter 2025） | 03 §3.2 §e；`services/svo_service.py::create_fact_fulltext_index()`、`_rrf_fuse_fact_ids()`、`vector_search_facts(hybrid=True)`；`embed_svo_chunks()` 前綴注入 | `tests/services/test_svo_service.py` | 核心模組已實作並通過單元測試；端到端 RQ1 消融實驗與極短 SVO 三元組（10~30字）BM25 鑑別力評測列為第五章待辦 |
| 法規結構感知切塊與主旨錨定（Header Anchoring，任務 B） | 2.6.7：Prior et al. (2026) 德國民法結構單元切塊實證、Ford et al. (2026) 多法域切塊、Anthropic (2024) Contextual Retrieval、LangChain MarkdownHeaderTextSplitter；文獻 32（`docs/參考文獻/32_法規結構感知切塊與主旨錨定/`） | 規則式主旨提取為自有設計（去 LLM 簡化版）；ArticleAwareChunking 為自有結構感知切塊 | 03 §3.1.2；`parser/core.py::article_aware_chunking()`、HeaderAnchoring 模組（commit `76dbbba`） | `tests/parser/`、`tests/services/` | 部分實作；結構感知切塊已實作，法規 KG 平均 145 字元天然免疫主旨遺失，Header Anchoring 保留給通用散文語料 |
| 原子事實評估與解耦評測架構（exact_span 原文標註與檢索／生成因果解耦） | 2.5.1：Min et al. (2023) FActScore 細粒度原子事實理論；報告 41/42/43/48/49/50；**2026-09-15 擴充：評測解耦升級為「Context Quality矩陣」（報告57任務A）**——Context Recall（既有，語意NLI fallback）＋新增 SNR（訊噪比，命中字元數/總檢索字元數）＋Chain Completeness（跨文件`source_law`分組覆蓋率），三項並列於`atomic_score`矩陣，不互相取代 | exact_span 為本研究第一性原理評測規範，直接以法規條文原文為唯一真值，嚴禁 LLM Judge 動態生成參考答案；SNR／Chain Completeness 為本研究自有指標設計 | 02 §2.5.1；**05 §5.5.1a（2026-09-16 新增正式章節，含 K arm 現行基準實測：Context Recall 27.8%／SNR 7.7%／Chain Completeness 33.3%，n=3）**；`report39_comparison/formal_20260913/manifest.json`、評測基準檔案；**`services/lineage_tracker.py::_compute_snr()`／`_compute_chain_completeness()`（2026-09-15）、`scripts/eval/run_rq1_comparison.py::_render_pareto_summary()`「Context Quality矩陣」區塊；`models/eval_schema.py::TestCase.mechanism_tags`（十維檢索機制挑戰標籤，複選，見05附錄A §A.3.3）／`trap_claim_spans`（2026-09-16，canary_refusal 拒答假陽性確定性修復）** | `report39_comparison/` 評測 harness；`tests/services/test_lineage_tracker.py`／`tests/services/test_evaluation_metrics.py`（新增18＋3項） | 已確立評測標準；將檢索端召回率與生成端 SLM 解讀能力因果解耦，杜絕評審模型自身幻覺污染；**題庫由32題擴充至46題（verified 10→27），涵蓋5個新mechanism_tags維度（alias_mapping／coreference_resolution／distractor_adjacent／canary_refusal近似誤導型／global_aggregation＋cross_doc_multihop）**；952 pytest綠燈 |
| 上下文精煉：同源多樣性抑制與檢索鑑別力增強（`source_doc_cap`／`vector_search_facts(hybrid=True)`） | MMR（Goldstein & Carbonell 1998，`docs/參考文獻/35_`）；Ross et al. (2026) 實證同源冗餘內容無助於答案準確度；DF-RAG（Khan et al. 2026）固定λ baseline；Fact-RAG 語意排名健壯性（Louis 2024、Reuter 2025 DRM、Cormack 2009 RRF，見第70列，2.6.5.1） | RRF 演算法沿用既有；`_apply_source_doc_cap()` 為 MMR/DF-RAG baseline 精神的規則式簡化版自有設計，非直接複用其 centroid 距離公式 | 03 §3.2 §f；04 §4.7.1；`services/svo_service.py::vector_search_facts(source_doc_cap=, hybrid=)`、`_apply_source_doc_cap()`（2026-09-16，prototype，預設關） | `tests/services/test_svo_service.py`（source_doc_cap 6 項＋既有 hybrid 4 項） | **研究背景（KG角色再收斂，報告57 §7，2026-09-16）**：現代 LLM 在小範圍文字內的邏輯推理能力已足夠，KG 角色從「推理鏈建構者」收斂為「上下文精煉與路由者」——評測重點從「最終答案正確性」轉為「檢索到的上下文本身是否精準完整」；`57-AGGR2` 真實案例證實 Fact 檢索側（`vector_search_facts()`）完全無精煉機制，同源主題相近但答非所問的 Fact 稀釋掉跨文件關鍵事實。兩個 prototype 已用獨立腳本驗證檢索端確有改善（`hybrid=True`：3個gold fact中2個進top-20；`source_doc_cap=5`：命中文件數5→8份），**但尚未接線進`chat()`**。**2026-09-16 追加**：正文骨架已補齊（03 §3.2 §f 設計依據、04 §4.7.1 實作段落、05 §5.2.2／§5.5.1a）。**端到端 K vs K+hybrid+cap 消融已執行完畢（n=3 題、單次 run）**：`hybrid=True` 讓 Context Quality（檢索階段、不受逾時汙染的量測）全面下滑（27.8/7.7/33.3%→16.7/3.4/16.7%），與單元測試層級「候選池命中率 0/3→2/3」的樂觀結果方向相反；`source_doc_cap=5` 大致持平。**使用者 2026-09-16 裁示暫緩接線 `chat()`**，prototype 程式碼保留、預設關閉；n=3 題單次 run 樣本量過小不足下定論，重新評估需 `--runs 3` 或題庫擴充後的更大樣本 |

## 4. 「尚未實作」的精確定義

本表中的「尚未完成」不是指專案完全沒有相關程式，而是指尚未完成該研究問題所需的端到端功能與實驗閉環：

| 層級 | 判定方式 | 目前例子 |
|---|---|---|
| 基礎零件 | 已有可呼叫的函式、索引、資料欄位或限制機制 | ConceptNode KNN index、confidence、BFS hops、Fact vector search、`resolved_at` |
| 部分流程 | 零件已串接，但缺少正式研究流程、上游接線或完整查詢語意 | RQ2 ConceptNode index、RQ3 單次檢索、RQ4b entity registry |
| 完整研究功能 | 研究問題定義的演算法已接入、可跑完整資料流程，且有比較實驗與指標 | 目前 RQ2、RQ3、RQ5、RQ6 尚未達此層級 |

因此論文應使用「基礎機制已實作，但完整研究功能尚未完成」，而不要籠統寫成「完全沒有實作」。

## 5. 目前發現的追溯缺口

### 高優先度

1. 01–03 的 RQ 對映已經存在，但原先缺少一張可直接查到「程式檔案與測試」的總表；本文件補上此索引。
2. RQ2、RQ3、RQ5、RQ6 的文獻與方法設計存在，但目前沒有完整實作，04 已改為明確列出未完成邊界；後續不得用「系統已支援」描述這四項。
3. RQ4a／RQ4b 的底層機制已存在，但正式研究問題仍需要實驗組別、基準方法、資料集與指標；「有程式」不等於「研究問題已驗證」。

### 中優先度

1. 目前程式的核心入口缺少一致的追溯註解，閱讀者必須來回搜尋 02、03、04 和 tests；已在關鍵入口補上 `Traceability` 註解。
2. `03_系統設計與方法論.md` 仍保留部分歷史性「尚未實作」敘述，雖已有訂正說明，但後續宜把正文直接改成目前狀態，以免讀者誤判。
3. 參考專案必須標示「直接依賴」或「架構／方法參考」。特別是 semantic-router、Paperless-ngx、BERTopic、Self-RAG、FLARE、IRCoT、PathRAG，不應在 04 寫成目前程式直接使用的套件或完整移植。
4. **（2026-09-16新增）報告51-59整批架構性設計與實測結果，只回寫進本表索引層級（第72/73列），尚未回寫`03_系統設計與方法論.md`／`04_系統實作.md`／`05_實驗設計與評估.md`正文段落與`02_文獻探討.md`的7篇新文獻**——本表可作為之後撰寫正式章節的索引依據，但目前查找這些主題（Context Quality矩陣、KG角色收斂定調、`source_doc_cap`/`hybrid`精煉機制）仍必須直接讀`docs/報告/57`與`docs/參考文獻/33`~`35`，正文章節查無對應段落。建議待`hybrid`/`source_doc_cap`端到端驗證完成、KG角色收斂定調有實測數字支持後，再一次性回寫正式章節，避免章節寫完又因驗證結果需要大改。

## 6. 程式註解格式

關鍵入口採以下格式，註解只負責導航，詳細論證仍放在論文：

```python
# Traceability:
# - Thesis: 02 §2.4.x -> 03 §3.x -> 04 §4.x
# - RQ: RQx（若為工程支援而非已完成研究，明確註明）
# - Literature: 作者／年份或附錄編號
# - Project: direct dependency / architecture reference / own design
# - Tests: tests/...
```

註解中的 `Project` 若不是 direct dependency，必須使用 `architecture reference` 或 `own design`，以維持論文的可驗證性。

## 7. 最終判定

01、02、03 的主要研究問題與章節關係目前可以對齊；04 應作為「03 方法在目前專案中的實作對映章」，而不是另一篇獨立的理論章。然而「每個方法都有直接對應專案」這個命題不成立，也不應強行成立：部分方法是直接使用套件，部分是借鏡既有系統，部分是本研究自行設計。論文若照本表區分三者，研究主張會比把所有名稱列為採用專案更準確。
