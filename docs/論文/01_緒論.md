# 第一章：緒論

> 狀態：🟡 草稿。研究問題框架是討論到一半的產物，尚未經過你明確確認，請優先審閱本章。

## 1.1 研究動機

TODO：從 v1 `docs/報告/01_專案總覽...md` 第二章「為什麼」的論證鏈改寫——但要從「工程決策說明」改寫為「學術研究動機」的語氣，強調現有 RAG／GraphRAG 方法在**多知識庫、多租戶場景**下的已知限制，而非單純的專案設計說明。

草稿要點（待你補充/修改）：
- 傳統向量 RAG 缺乏多跳推理與可解釋性（可引用 Lewis et al. 2020 RAG、Edge et al. 2024 GraphRAG 作為背景）
- 現有 GraphRAG 方法（如 Microsoft GraphRAG）多針對**單一大圖**設計，較少討論**多個獨立知識庫並存**時的路由與查詢效率問題
- 圖遍歷在遇到 Hub Node（高連結度節點）時容易發生路徑爆炸，現有工程實務多採 naive 硬截斷，缺乏系統性的剪枝設計與驗證

## 1.2 研究問題（草稿，待確認）

統整框架：**一個核心貢獻、四個可獨立消融驗證的設計元件**。

> 為什麼這樣框架：四個項目原本個別提出時，容易變成「逐一宣稱優於某個對象」的鬆散比較（見 v1 `04_對標NotebookLM...md`，該文件是產品競爭分析語氣，不適合直接搬進論文）。改為「同一系統的四個元件、共用一套消融實驗」後，可以用一套實驗骨架同時回答四個 RQ，範圍更可控，也更符合系統類論文的常見寫法（例如 Microsoft GraphRAG 論文本身即採消融設計）。

**核心貢獻主張**：一套面向多知識庫企業場景、強調可解釋性與低幻覺率的混合式 GraphRAG 架構。

| # | 研究問題 | 對應系統元件 | 現況 |
|---|---|---|---|
| RQ1 | 多知識庫場景下，「輕量路由層 + BFS 知識層」的兩層架構，能否在準確率不顯著下降的前提下改善查詢延遲與可擴展性？ | ConceptNode 路由（`concept_engine.py`） | v1 已實作，v2 待遷移 |
| RQ2 | 封閉式 30 種語意關係抽取，相較開放式關係抽取，對多跳問答準確率與可解釋性的影響（trade-off 召回率）為何？ | SVO 抽取（`svo_service.py`） | v1 已實作，v2 待遷移 |
| RQ3 | 相較單輪檢索，自我精煉迴圈（低信心時動態回補原文）能否顯著降低幻覺率？延遲/成本代價為何？ | 自我精煉迴圈 | v1 已實作，v2 待遷移 |
| RQ4 | 相較 naive 硬截斷（`_PER_SEED_FACT_LIMIT=20`），向量引導的圖剪枝能否在 Hub Node 場景下提升事實相關性、同時控制延遲？ | 圖剪枝（尚未實作） | **完全未實作**，v1 為硬截斷，v2 為 stub |

**風險提示（待你決策）**：
- RQ4 目前沒有任何實作可供測試，若列為正式貢獻，需要先在 v2 的 `services/svo_service.py`（或新模組）中實作向量剪枝，才能進入實驗階段。
- 四個 RQ 全部納入，對碩士論文是偏滿的工作量。建議的收斂順序（可討論）：RQ1-3 優先做完整實驗，RQ4 視進度決定是否留在正式章節，或改列為「已提出設計、留待未來驗證」。

## 1.3 研究貢獻

TODO：待 1.2 確定後，用一段話總結「這篇論文對這個問題領域增加了什麼」——注意要對應具體、可驗證的貢獻，不要用「完全超越 XXX」這類行銷語言（v1 的 04 文件在這點上需要避免直接搬用其語氣）。

## 1.4 研究範圍與限制

TODO：明確排除項——例如多模態表格解析（04 文件方案二）、社群摘要全局檢索（方案七）、人類反饋權重學習（方案八）等，若不在本論文驗證範圍內，應在此明確聲明為「未來工作」，避免讀者誤以為是本論文的貢獻。

## 1.5 論文架構

TODO：一段話帶過各章內容（可等其餘章節大綱定案後再寫，通常是全篇最後寫的一節）。
