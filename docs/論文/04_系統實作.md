# 第四章：系統實作

> 狀態：⚪ 大綱。待 v2（`智慧知識庫3`）核心演算法從骨架階段填入實作後再撰寫細節，目前先列出對應章節與程式碼位置。

## 4.1 開發環境與技術棧

TODO：對應 v2 `README.md` 技術棧表格（FastAPI、Neo4j、sentence-transformers 等）。

## 4.2 資料層設計

TODO：`core/database.py`（Neo4j 連線、KG 專用資料庫管理）、`models/`（Pydantic 資料模型）。

## 4.3 路由層實作（對應 RQ1 / 3.2）

TODO：`repositories/concept_repo.py`、`services/concept_engine.py`——目前 v2 為 stub，待實作後補上具體 Cypher 查詢與評分公式。

## 4.4 SVO 抽取實作（對應 RQ2 / 3.3）

TODO：`services/svo_service.py`——目前 v2 為 stub。

## 4.5 問答與精煉迴圈實作（對應 RQ3 / 3.4）

TODO：`routers/agent.py`、SSE 串流機制。目前 v2 為 stub。

## 4.6 圖剪枝實作（對應 RQ4 / 3.5，若納入）

TODO：新模組，v1、v2 皆未實作。

## 4.7 前端實作

TODO：v2 `ui/` 模組化前端（KG 側欄、暫存區、聊天串流），可對比 v1 單檔 HTML 的差異，作為「架構嚴謹化」的佐證素材。

---

**寫作提醒**：本章應以「做了什麼、怎麼做的」為主，避免重複第三章已經講過的「為什麼」；程式碼片段只放關鍵邏輯（例如 Cypher 查詢、評分公式），不要整段貼原始碼。
