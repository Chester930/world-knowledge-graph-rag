# Agent Continuation Instructions

Before continuing active work in a new terminal or agent session, read [`HANDOVER.md`](HANDOVER.md). It is the canonical, vendor-neutral record of the current branch, completed work, experiment evidence, artifact locations, and next step.

Then read the linked reports as needed, especially report 57 §4.13 and report 60 §1.4. Check `git status -sb`, the current branch, and `git log -1` before making changes. Preserve existing untracked files and do not push without explicit user authorization.
