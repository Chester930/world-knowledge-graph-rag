# Gemini CLI Project Notes

Before resuming work in this repository, read [`HANDOVER.md`](HANDOVER.md). It is the canonical cross-agent progress record for Claude Code, Codex, Gemini, and other agents.

Verify the active worktree, branch, and Git status before editing. Preserve pre-existing untracked experiment artifacts. Do not push a new commit without explicit user authorization.
